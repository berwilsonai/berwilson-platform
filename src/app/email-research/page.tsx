import { redirect } from 'next/navigation'

// Merged into Email Intake (2026-07-03) — research + paste + review live on one page.
export default function EmailResearchRedirect() {
  redirect('/intake')
}
