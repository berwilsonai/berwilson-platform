# BER WILSON — Executive Intelligence Platform
# Master Architecture & Build Reference (CLAUDE.md)
# Version: 2.0 | 2026-06-22

---

## WHAT THIS FILE IS

Canonical reference for every Claude Code session working on the Ber Wilson platform. Lives in the project root as `CLAUDE.md`; Claude Code reads it automatically.

**Builder:** Richard (EVP, Ber Wilson) — builds in Claude Code terminal. Not a developer.
**Golden rule:** Never introduce complexity without demonstrated need. Every decision reversible or portable.

> **Version 2.0 note:** This file was rewritten on 2026-06-22 to match the code as it actually is. The v1.0 reference described a Claude-Haiku/Sonnet app that no longer exists — runtime AI is now **Google Gemini**. If anything below disagrees with the code, the code wins; fix this file.

---

## HOW TO DEPLOY TO PRODUCTION (read this when Richard says "deploy" / "ship it" / "make it live")

**This exact folder — `/Users/richardwhite/berwilson-platform` on the Mac Studio — IS production AND the git repo.** Editing here + the three steps below is a full deploy. There is no separate server. (Set up 2026-08-25: Richard works directly on the Studio; the old MacBook→Studio rsync flow and the stale `~/Documents/berwilson-platform-main` copy are both retired/deleted.)

When Richard asks to deploy, run these from `/Users/richardwhite/berwilson-platform`:
```bash
git add -A && git commit -m "<what changed>"          # save + version
git push origin main                                   # back up to GitHub (berwilsonai/berwilson-platform, public)
export PATH="$HOME/.node/bin:$PATH" && npm run build   # build (PATH must include ~/.node/bin)
launchctl kickstart -k gui/$(id -u)/com.berwilson.platform   # restart the live app (launchd keeps it on :3000, tailnet-only)
```
Then confirm: `git log --oneline -1` and check the app responds. If `npm run build` fails, do NOT kickstart — the old build stays live; fix the error first. Always `git pull --ff-only` before starting a fresh editing session. Full host/infra detail lives in the `self-hosted-mac-studio-deployment` memory.

---

## 1. PROJECT IDENTITY

**Company:** Ber Wilson — vertically integrated construction, development, and USA prefab steel manufacturing. Salt Lake City, UT.
**Website:** berwilson.com
**Platform:** Internal executive intelligence tool for two executives managing a multi-sector construction pipeline (government contracting, infrastructure, real estate development, prefab manufacturing, institutional).
**Core problem:** Two people managing billions in pipeline across federal bids, PE negotiations, JV structures, subcontractor relationships, and manufacturing coordination. They need a single AI-powered source of truth that thinks like a construction COO.

**Where it's headed:** The near-term job is a clean CRM — track projects, tasks, people, and the company itself so the executive team can steer from a high level. The long-term job is an intelligence layer: load an emailed opportunity (text or RFP docs) into "Ber AI," have it assess the opportunity against Ber Wilson's capabilities and appetite, and recommend whether to pursue and spin up a project. The proposal-intake flow (§6) is the first working slice of that.

---

## 2. TECH STACK

| Layer | Technology | Notes |
|-------|-----------|-------|
| Database + Auth | **Self-hosted Supabase** (Postgres, Auth, Storage) on the Mac Studio | Docker (Colima) lean stack, tailnet-only at `:8443`. RLS enabled on every table (but see §8 — app traffic uses the service role). pgvector enabled. Cloud project paused as rollback safety net. |
| Frontend | Next.js 16 (App Router, TypeScript) | Server components default. Client only for interactivity. |
| Styling | Tailwind CSS v4 + shadcn/ui (`@base-ui/react`) | No custom CSS unless unavoidable. |
| Runtime AI | **Local Qwen via LM Studio** (`src/lib/ai/local.ts`) | All runtime AI. Gemini path dormant behind `AI_PROVIDER` flag (web research only). See §5 + AI Model Rules below. |
| Microsoft Graph | Microsoft Graph API (OAuth) | Powers `/calendar` meeting prep, party enrichment, and **on-demand Email Research** (`/email-research` — human-triggered `$search` over the connected mailbox, human-confirmed intake). The automatic email-to-task scraper was removed 2026-06-25 and stays removed. |
| Vector Search | pgvector inside Supabase Postgres | `text-embedding-qwen3-embedding-0.6b`, 768-dim (truncated + renormalized). |
| File Storage | Supabase Storage (`documents` bucket) | Organized by project / entity / site ID. |
| Deployment | **Mac Studio, tailnet-only** (launchd + `tailscale serve`) | `zsh deploy/deploy-to-studio.sh` from the MacBook. Vercel deleted 2026-07-07; `git push` = GitHub backup only. Crons are launchd agents on the Studio. |

### AI Model Rules (CURRENT — FULLY LOCAL since 2026-07-07)

**The platform is fully self-hosted and fully local-AI as of the 2026-07-07 cutover** (Richard's decision: absolute security, nothing leaves his hardware). Production = the Mac Studio, tailnet-only: app at `https://richards-mac-studio.tail0e5306.ts.net/`, self-hosted Supabase at `:8443`, LM Studio on localhost:1234.

- **All runtime AI** → `qwen/qwen3.6-35b-a3b` via LM Studio's OpenAI-compatible API (`AI_PROVIDER=local`, `src/lib/ai/local.ts`). Expect ~30–60s on extraction-class tasks (reasoning-heavy model, ~75 tok/s generation).
- **Embeddings** → `text-embedding-qwen3-embedding-0.6b`, truncated+renormalized to 768 dims (schema unchanged). The whole knowledge base was re-embedded locally at cutover (213 chunks). **Never mix embedding models** — a model change means wipe + re-embed (`deploy/reembed.mjs`).
- **PDFs** → local text extraction via `unpdf` (no model call for transcription); images need a vision model loaded in LM Studio.
- **Web research / enrichment** (`research.ts`) → blocked in local mode unless `LOCAL_ALLOW_WEB_RESEARCH=true` (uses Gemini + Google Search; only the query leaves, never platform data). Currently ON — Richard kept the Gemini key for the Enrich Profile buttons on contacts/vendors (verified live 2026-07-07).
- The Gemini path in `gemini.ts` still exists behind the flag but is dormant; cloud Supabase project `qauclkrdejgtpywqixho` is **paused** (restorable safety net); the Vercel project is **deleted**. Every AI call still logs to `ai_queries`.

Anthropic Claude is not used at runtime (removed 2026-06-22).

**Dormant Gemini-path reference (pre-cutover roles; today only web research touches Gemini):**

- **`gemini-2.5-flash`** → Extraction, classification, document/cert summarization, synthesis, briefs, enrichment, research. The workhorse. Routed through `callGemini` / `callGeminiWithFile` in `src/lib/ai/gemini.ts`.
- **`gemini-2.5-pro`** → The construction-executive agent's main reasoning loop (`src/lib/ai/agent.ts`).
- **`gemini-embedding-001`** (768-dim) → Embeddings for RAG (`src/lib/ai/embeddings.ts`, direct v1beta REST call).
- **Web research** → Gemini with Google Search grounding (`src/lib/ai/research.ts`). Perplexity was the original plan and was never adopted; the file header comment still references it — ignore that.
- **Opus / any Claude model** → not used here. (Claude Code, the dev tool, is separate from the app's runtime AI.)

Every AI call logs to the `ai_queries` table (user, prompt, response, model, tokens, latency) — keep that contract when adding calls.

---

## 3. PROJECT STRUCTURE (actual)

The app has grown well beyond the original Phase-1/2 plan. High-level map of what exists today:

```
src/
├── app/
│   ├── dashboard/            # Portfolio overview (health, alerts, daily brief, needs-attention)
│   ├── attention/            # Cross-portfolio "what needs me" list
│   ├── projects/             # List (pipeline + program views) + detail tabs:
│   │   └── [id]/             #   overview, players, updates, documents, milestones,
│   │                         #   financing, diligence, entities, tasks, edit
│   ├── tasks/                # All-tasks view across projects
│   ├── timeline/             # Gantt-style timeline
│   ├── capacity/             # Capacity board
│   ├── investors/            # Capital raise pipeline (investors + investments vs parent co / project SPVs)
│   ├── proposals/intake/     # AI proposal intake wizard (RFP → assessment → create project)
│   ├── intel/                # AI query/agent surface (hybrid retrieval + agent chat)
│   ├── calendar/             # Calendar + meeting prep
│   ├── contacts/             # Global rolodex (parties) + detail
│   ├── vendors/              # Vendors & contractors (entities) + detail, federal scorecards
│   ├── company/              # Ber Wilson company profile (capabilities, certs) — UNDERBUILT
│   ├── review/               # Review queue for low-confidence AI items
│   ├── email-log/            # Ingested-email log
│   ├── activity/             # Append-only audit log
│   ├── login/, auth/         # Auth
│   └── api/                  # ~85 route handlers (see below)
├── components/               # Feature-grouped UI (projects/, dashboard/, contacts/, vendors/,
│                             #   intel/, agent/, review/, opportunities/, layout/, ui/, shared/)
├── lib/
│   ├── ai/                   # gemini.ts, agent.ts, agent-tools.ts, embeddings.ts, research.ts,
│   │                         #   proposal-matching.ts, prompts/*
│   ├── email/                # pipeline.ts, participants.ts
│   ├── integrations/         # microsoft-graph.ts, graph-search.ts, types.ts
│   ├── supabase/             # client.ts, server.ts (RLS), admin.ts (service role), types.ts
│   ├── risk-scoring.ts, rate-limit.ts
│   └── utils/                # constants, sectors, stages, activity
├── hooks/                    # use-stored-state
└── types/                    # database.ts (GENERATED — source of truth), domain.ts
```

**API routes** live under `src/app/api/`. Major groups: `ai/*` (extract, classify, synthesize, brief, draft, agent, research, meeting-prep), `proposals/*` (intake, confirm, upload-chunk), `documents/*`, `projects/*`, `parties/*`, `entities/*`, `email/*`, `cron/*`, plus per-resource CRUD.

---

## 4. DATABASE SCHEMA

**Source of truth is `src/types/database.ts` (generated).** Run `npm run gen-types` after every migration. The schema has expanded far past the original core tables; do not trust a hand-maintained list — read the generated types.

Migrations live in `supabase/migrations/` (41 as of this writing, numbered chronologically).

### Conventions (unchanged)
- All PKs: `id uuid default gen_random_uuid() primary key`.
- All timestamps: `timestamptz default now()`, UTC.
- RLS enabled on every table.
- `activity_log` is append-only — no UPDATE/DELETE policies, ever.
- `updated_at` auto-maintained by trigger; tracked tables auto-insert into `activity_log` via trigger.

### Table groups (read database.ts for columns)
- **Core CRM:** `projects` (with `parent_project_id` hierarchy, `bid_due_date`, `win_probability`, capture fields), `project_players`, `milestones`, `updates`, `documents`, `dd_items`, `financing_structures`, `compliance_items`, `project_dependencies`.
- **Tasks (2026-06-25):** `tasks` (real task model — title/what/why/how/assignee_id/project_id/due_date/status/completed_at), `task_notes` (per-task notes feed), `team_members` (assignee list, seeded Richard/Eric). Replaces the old `updates.action_items` JSON for the task UI.
- **Directory:** `parties` (people + orgs via `is_organization`), `contact_aliases`, `entities` (legal entities/vendors), `entity_projects`, `party_entities`, `certifications`.
- **Capital raise (2026-07-10):** `investors` (relationship pipeline, links to `parties`), `investments` (investor × target: parent company or project, optional `spv_entity_id` → `entities`), `investor_notes`.
- **Steel CRM (2026-07-25):** `steel_deals` (prefab steel deal pipeline: quote→engineering→order_placed→delivered→paid, lead source, salesperson FK→team_members, sqft/$SF/value), `steel_deal_notes`. Own role `steel_sales` sees only this module.
- **Dino (2026-07-28):** internal operating company (acquired plumbing/HVAC co, dinoservicepros.com — NOT a vendor). `dino_revenue` (source_type internal|external; internal → FK project_id, external → client_name; per-job or periodic lump), `dino_payments` (the $150k/12-mo obligation schedule), `dino_notes`. Tracks the internal-vs-external revenue split (show Dino its revenue increasingly comes from Ber Wilson) + money we owe them. Admin-only. See build-status.
- **Company:** `company_profile`, `media`.
- **AI / intelligence:** `ai_queries`, `chunks` (pgvector), `agent_conversations`, `agent_messages`, `research_artifacts`, `review_queue`, `risk_scores`, `proposal_intake_sessions`, `stored_briefs`, `portfolio_briefs`.
- **Microsoft Graph:** `email_tokens` (OAuth — calendar/enrichment/email research).
- **Audit:** `activity_log` (append-only).
- **RPCs:** `match_chunks`, `match_parties_by_name`, `match_projects_by_name`.

---

## 5. AI ARCHITECTURE

### Unified Gemini client — `src/lib/ai/gemini.ts`
- `callGemini({ task, systemPrompt, userMessage, userId, promptVersion?, maxTokens?, jsonMode? })` — text in, text/JSON out. `jsonMode` defaults true (uses `responseMimeType: application/json` + strips stray code fences).
- `callGeminiWithFile({ systemPrompt, prompt, file: { mimeType, dataBase64 }, userId, logLabel?, promptVersion?, maxTokens?, jsonMode? })` — multimodal: a PDF/image plus a text instruction. Used by document and certification summarization.
- Both return `{ data, model, tokensIn, tokensOut, latencyMs }` and log to `ai_queries` (fire-and-forget). When the model returns valid JSON, `data` is the parsed object; otherwise `data` is the raw string — callers should branch on `typeof data === 'object'`.

### Extraction shape
Extraction prompts return structured JSON (summary, action_items, waiting_on, risks, decisions, mentioned_parties, mentioned_projects, confidence). See `src/lib/ai/prompts/extraction.ts`.

### Agent — `src/lib/ai/agent.ts` + `agent-tools.ts`
`gemini-2.5-pro` agentic loop (up to 5 tool-call rounds). Injects the Ber Wilson company profile into context so the agent knows the company's qualifications without a tool call. Tools are declared in `agent-tools.ts` and executed via `executeToolCall`. Conversations persist to `agent_conversations` / `agent_messages`.

**Construction Executive Agent persona:** senior EVP/COO, 25+ yrs government contracting, large-scale development, design-build GC, prefab, construction finance. An owner-operator, not a consultant. Four lenses on every recommendation: **Commercial** (can we win it?), **Operational** (can we deliver it?), **Financial** (can we get paid, at what margin?), **Compliance** (can we protect the downside?). Response protocol: Situation → Risks → Recommendation → Next Decision. Never ungrounded guesses; never hide risk; never substitute for legal/tax counsel; always distinguish facts, estimates, and judgments.

### Hybrid retrieval (RAG)
Query → SQL filter → vector search (`match_chunks` RPC over `chunks.embedding`) → re-rank (recency/confidence/cosine) → top chunks into context → grounded answer with citations → log to `ai_queries`.

### Proposal matching — `src/lib/ai/proposal-matching.ts`
Dedupes an inbound opportunity against existing `projects`/`parties` (solicitation #, trigram name, location, client). Does NOT judge fit — that's `fit-assessment.ts`.

### Company context + fit assessment — `src/lib/ai/company-context.ts`, `fit-assessment.ts`
- `getCompanyContext()` builds one prompt-ready markdown block from `company_profile` + active certs (identity, capabilities, bonding, **pursuit profile**: target sectors, project-size range, geographies, delivery/contract vehicles, differentiators, disqualifiers, past performance). Single source of truth — the agent and fit assessment both use it, so they judge against the same picture. Returns `hasPursuitProfile` so callers know when the profile is too thin to judge confidently.
- `assessFit(extraction, userId)` scores an opportunity against that context (Commercial/Operational/Financial/Compliance lenses) and returns `{recommendation: pursue|consider|pass, fit_score, summary, strengths, concerns, gaps, key_questions, profile_incomplete}`. Wired into `api/proposals/intake` (non-fatal) and surfaced as a card in `ProposalIntakeWizard`. **Quality scales with how completely the pursuit profile is filled in on `/company`.**

---

## 6. PROPOSAL INTAKE (the intelligence on-ramp)

Already built, end to end:
1. `/proposals/intake` — `ProposalIntakeWizard` uploads RFP text/docs (chunked upload via `api/proposals/upload-chunk`).
2. `api/proposals/intake` — runs extraction + `proposal-matching` against the company profile, produces an assessment and a proposed set of projects/parties/entities.
3. `api/proposals/confirm` — on approval, creates the projects, contacts, and entities in one transaction.

This is the spine of the "email an opportunity → Ber AI assesses → you decide → project created" loop. As of 2026-06-22 the intake also runs a **fit assessment** (§5) — a pursue/consider/pass recommendation scored against the company pursuit profile, shown at the top of the review step. The remaining work is **data, not plumbing**: fill in the pursuit profile on `/company` (target sectors, size range, geographies, delivery/contract vehicles, differentiators, disqualifiers). Until then the assessment self-flags as low-confidence (`profile_incomplete`).

---

## 7. CONVENTIONS

### Code
- TypeScript strict mode. **Zero `any` escapes as of 2026-07-12** (was ~115 → ~23 → 0); don't add any back. Jsonb inserts use `as unknown as Json`; enum filters cast to `Database['public']['Enums'][…]`; polymorphic JSX wrappers use `ElementType`.
- Server components default; `'use client'` only when interactive.
- All DB access via Supabase clients — no raw SQL in components.
- API routes handle AI + mutations; pages fetch via server components.
- Error boundary + `loading.tsx` on every route.

### Naming
- Database: `snake_case`. TypeScript: `camelCase` vars/functions, `PascalCase` types/components.
- Files: `kebab-case` (non-components), `PascalCase` (components). API paths: `kebab-case`.

### Environment variables
```
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
AI_PROVIDER=local                # all runtime AI via LM Studio (unset = Gemini path, dormant)
LOCAL_AI_BASE_URL=               # LM Studio OpenAI-compatible endpoint
LOCAL_AI_MODEL=                  # must match LM Studio's model identifier
LOCAL_EMBEDDING_MODEL=
EMBEDDINGS_PROVIDER=             # defaults to AI_PROVIDER; NEVER flip without re-embedding (§2)
LOCAL_ALLOW_WEB_RESEARCH=        # true = Enrich Profile web research via Gemini (query-only leaves)
GEMINI_API_KEY=                  # only used for web research when the flag above is true
MICROSOFT_TENANT_ID=             # Graph OAuth (calendar, enrichment, email research)
MICROSOFT_CLIENT_ID=
MICROSOFT_CLIENT_SECRET=
MICROSOFT_WEBHOOK_SECRET=
CRON_SECRET=                     # Bearer auth on cron routes; launchd cron agents on the Studio send it
APP_URL=                         # tailnet base URL for links in outbound notifications (task digest "Open my tasks")
SUPABASE_DB_URL=                 # optional; local Postgres URL for `npm run gen-types` (self-hosted, replaces --linked)
MAP_PMTILES_PATH=                # optional; /map detail basemap archive (default ~/berwilson-data/maps/us.pmtiles)
MAP_WORLD_PMTILES_PATH=          # optional; /map world-overview archive z0-7 (default ~/berwilson-data/maps/world.pmtiles)
MICROSOFT_SECRET_EXPIRES=        # optional YYYY-MM-DD; Azure client secret expiry — /settings/health warns 30d ahead
BACKUP_DIR=                      # optional; nightly-backup dir the health page checks (default ~/Backups/berwilson)
GOOGLE_LEAD_MAILBOXES=           # mailbox(es) swept for INBOUND LEADS (default info@berwilson.com) — kept apart from the deal mailboxes
GMAIL_LEAD_EXCLUSIONS=           # optional; overrides the lead sweep's Gmail-side marketing filter (default: -category:promotions -category:social -label:bw-filtered)
LEADS_NOTIFY_EMAIL=              # optional; where scored leads are announced. UNSET = nothing is sent (the queue still fills)
GOOGLE_DRIVE_KNOWLEDGE_FOLDER_ID=# optional; Drive folder indexed nightly into the company KB so lead fit scores are grounded. UNSET = sync no-ops
DINO_LEAD_EMAIL=                 # optional; where plumbing/HVAC leads are forwarded (Dino has no platform access). UNSET = Forward errors
GOOGLE_CHAT_WEBHOOK_URL=         # optional; Chat space lead digests are ALSO posted to. Carries its own auth token — treat as a secret. UNSET = email only
GOOGLE_CHAT_WEBHOOK_URL_<KEY>=   # optional; a second space, addressed by <KEY> (e.g. STEEL) without touching the notify layer
LEAD_GMAIL_LABELS=               # optional; "off" stops writing the triage verdict back as a Gmail label on info@ threads. Unset = labelling ON
```
`ANTHROPIC_API_KEY` and `PERPLEXITY_API_KEY` are no longer used — remove from any new env files. The n8n-era vars (`N8N_*`, `INGESTION_INBOUND_SECRET`) are gone from Vercel (verified 2026-07-03). `NEXT_PUBLIC_SITE_URL` is no longer referenced anywhere (the agent self-fetches that used it were refactored to direct lib calls 2026-07-03).

### Git
- Main branch: `main`. Pushing is GitHub backup only (Vercel deleted 2026-07-07) — **deploying = `zsh deploy/deploy-to-studio.sh`** after pushing.
- Conventional commits: `feat:`, `fix:`, `refactor:`, `docs:`. Never commit `.env.local`.

---

## 8. SECURITY / COMPLIANCE

- Middleware (`middleware.ts`) gates every route: unauthenticated users are redirected to `/login`. Public exceptions: login/auth, the risk-scores cron, and the Microsoft OAuth callback.
- **RLS is defense-in-depth, not the active boundary.** App traffic — including ~33 server pages — uses the service-role admin client (`lib/supabase/admin.ts`), which bypasses RLS. The auth boundary is the middleware. This is an accepted trade-off for a 2-user internal tool; if the team grows or sensitive data lands, move pages to the RLS-respecting server client (`lib/supabase/server.ts`).
- `documents.classification` flags standard vs sensitive. All AI calls logged to `ai_queries`.
- US-only infrastructure. **Do not store CUI** until a GovCloud migration is done. GovCloud trigger: annual DoD revenue > $2M OR a contract requiring CMMC L2. Stack is portable by design (standard Postgres, no vendor lock-in).

---

## 9. KNOWN DEBT / AUDIT NOTES (2026-06-22)

Captured so future sessions don't rediscover them:
- ~~Scope sprawl~~ **LARGELY RESOLVED 2026-07-03:** the Equity & Valuation and Portfolio site-hierarchy modules were removed entirely (~13.5K lines across code + generated types), along with background checks, vendor scorecards/reviews, the Procore stub, and dead scraper-era tables/functions. Nav is 15 → 13 destinations. If a CFO joins and needs basic finance tools/reports, build a fresh purpose-built section then — do not resurrect the old modules.
- ~~Overlapping "attention" surfaces~~ **RESOLVED 2026-07-03:** `/attention` folded into Dashboard (Needs Attention panel + sidebar badge on Dashboard), `/capacity` folded into `/tasks` (per-person workload chips); both old routes redirect. `/timeline` left primary nav (linked from the Projects toolbar). One attention engine remains: `/api/attention` (also feeds the agent's `get_attention_items`).
- **Two directory concepts** — **surface consolidated 2026-07-03:** one nav destination (`/contacts` = "Directory" with Contacts | Vendors & Contractors tabs via `?tab=vendors`; the `/vendors` list redirects there, detail/new routes stay). The *data model* is still two tables (`parties` + `entities`); a full data merge into `parties` remains optional future work — only if the split causes real pain.
- ~~Type drift~~ **RESOLVED 2026-07-12:** every stale `as any`/`: any` removed (the generated types had long covered `project_dependencies`/`stored_briefs`); `src` now has zero `any` escapes. Keep it that way (§7).
- ~~Speculative early-build features~~ **RESOLVED 2026-07-03:** Procore stub, background checks, and vendor scorecards/reviews removed. Party/entity enrichment (Gemini research) is real and stays.
- ~~Legacy `action_items`~~ **FULLY RETIRED 2026-07-03.** Reads moved to the `tasks` table earlier (via `src/lib/tasks/queries.ts`); writes stopped 2026-07-03 — manual-paste extraction creates real tasks at save time (`src/lib/tasks/from-action-items.ts`) and review-queue approval converts legacy pending items. `20260704000004_drop_action_items.sql` drops the column (code shipped first; dual-schema safe). The extraction *prompt* still returns `action_items` JSON — that's the contract feeding task creation, not schema debt. Old email-era tables `processed_emails`/`graph_subscriptions` and `updates.outlook_web_link` are dropped by `20260704000001_simplification_drops.sql`.
- **Pre-existing lint noise (recount 2026-07-05, after the lint-debt pass):** two `react-hooks/purity` errors in `src/app/dashboard/page.tsx` (`Date.now()` in a server component — rule misfire, runtime fine); 12 `react-hooks/set-state-in-effect` errors (load-on-mount fetch / localStorage hydration / sync-form-on-modal-open patterns — each needs a per-component rewrite to satisfy the React Compiler; deliberately left, they work; recount 2026-07-12); 6 `@next/next/no-img-element` warnings (Supabase Storage images — switching to `next/image` needs `remotePatterns` config; do it if image cost/LCP ever matters). Everything else was cleaned 2026-07-05 (76 → 21 problems; 20 as of 2026-07-12).
- **`team_members` is a 4th people-concept** (alongside parties/entities/stakeholders), kept tiny for fast task assignment. **Partly reconciled 2026-07-21:** `team_members.party_id` now links a task owner to their `parties` contact (quick-add + meeting owner-promotion both find-or-create the linked contact), so owner and contact are one person. Full table merge still deferred — tasks keep `assignee_id → team_members.id`; do a full merge only if the split causes real pain.

---

## 10. CONTACTS / DIRECTORY (original intent)

`parties` is the global contact directory. People and organizations both live here (`is_organization=true` for firms). A party is global — editing it updates everywhere. Contact detail shows: bio/contact info, every project they're linked to (via `project_players`) with role, updates/documents mentioning them, DD items assigned, compliance items owned. Routes: `/contacts`, `/contacts/[id]`.

Note the drift flagged in §9: `entities`/vendors partly duplicate this. The *surface* is consolidated (one Directory destination, 2026-07-03); when touching directory data code, still prefer consolidating toward `parties`.

---

## 11. DO NOT

- Add a separate vector database (pgvector is fine for years).
- Use LangChain, LlamaIndex, or any AI orchestration framework.
- Build microservices, use Docker/containers, or add Prisma/Drizzle (Supabase client + generated types only).
- Reintroduce a second AI provider SDK — runtime AI is Gemini. (Removing Anthropic was deliberate.)
- Pre-build features for future phases.
- Store CUI until GovCloud migration is complete.
- Never let mail become a record **without human review**. The webhook pipeline removed 2026-06-25 was banned because it *silently turned inbox mail into records*; that prohibition stands and is the invariant to protect.
  - **Amended 2026-08-23 (Richard's explicit direction):** the platform now DOES read both mailboxes automatically on a schedule — `/api/cron/email-sweep` sweeps all of `moose@` and `tuaone@`, summarizes every thread, and groups them into candidate deals. This is a deliberate reversal of the "human triggers each mailbox search" half of the old rule, made to populate the CRM from years of existing correspondence.
  - **What did NOT change:** the sweep only ever stages `pending` sessions. Creating a project, opportunity, party, or task still requires the human confirm step in Email Ingestion. No automatic record creation, ever.
- Reintroduce Microsoft Graph / Outlook. The platform moved to Google Workspace 2026-08-23 (service account + domain-wide delegation, read-only scopes). There are no stored mail tokens and no OAuth flow to maintain.

---

## 12. BUILD STATUS

**Reality:** well beyond the original Phase 1/2 plan. Live and in daily use, **self-hosted on the Mac Studio** (`100.86.79.4`, Tailscale-only) against self-hosted Supabase under Colima, with AI served by LM Studio on the same machine. Vercel is no longer the runtime.

**Working:** projects (CRUD, pipeline/program views, hierarchy, all detail tabs), **interactive project map (/map — offline basemap, illustrated markers, rail corridors, present mode)**, **task handoffs (waiting-on) + printable weekly report (/reports/weekly/print, per-person pages)**, **opportunities**, **investors (capital raise pipeline: relationship stages + per-deal commitments vs parent co / project SPVs; named raises w/ tranche schedules + per-raise dashboards; task tags, Ber AI tools + RAG, attention + daily-brief wiring)**, **objectives steering board (Now/Soon/Possibly + PDF export, wired into tasks/dashboard/brief)**, **steel CRM (/steel — prefab steel deal pipeline w/ its own `steel_sales` role, 2026-07-25)**, **dino (/dino — internal operating-company revenue tracker: internal-vs-external split + $150k payment schedule, admin-only, 2026-07-28)**, dashboard (single attention surface, opens with Now objectives), timeline, **team tasks** (per-person workload, project/opportunity/objective tags), **one Directory (Contacts | Vendors tabs)**, company profile (thin), review queue, activity log, manual-paste extraction (action items → real tasks), intel (RAG + streaming agent) + **ambient Ask Ber AI dock (⌘J, every page)**, **one Intake destination (`/intake`: Email | Proposal tabs, 2026-07-17)** — proposal intake → assessment → project creation, and Email Intake (in-platform **Gmail** sweep → report → opportunity/project + people + tasks). **Calendar/meeting-prep and mail both run on Google Workspace via per-mailbox OAuth (Microsoft Graph removed 2026-08-23); the email-to-task scraper was removed (see below).** Equity & Portfolio modules removed 2026-07-03 (see below).

**Done 2026-08-18 (task "why" polish + per-member task digest notifications; DEPLOYED to the Studio + MIGRATED):**
- **Context:** team is scaling as big projects land; members are getting abbreviated (`member`) logins. Two asks: make task "why" easy to capture but discreet when unused, and give each member a reliable daily push of their overdue/upcoming work (they're often off the tailnet, so the message must CARRY the tasks, not just link).
- **"Why" was already fully built** (schema + API + `BoardTask` carry `what`/`why`/`how`; detail sheet edited them). UI polish only, **no schema/API change:** (1) board tiles show `why` as a muted one-line sub-title when present (hidden for done/empty); (2) the detail sheet collapses empty what/why/how to a subtle `+ Add why` affordance (re-collapses on blur-if-empty; per-field `expanded` state seeded from loaded values); (3) quick-add gained a collapsed **"+ Add details (why / what / how)"** disclosure (collapsed by default; API already accepts the fields).
- **NEW outbound-email capability (the app had none).** Originally via Microsoft Graph `Mail.Send`; **re-pointed at Gmail `users.messages.send` on 2026-08-24** when Graph was removed (see that entry) behind a channel abstraction so Telegram can drop in later. `src/lib/notify/index.ts` (`notify({channel,to,subject,html})`; only `email` wired), `sendMail()` added to `microsoft-graph.ts`, `Mail.Send` added to `SCOPES`. **REQUIRES a one-time reconnect via `/api/email/oauth`** (health page shows a "Reconnect Mailbox" action until the token carries Mail.Send).
- **Per-member daily digest.** `src/lib/notify/task-digest.ts`: `buildMemberDigests()` groups open due-dated tasks by active member-with-email into Overdue + Due-next-7-days (quiet if neither); `renderDigestEmail()` returns subject + HTML that **lists the tasks inline** (title, days-overdue/due, project/opp tag, `why` sub-line) — the "Open my tasks" link (`APP_URL`) is only convenience. New query `fetchTasksForDigest()` in `src/lib/tasks/queries.ts` (separate from `fetchOpenTasks`/`TaskSummary` which the AI briefs share). New cron `src/app/api/cron/task-digest/route.ts` (CRON_SECRET fail-closed, `maxDuration=300`), added to the middleware public allowlist, **idempotent per member per day** via new `notification_log` table.
- **launchd cron** `com.berwilson.cron-task-digest` (**Monday 7:00am local, weekly**) — template in `deploy/`, added to `deploy-to-studio.sh`'s service loop, and **installed + loaded live on the Studio**. New env **`APP_URL`** (set to the tailnet URL in the Studio `.env.local`).
- **`/settings/health`** gained a **Task Digest** card; the mailbox card now reports the Google Workspace connection instead (the Microsoft card is gone).
- **Cloud-reference cleanup (per Richard):** `package.json` `gen-types` no longer uses `--linked` (now `--db-url $SUPABASE_DB_URL`); `README.md` rewritten from the create-next-app/Vercel boilerplate to the self-hosted reality.
- **Schema:** migration `20260818000001_notification_log.sql` — **APPLIED to the Studio Postgres + PostgREST schema reloaded**; `database.ts` hand-extended. `tsc` + `next build` clean. Verified live: `/api/cron/task-digest` returns 200 (`candidates:0` right now — no open task has a due date yet) and 401 unauth; app restarted on the new build (HTTP 200).
- **Deploy caveat:** these edits were made **directly on the Studio copy** (`~/berwilson-platform`). `deploy/deploy-to-studio.sh` rsyncs from the **MacBook source repo with `--delete`** — so the same changes must be mirrored into the MacBook repo (and committed to GitHub) or the next MacBook-driven deploy will overwrite them. **Also pending:** the one-time Microsoft reconnect for `Mail.Send`. **Future:** Telegram channel (branch in `notify/index.ts` + `telegram_chat_id` on `team_members`); per-member send prefs only if asked.


**Done 2026-07-29 (Meetings — recording upload in the create flow, Opportunity-level meetings, attendee pick lists, contact activity logging; DEPLOYED + MIGRATED):**
- **Richard's report: he went to record a meeting and saw only a "paste transcript" box — the Whisper upload/transcribe he expected looked missing.** Root cause was placement, not a missing feature: the Whisper pipeline (07-28) only lived in the meeting **detail** view (reachable after saving); the **create** form (`MeetingForm`) offered just a paste box. Fixed + three additions he asked for. His picks (AskUserQuestion): **audio + video** recordings (afconvert extracts the audio track from .mp4/.mov — verified afconvert lists `mp4f` decode); tagging a contact **only logs the meeting** on their profile (no auto-add to project players).
- **One-shot recording upload in the create form:** `MeetingForm` gained a "Recording (audio or video)" picker; on **Create record** it saves the meeting, uploads the file to the new meeting id (reuses the exact `signed-upload-url` → `uploadToSignedUrl` → `POST /api/documents {meeting_id}` flow from `MeetingFiles`), then fires `POST /api/meetings/[id]/transcribe` and advances to the detail view (which already polls `transcription_status`). Copy tells the user Whisper writes the transcript + summary automatically.
- **Video support:** `MeetingFiles`/`MeetingDetail` treat `audio/*` **or** `video/*` as the transcribable "recording" (input `accept="audio/*,video/*"`, inline `<video>` player); transcribe route allows video mime; `whisper.ts` unchanged (afconvert decodes the container). Bucket already unrestricted + 500MB on the Studio.
- **Opportunity-level meetings (3rd scope):** migration `20260802000001_meeting_opportunity_scope.sql` (**APPLIED to the Studio DB** + PostgREST restarted; `database.ts` hand-extended) adds `meetings.opportunity_id` (FK→opportunities on delete set null) + index, widens `meetings_scope_check` to `company|project|opportunity`, and replaces the pairing constraint → `meetings_scope_target_check` (exactly the matching target set per scope). `MeetingScope`/`meetingScope`/`OPPORTUNITY_KINDS` in `meetings.ts`; `parse.ts` requires opportunity_id + index_ai defaults ON for project/opportunity; `access.ts` opportunity→`canAccessOpportunity`; embedding needs no change (opportunity meetings have project_id null + board false → document-scoped chunk). A **Meetings section** renders on the opportunity detail page via `MeetingsView scope="opportunity"`.
- **Attendee pick lists (new `MeetingAttendees.tsx`, used on all 3 scopes):** a "＋ Ber Wilson team…" dropdown (active `team_members`, carries `team_member_id`+`party_id`), a contacts-directory type-ahead (reuses the meeting-intake `ContactPicker` pattern, sets `party_id`), and manual name/title/company fallback. `MeetingAttendee` jsonb shape extended with `party_id`/`team_member_id`; `parseAttendees` carries them (accepts `title`/`company` aliases). Pages pass `teamMembers`+`contacts` via new server helper `src/lib/meetings/picker-data.ts`.
- **Contact activity logging:** new `src/lib/meetings/attendee-activity.ts` `syncAttendeeActivity` — idempotent reconcile (via `metadata->>meeting_id`) of `activity_log` rows (`table_name='parties'`, `action='meeting_attended'`) for each attendee with a `party_id`; called from POST + PATCH (when attendees change); DELETE sweeps them (no FK cascade to activity_log). Surfaced as a **Meetings section on the contact Activity tab** (`/contacts/[id]?tab=activity`) linking to the record.
- **Verified:** tsc + eslint (touched files) + full build clean; migration applied + **rolled-back live psql round-trip** (company/project/opportunity inserts succeed, opportunity-without-opportunity_id rejected by the constraint, 0 rows persisted); PostgREST 200 for `meetings?select=opportunity_id`; `metadata->>meeting_id` REST filter returns the row (activity idempotency); afconvert `.mp4` decode confirmed; deployed (app 200; `/company/board` 307, `/api/meetings` 401/POST 401 unauthed). **Eyeball when convenient (authed):** create a project meeting from the form with an `.m4a` AND an `.mp4`, watch the banner poll to transcript+summary; add a team member + a searched contact + a manual attendee, save, and confirm the contact shows the meeting under Activity (and that removing them on edit clears it, no dupes); create an opportunity meeting and confirm Ber AI retrieval.

**Done 2026-07-28 (Meetings — local Whisper transcription: audio → verbatim transcript → auto-summary; DEPLOYED + INFRA):**
- **Richard's follow-up to the Meetings module: add local Whisper transcription** (more accurate than iPhone native for multi-speaker/jargon/long-form legal records). Three-artifact model he specified: **audio** (verbatim voice) + **transcript** (Whisper literal text) + **summary** (AI, useful, non-authoritative). Board **minutes** stay hand-authored; the auto-summary never touches them.
- **Whisper runs 100% locally on the Studio** — `whisper.cpp` built from source with **Metal** (cmake via a `~/whisper-venv`, since the box has no brew/cmake; `~/whisper.cpp/build/bin/whisper-cli`), model **`ggml-large-v3-turbo`** (~1.5GB, `~/whisper.cpp/models/`). **No ffmpeg** — the macOS built-in **`/usr/bin/afconvert`** decodes m4a/aac → 16kHz mono WAV, which whisper.cpp reads natively. LM Studio uses ~20GB of the 36GB RAM; turbo adds ~1.6GB only during a run (CLI per-file, no resident service). **To swap to max-accuracy large-v3 or rebuild:** `download-ggml-model.sh large-v3` + point `WHISPER_MODEL` at it. See [[whisper-transcription-studio]].
- **Code:** `src/lib/ai/whisper.ts` (`transcribeAudio(buffer, fileName)` — spawns afconvert then whisper-cli `-bs 5` for accuracy, auto language, temp-file cleanup; `whisperEnabled()` gated on `WHISPER_BIN`/`WHISPER_MODEL`). `src/lib/meetings/transcribe.ts` (`runMeetingTranscription` — background: download audio → Whisper → set `meetings.transcript` + save an **immutable `Transcript — {title}.txt` document** (doc_type `transcript`, delete-and-replace on re-run) → **auto-summary only if none yet** (reuses `extractMeeting`, never overwrites minutes) → refresh the minutes doc; settles `meetings.transcription_status` idle|processing|complete|error, never throws). New column `meetings.transcription_status` (migration extended + applied). `POST /api/meetings/[id]/transcribe {document_id, force?}` (fires the background job, no-ops if already transcribed unless `force`) + `GET /api/meetings/[id]` (poll meeting+files). Transcript doc embeds for Ber AI when `index_ai` (project-scoped chunk / is_company for board), same gating as the minutes doc.
- **UI:** audio upload in `MeetingFiles` **auto-triggers** transcription (`onAudioUploaded`); `MeetingDetail` shows a status banner (Transcribing…/ready/failed) + **Transcribe / Re-transcribe** button, and **polls `GET /api/meetings/[id]`** every 5s while processing, lifting the finished transcript + summary + new transcript file up to the parent. Env wired into the Studio by `deploy/deploy-to-studio.sh` (`WHISPER_BIN`/`WHISPER_MODEL`, Studio-specific paths).
- **REGRESSION found + fixed (was live briefly):** the meetings migration's bucket line `allowed_mime_types = allowed_mime_types || array[audio…]` — on the Studio `allowed_mime_types` was **NULL (unrestricted)**, and `NULL || array` collapses to **audio-only**, which silently began **rejecting pdf/text/docx uploads platform-wide**. Caught by the transcription test (transcript `.txt` + minutes `.md` uploads failed). Fixed: bucket reset to NULL (unrestricted, its original state — audio was never MIME-blocked there; only the 500MB size bump mattered) + the migration UPDATE made NULL-safe (`case when allowed_mime_types is null then null else … end`). **Lesson: never `NULL || array` on a bucket's allowed_mime_types.**
- **Verified end-to-end on the Studio** (alias-loader script against the live stack): real `say`-generated m4a → afconvert → whisper-cli produced an accurate verbatim transcript ("$150,000 over 12 months"), the local LLM auto-summary ran, all **three artifacts** (audio + `transcript:….txt` + `Meeting —….md`) were created and **cascade-deleted cleanly** (0 orphans); tsc+eslint+build clean; deployed (whisper env present in the running app; transcribe route 401 unauthed). **Eyeball when convenient:** upload a real `.m4a` in the UI and watch the banner poll to "Transcript ready," then confirm the transcript + summary + `Transcript —….txt` file appear.

**Done 2026-07-28 (Meetings & Interaction Records — board/governance register + per-project client-interaction log; DEPLOYED + MIGRATED):**
- **Richard's ask: track board meetings + per-project client interactions for liability/compliance, with a place to upload the iPhone audio recording + a transcript/script.** His picks (AskUserQuestion): **store audio + paste the iOS-generated transcript** (no Whisper/new AI service); **manual board minutes** (authoritative legal text, no AI wording risk) but **AI-assist for project interaction summaries**; board register lives as a **Company → Board tab**; **per-meeting "Include in Ber AI" toggle** (board OFF by default, project ON) for confidentiality.
- **Why a new module:** the existing Meeting Notes Intake (`/intake` Meeting tab, `email_intake_sessions`) is *ephemeral* — it fans a transcript onto other records and keeps no browsable meeting entity. Compliance needs a **durable, browsable record with an approval state**. New `meetings` table is that entity (NOT the intake sessions).
- **New `meetings` table** (migration `20260801000001_meetings.sql`, **APPLIED to the Studio DB** via ssh+docker psql + PostgREST restarted; `database.ts` hand-extended, `Meeting` alias): scope company|project (+project_id, check-constrained as a pair), kind board|client|site_visit|call|internal|other, title/date/time/location, meeting_type_label/chair/secretary (board), attendees jsonb, summary, **minutes** (authoritative body), **transcript** (pasted), decisions jsonb, status draft|approved (+approved_at/by), confidential, **index_ai**, minutes_document_id. Plain-text vocab + all helpers in `src/lib/utils/meetings.ts` (`composeMeetingDocument`, badge maps); shared write validation in `src/lib/meetings/parse.ts` (create + partial PATCH); access in `src/lib/meetings/access.ts` (`canAccessMeeting`: admin always, project→canAccessProject, board→admin-only). No `log_activity()` (dino/investors precedent).
- **Attachments reuse the `documents` infra** — new `documents.meeting_id` column (a meeting's files = documents where meeting_id=X, stored under `meetings/{id}/…`, project_id/is_company left null). Audio + exhibits upload via the **client-direct signed-upload path** (`/api/documents/signed-upload-url` → `uploadToSignedUrl` → register via JSON `/api/documents`, both extended to honor `meeting_id`) so big recordings bypass the serverless body limit; the bucket's 50MB `file_size_limit` was raised to **500MB** (allowed_mime_types was already NULL/unrestricted on the Studio, so audio was never MIME-blocked there). `documents/[id]` GET/DELETE gained meeting-aware access (`canViewerAccessDoc`).
- **TWO PRE-EXISTING CONSTRAINTS bit meeting-only docs (both fixed in the migration):** (1) `documents_scope_check` required project/entity/company/reference — **widened to also accept `meeting_id is not null`**, else meeting files are rejected. (2) `documents.meeting_id` FK is **ON DELETE CASCADE** (not SET NULL) — a meeting attachment is scoped only by meeting_id, so SET NULL would leave a scopeless row that re-violates the scope check; cascade deletes the files (chunks cascade in turn). The API DELETE route also sweeps the storage objects (a DB cascade can't reach storage).
- **RAG (gated by index_ai):** on save, a self-contained minutes **markdown document** is (re)generated + tagged meeting_id (`src/lib/meetings/document.ts` `syncMeetingDocument`, delete-and-replace snapshot) — doubles as the downloadable/preserved compliance export AND the embedding vehicle (a chunk's document_id satisfies chunks_source_check alone → no chunks-table migration). **Board minutes are is_company=FALSE on the doc row** (kept OUT of the general company KB browse list) but embed as an is_company chunk when opted in; project meetings embed project-scoped. index_ai off → `embedding_status='skipped'`, no chunk.
- **AI-assist:** project MeetingForm has a **"Draft summary from transcript"** button → `POST /api/meetings/draft` runs the session-less `extractMeeting()` (reused from the meeting-intake module) and prefills summary/minutes/attendees/decisions. Board meetings never get AI drafting.
- **Surfaces:** `/company/board` (admin-only by default-deny; new Board tab in `CompanySectionTabs`, gated on the admin `showProfile` flag) + a **Meetings tab on every project** (`ProjectTabBar`, after Updates; project layout already gates canAccessProject — v1 non-admins with project access READ + play recordings, writes admin-only). Both render `MeetingsView` (list → detail/form router; `MeetingForm`/`MeetingDetail`/`MeetingFiles` in `src/components/meetings/`, Panel/Chip/DatePicker/ConfirmDialog idiom; audio plays inline via an on-demand signed URL).
- **Verified:** tsc + eslint + full build clean (local + Studio); migration applied + PostgREST 200 for `meetings` and `documents?meeting_id`; **rolled-back live psql round-trip** — board+project insert, doc link, **cascade delete removes the file**, and both check constraints (scope/project pairing, widened scope-check) enforced; deployed (app 200; `/company/board` 307, `/api/meetings*` 401 unauthed). **NOT yet exercised (eyeball when convenient):** the authenticated end-to-end UI — create a board meeting from the real minutes, upload an actual `.m4a` through the signed-upload flow + play it back, and toggle Include-in-Ber-AI to confirm a project interaction becomes retrievable. **Deliberately deferred:** local Whisper auto-transcription; a top-level cross-portfolio `/meetings` register; opening project-meeting *writes* to project_managers (the `canAccessMeeting`/canAccessProject groundwork is in place — just allowlist `/api/meetings` + the pages when wanted).

**Done 2026-07-28 (Dino Service Pros — internal operating-company revenue tracker; DEPLOYED + MIGRATED):**
- **Context: Richard is acquiring Dino Service Pros (plumbing/HVAC). It's one of OUR companies now — not a vendor, not a project.** Dino does ~$2M/yr, mostly from its own existing clients; the goal is to **track revenue split by SOURCE (internal = Ber Wilson work vs external = Dino's clients)** to show Dino its business increasingly comes from us, plus track the **$150k we owe them over 12 months**. His picks (AskUserQuestion): flexible ledger (itemize internal, lump external OK); **Dino-specific** build (not a generic operating-companies framework); **$150k as an installment schedule**.
- **Placement decision (not a build, done via existing surfaces):** Dino belongs in three places — the **org-structure chart** (`/company/structure`) as a new **division** (Richard adds it — admin Add-division form, no code); the **company capability profile** (`/company` — write self-perform plumbing/HVAC into `capabilities`/`differentiators` + NAICS so Ber AI/`assessFit` factor it in); and the **opportunity** gets marked `closed_won` when the acquisition finalizes (no auto-conversion machinery exists — it's just closed in place). Analysis rejected a steel-clone (steel = quotes/SF-pricing/commissions/own sales role, wrong shape) and investors (money IN, wrong direction) — Dino is a **revenue+relationship tracker**, so it clones the *investors* module shape (money records + rollup band + notes + admin-only + attention wiring).
- **New module `/dino`** (admin-only by default-deny — NOT in any `permissions.ts` allowlist; `/api/dino` routes also carry in-route `getViewer` admin guards). Migration `20260731000001_dino.sql` (**APPLIED to the Studio DB** via ssh+docker psql + PostgREST restarted/reloaded; `database.ts` hand-extended; `DinoRevenue`/`DinoPayment`/`DinoNote` aliases): `dino_revenue` (source_type internal|external, project_id FK→projects **on delete set null**, client_name, amount, revenue_date, description, notes), `dino_payments` (label/amount/due_date/paid/paid_date — the obligation schedule), `dino_notes`. Plain-text source vocab + all math in `src/lib/utils/dino.ts` (`revenueRollup(range: ttm|ytd|all)` → total/internal/external/**internalPct**, `splitByPeriod` monthly/quarterly buckets, payment helpers); no `log_activity()` (investors precedent). Shared write validation in `src/lib/dino/parse.ts`. Payment PATCH is **partial-update** so the mark-paid toggle can't wipe the amount; note author stamped server-side from the session (never client). Deliberately deferred fast-follows: Ber AI/RAG snapshot embedding + agent tool, daily-brief Dino line, generalizing to reusable operating-companies.
- **UI (`src/components/dino/`):** `DinoDashboard` (client orchestrator, range state, `router.refresh()` after mutations) → `RevenueRollup` (band: total / Ber Wilson / external / **Ber Wilson share %** + range selector), `RevenueSplitBar` (pure CSS stacked bars, internal=primary/external=slate — the persuasion picture, no chart lib), `RevenueLedger` (add/edit/delete; internal→project picker, external→client field; DollarHint), `PaymentSchedule` (installments, owed/paid/total, mark-paid toggle, overdue amber), `DinoNotes`. Nav item in `nav.ts` (Wrench icon, primary group). **Attention wiring:** new `dino_payment` category in `computeAttention` (unpaid installments ≤30d/overdue) + dashboard **Needs Attention** "Dino Payments" section + agent `get_attention_items` enum.
- **Verified:** 28-case pure-function unit check of the rollup/split/payment math (**caught + fixed a real bug** — quarter labels rendered "3 '26" not "Q3 '26", `slice(6)`→`slice(5)`); live rolled-back psql round-trip against the Studio (internal row links to a real project, external has client_name/null project, 12 installments=$150k with owed/paid split correct, FK confirmed SET NULL, 0 rows after rollback); PostgREST exposes all 3 tables + resolves the `project:projects(...)` embed the page uses (HTTP 200); tsc + eslint (only the 2 documented dashboard purity survivors) + full build clean; deployed to the Studio (app 200; `/dino` 307 + `/api/dino/*` 401 unauthed). **Eyeball when convenient:** open `/dino` as admin, add an internal (project-linked) + external revenue row and confirm the split band/trend bar, add the $150k installments and confirm one comes up on the dashboard Needs Attention as it nears due. **Manual (Richard):** add the Dino division on `/company/structure`; add self-perform plumbing/HVAC to `/company` capabilities; mark the Dino opportunity `closed_won` when it closes.

**Done 2026-08-03 (steel — sales reps restricted to a flagged set; marketing/referral source = any contact + referral fee; DEPLOYED + MIGRATED):**
- **Richard's ask: sales reps are ONLY Colton/Richard/Eric/Jason; marketing sources can be ANY contact (typically Ber Wilson employees, but outside referrers too); track their referral fees (% of margin OR flat) with editing + tracking.** His AskUserQuestion picks reshaped the marketing model: (1) **unify** — the previously-separate internal tier-rate "Marketer" (5/4/3%) and team-member "Referred By" were **both retired** into ONE **Marketing / Referral Source = any party**, paid a referral fee (flat or % of margin); (2) sales reps managed by a **flag** (add more later in the UI), not a hardcode.
- **Data findings that made this safe:** the internal `marketer_id` tier-rate was used on **0 of 8 deals** and referral fees on **0**; current salespeople were only Eric & Richard (both now flagged reps) — so the reshape had essentially no data to migrate. Colton & Jason didn't exist as team_members and were **created** (role `steel_sales`, no login yet).
- **Migration `20260804000001_steel_marketing_source.sql` (APPLIED to the Studio DB + PostgREST restarted; `database.ts` hand-extended; non-destructive ADD-only):** `team_members.is_steel_rep` (flagged Richard/Eric, inserted Colton/Jason as steel_sales reps); `steel_deals.referral_party_id` (FK→parties **on delete set null**) + index. **Retired but kept (unused, non-destructive):** `steel_deals.marketer_id`, `marketing_rate_override`, `marketing_commission_paid`(+date), and `lead_source_id` (the old team-member referrer). `lead_source`/`lead_source_detail` **stay** as the channel categorization (relabeled "Channel" on the form), distinct from the paid source.
- **Commission math (`steel.ts`):** removed the whole tier-rate marketing path — `DealCommissionInput`/`DealFinancials`/`dealFinancials` dropped `marketing_rate_override`/`hasMarketer`/`marketingAccelerated`/`marketingRate`/`marketingCommission`; deleted `MARKETING_RATE_BY_TIER` + `marketingRate()`. `totalPayout = salesCommission + installFee + referralFee`. **Referral fee unchanged** (`referralFeeAmount(type,value,totalMargin)` — flat or % of the deal's total margin), now paid to `referral_party_id`.
- **Rollups (`rollups.ts`):** `PayoutKind` is now `sales|install|referral` (marketing gone); `payoutItems(rows, members, partyName)` resolves the referral payee from a **party** name map; `RepScorecard` = **sales + install only** (a rep's earnings; the referral fee is a per-source concept, not rep income); new **`referralSourceRollup(rows, partyName)`** → per-source owed/paid/projected/total. `financialsFor` drops marketing.
- **Form (`SteelDealForm`):** Salesperson dropdown = **steel reps only** (edit page unions in the deal's current salesperson if not flagged, so editing never silently drops them; both pages fall back to all active members pre-migration / if none flagged). Removed the Marketer field + marketing rate override. New **"Marketing / Referral Source"** section: an inline **contacts type-ahead** (`SourcePicker`, over ~60 active parties passed to the client, hidden `referral_party_id`) visible to everyone (attribution), with the **referral fee (type + value) financials-only**. `actions.ts` writes `referral_party_id` for any author; fee stays financials-gated. new/edit pages fetch `reps` (`is_steel_rep`) + `contacts` (parties) + the current source for edit prefill.
- **Surfaces updated:** deal detail (referral source resolved from parties; "Marketing / Referral Source" fact; panel props); **`SteelCommissionsPanel`** rewritten (no marketing row; `scope='self'` = a rep's sales+install only, referral is management-only; referral row shows the party name); **`/steel/commissions`** resolves referral party names + renders a new **`ReferralSourceTable`** (referral fees by source); **`/steel/earnings`** now sales+install only; list-page cards resolve the "referred" name from the party; deal PATCH dropped the marketing mark-paid flag.
- **Admin:** `/settings/users` member cards gained a **"Steel rep" toggle** (violet chip) → `PATCH /api/admin/users/[id] {is_steel_rep}` — Richard flags/unflags reps without code.
- **Verified:** tsc + eslint + full `next build` clean; migration applied (2 flagged, Colton+Jason created — the four reps confirmed = Colton/Eric/Jason/Richard); **rolled-back live psql round-trip** (insert with referral_party_id + percent fee, FK join resolves the source name, 0 rows after rollback); deployed (app 200; `/steel*` + `/settings/users` 307, `PATCH /api/admin/users` 401 unauthed). **Eyeball when convenient (authed):** open a steel deal → Edit, confirm the Salesperson list shows only the 4 reps, pick a Marketing/Referral Source from the contact search + set a % referral fee, mark the deal Paid, then check `/steel/commissions` → the referral shows in Outstanding Payouts + the Referral Fees by Source table. Note: an employee who's a marketing source must exist as a **contact** in the directory (the source picker is parties-only, by design).

**Done 2026-08-03 (steel — commission engine aligned to the founding-phase comp plan + per-deal documents; DEPLOYED + MIGRATED):**
- **Context: Richard handed me the CFS commission plan PDF + GTM brief** (`Ber_Wilson_CFS_Commission_Plan.pdf`, `Ber_Wilson_GTM_Brief.pdf` — reps Colton & Jason, $3k/mo media budget, founding-phase ramp). The CRM shipped with a generic "per-line % of margin" model that did NOT encode the actual plan. His AskUserQuestion picks + a follow-up note reshaped the build.
- **The comp plan (now the model):** `Commission = commissionable margin × RATE`, cost basis fixed $20/SF, paid on cash collected. **RATE set by total project SF** (Sales 15/12/10, Marketing 5/4/3 for <20k / 20k-100k / 100k+ SF), applies to the whole deal. **Sales + Marketing are two separate commissionable roles** (one person can hold both). **Installation/frame-assembly is billed separately and earns NO margin commission — flat $250-$500/install job to the salesperson** (Richard's explicit "separate the assembly line from the margin calc"). **Per-rep volume accelerator: +1pt once a rep collects $1M gross profit in a calendar year** (Richard's clarification: per individual rep on their own deals' profit, NOT company-wide as the PDF's original line read).
- **His decisions (AskUserQuestion):** add a first-class **Marketer role** (over reusing the referrer slot); **auto-derive rate from SF tier, editable override** (over manual %); **track install price/cost for P&L but pay fee-only**; and — the reshaping note — **track each rep's total sales / profit / commissions, reps see their own paid/owed/projected, admins mark paid, don't restrict basic users unless important.**
- **Migration `20260803000001_steel_comp_plan.sql` (APPLIED to the Studio DB via ssh+docker psql + PostgREST restarted; `database.ts` hand-extended, non-destructive ADD-only):** `steel_deals` gained `marketer_id` (FK→team_members on delete set null), `sales_rate_override`/`marketing_rate_override` (null = tier rate), `install_fee` (+`install_fee_paid`/date), deal-level `sales_commission_paid`/`marketing_commission_paid` (+dates), and `collected_date` (stamped when a deal reaches Paid — the accelerator's calendar-year basis). `documents` gained `steel_deal_id` (on delete cascade) + widened `documents_scope_check` to accept it (mirrors the meetings pattern). **Per-line `commission_pct`/`commission_paid` on `steel_deal_services` are now VESTIGIAL** (rate is deal-level) — left in place, unused; the `commissionable` flag stays (freight/permit exclusion).
- **Commission math rewritten in `src/lib/utils/steel.ts`:** `steelSizeTier`/`SIZE_TIER_LABELS`, `SALES_RATE_BY_TIER`/`MARKETING_RATE_BY_TIER`, `salesRate()`/`marketingRate()` (tier base → override wins → +1 if accelerated), `commissionableMargin()` (**excludes installation/assembly-category lines AND non-commissionable lines**), `installMargin()`, `isInstallCategory()`, `ACCELERATOR_THRESHOLD=$1M`, `INSTALL_FEE_MIN/MAX`, and a new `dealFinancials(lines, DealCommissionInput)` returning revenue/cost/margin/commissionableMargin/installMargin/salesRate/marketingRate/salesCommission/marketingCommission/installFee/referralFee/totalPayout/net. **Removed** `serviceCommission` + `DEFAULT_SERVICE_COMMISSION_PCT` (per-line commission is gone).
- **New `src/lib/steel/rollups.ts`** — single source of truth for per-rep scorecards + the accelerator + the payout worklist (pure functions, shared by the commissions page and the rep earnings page): `groupServices`, `acceleratorMap` (two-pass: collected profit is rate-independent → sets the flag → commissions computed with it), `financialsFor`, `repScorecards` (sales/profit/owed/paid/projected per role + accelerator YTD), `payoutItems` (deal-level: sales/marketing/install/referral, each backed by its own paid flag via `PAID_FIELD`/`PAYOUT_LABELS`).
- **Confidentiality shift (was: reps see zero financials):** management (admin/exec, `canSeeSteelFinancials`) sees the whole deal + company rollups + marks paid. **A rep (steel_sales) now sees their OWN cut** — the deal-detail panel has a `scope='self'` mode showing only the roles they hold, and a new **`/steel/earnings`** page (their sales/profit/commissions owed-paid-projected by role + accelerator progress bar + their deals). `/steel/commissions` (management) rewritten: headline band + payout worklist + **`RepScorecardTable`** (all reps). `/steel/commissions` now redirects a non-financials rep to `/steel/earnings`. "My Earnings" link added to the /steel toolbar for everyone; "Commissions" stays financials-only.
- **Deal form (`SteelDealForm`) rewritten:** added Marketer picker, a "Commission Rates & Install" section (tier display + sales/marketing rate overrides + install fee, financials-only) with live readback, and dropped the per-line Commission % input (assembly lines show a "flat fee, not margin-commissioned" note). `actions.ts parseFields`: marketer_id set by any author (attribution); rate overrides + install fee are financials-only (absent-key preservation for reps). Deal PATCH route stamps `collected_date` on Paid and gained financials-gated mark-paid for all four deal-level payouts.
- **Per-deal documents:** new `SteelDealFiles` component + a dedicated **`/api/steel/documents*`** API (signed-upload-url / register / GET-signed-url / DELETE) kept inside the already-permitted `/api/steel` prefix (cleaner than widening the global `/api/documents` for steel_sales). Any steel worker can attach plans/quotes/signed orders to any deal (module shares all deals); files live under `steel-deals/<id>/`, no AI embedding. Removed the now-dead `/api/steel/services/[id]` per-line paid route.
- **Verified:** 22-case pure-math unit check incl. the comp plan's exact worked example (15,000 SF @ $32/SF → **$27,000**); **live end-to-end round-trip against the Studio DB** (13/13: new-column insert, commissionable margin excludes install+freight, sales 12%/marketing 4%/install flat/referral 5%-of-total-margin, 4-item payout worklist, rep scorecard owed, `documents.steel_deal_id` insert under the widened scope check, **cascade delete → 0 orphan services + 0 orphan documents**); tsc + eslint + full build clean; deployed (app 200; `/steel/earnings` + `/steel/commissions` 307, `/api/steel/documents*` 401 unauthed). **Eyeball when convenient (authed):** create a deal with materials+engineering+assembly lines + a marketer + an install fee, mark it Paid, confirm the deal panel + `/steel/commissions` scorecards, mark a payout paid, then log in as a steel_sales rep and confirm `/steel/earnings` shows only their cut.

**Done 2026-08-03 (steel — rep workspace redesign: Kanban pipeline, My/All scope, needs-attention, tab bar; DEPLOYED, no migration):**
- **Context: granting Colton & Jason (steel_sales reps) access; Richard asked what would make the module's UX/UI best for reps managing pipelines + marketing.** The plumbing was excellent but the layout was exec-oriented (a card grid sorted by updated_at, scattered toolbar links). His AskUserQuestion pick: **Full workspace redesign.** Presentation-layer only — reuses the existing stage PATCH, `repScorecards`, overdue helper, `canWorkSteel`; NO schema/API change.
- **Kanban pipeline board (`SteelPipelineBoard.tsx`, new):** replaces the `SteelDealsClient` card grid on `/steel` as the default. Columns = the full funnel (`STEEL_PIPELINE` = quote→…→paid) + a muted **Lost** off-ramp column; each header shows count + Σ value; cards sorted soonest-next-step then value. **Drag a card to another column → advances its stage** (native HTML5 DnD, mirrors `ObjectivesBoard`; optimistic + revert on failure via the same `PATCH /api/steel/deals/[id] {stage}` the detail dropdown uses; `router.refresh()` after). Compact `PipelineCard` (name/customer/value/sqft/source chip/below-floor flag/salesperson avatar in All scope/next-step overdue amber). **Board | List** segmented toggle (List reuses `SteelDealsClient`); persisted via `useStoredState('bw.steel.view')`. Horizontal-scroll on mobile; DnD is desktop-only (touch → open the deal to change stage, noted in the UI).
- **My Pipeline vs All Deals:** `useStoredState('bw.steel.scope')`, **defaults to "mine"** when the viewer has a linked `teamMemberId` (scoped to deals they're the salesperson on); toggle to All (shared pipeline decision unchanged — it's a default view, not a restriction). All-scope adds a salesperson filter; both scopes keep a lead-source filter. Filters are now **client-side/in-board** (the old URL `?stage/source/sales` params + `SteelFilters.tsx` are gone — `SteelFilters` deleted).
- **Needs-attention strip:** chips for **overdue** (open + past next_step_date), **no next step** (open, none set), **stale** (open, no update ≥14d) computed over the scoped set; clicking a chip filters the board/list. Rep-centric **stat band**: My open pipeline · Win rate (paid ÷ decided) · **Commission owed · Projected** (viewer's own, server-computed via `repScorecards` on the pipeline page; falls back to Deals/Need-action when the login isn't team-linked).
- **Workspace tab bar (`SteelWorkspaceTabs.tsx`, new):** Pipeline · Marketing · My Earnings (· Commissions for `canSeeSteelFinancials`), New Deal pinned right — rendered on all four workspace pages, replacing their ad-hoc toolbar links / back-links (`/steel`, `/steel/marketing`, `/steel/earnings`, `/steel/commissions`). **Deliberately a shared component, NOT a `steel/layout.tsx`** — a route-group layout would wrap the chromeless `/steel/[id]/quote` print view.
- **Verified:** tsc + eslint (touched files clean) + full `next build` clean (all 6 steel routes compile); deployed to the Studio (app 200; `/steel`, `/steel/marketing`, `/steel/earnings`, `/steel/commissions` all 307 unauthed; `PATCH /api/steel/deals/*` 401 unauthed). **Authed end-to-end click-through not yet done — eyeball when convenient:** open `/steel`, confirm My Pipeline defaults + the board renders, **drag a deal to the next column** and confirm the stage sticks + toast, toggle Board/List, click a needs-attention chip, and confirm the tab bar navigates across Pipeline/Marketing/Earnings.

**Done 2026-08-03 (steel — marketing intelligence layer: ICP, spend/ROI, channel analytics, quote generation; DEPLOYED + MIGRATED):**
- **Follow-on to the comp-plan build — Richard: "scope and build all 4"** (the deferred marketing layer from the GTM brief). All four shipped in one pass.
- **Migration `20260803000002_steel_marketing.sql` (APPLIED to the Studio DB + PostgREST restarted; `database.ts` hand-extended; `SteelMarketingSpend` alias):** `steel_deals` gained `icp_segment` + `buying_trigger` (free-text, datalist-suggested like lead sources); new table **`steel_marketing_spend`** (channel × month ledger: channel/amount/spend_month[first-of-month]/description/notes, `update_updated_at` trigger, authenticated RLS mirroring steel_deals).
- **#3 ICP fields** — Buyer Segment + Buying Trigger on the deal form (datalist defaults in `steel.ts`: `DEFAULT_ICP_SEGMENTS`/`DEFAULT_BUYING_TRIGGERS`), set by any author, shown on deal detail, feed the analytics. `actions.ts parseFields` writes them.
- **#1 lead-source conversion analytics + #2 spend/ROI** — new `src/lib/steel/marketing.ts` (`channelAnalytics` → per-channel leads/won/lost/open/win%/won-revenue/avg-$SF/spend/CAC/ROAS, "won" = order placed or beyond via `STEEL_STAGE_INDEX`; `icpBreakdown` by segment/trigger; `spendByMonth` vs `STEEL_MARKETING_MONTHLY_BUDGET=$3000`). New **`/steel/marketing`** page: spend-vs-budget band + month bars (budget marker line, over-budget red), channel performance table, ICP breakdowns, and the spend ledger. **`SteelSpendLedger`** (client add/delete) + **`/api/steel/marketing-spend`** POST + `[id]` PATCH/DELETE (canWorkSteel). Confidentiality: revenue/CAC/ROAS shown to all steel workers; the **Margin-ROAS column is `canSeeSteelFinancials`-only** (channel margin is the only cost-derived figure). Marketing link added to the /steel toolbar (all steel workers).
- **#4 quote/proposal generation** — **`/steel/[id]/quote`**, a chromeless customer-facing document (layout shell exclusion extended `endsWith('/print') || endsWith('/quote')`): company letterhead from `company_profile` (dba/legal name, address, phone, email + logo), quote ref `Q-{id8}`, line items grouped **Steel Kit vs Installation (billed separately)** with subtotals + total, standard terms, `SteelQuoteToolbar` (Print/Save-PDF via `window.print`) + `ForceLightTheme`. **Prices ONLY — never cost/margin/commission** (it's customer-facing, even for financials users). "Quote" button on the deal detail header. Tab title = suggested PDF filename.
- **Verified:** live round-trip against the Studio DB (16/16: spend insert normalizes to first-of-month, Marketing channel leads=2/won=1/win%=50%/wonRev=$1.6M/margin=$800k/avg=$40SF/CAC=$3000/ROAS=533×, Website 0-won→CAC null, ICP Developer + Unspecified split, spend-by-month=$4000 vs $3000 budget; cleaned up); tsc + eslint + full build clean; deployed (app 200; `/steel/marketing` 307, `/api/steel/marketing-spend` 401, `/steel/[id]/quote` 307 unauthed). **Eyeball when convenient (authed):** set segment/trigger on a couple deals, log spend on `/steel/marketing`, confirm the channel table + spend-vs-budget bars, and open a deal's Quote → Print preview. **Note:** channel names in the spend ledger must match deal `lead_source` values to join (the datalist suggests both). Marketing spend edit is delete+re-add in the UI (PATCH route exists for future inline edit).

**Done 2026-07-26 (steel — commissions owed-logic bug fix + custom line items; DEPLOYED + MIGRATED):**
- **Richard's report: a commission showed as "needs to be paid" on the IHC Steel deal even at QUOTE stage.** Root cause: `/steel/commissions` built the payout worklist from every non-lost deal, so any deal with service prices entered produced an "owed" commission the moment prices existed — the model had **no notion of when a commission is EARNED**. Fix + a line-items redesign, both his AskUserQuestion picks: payable trigger = **cash collected (Paid stage)**; **fix bug now, then custom line items**.
- **Pass 1 (bug fix, no migration):** new `isCommissionPayable(stage)` in `steel.ts` (=== `'paid'`, the SINGLE trigger point — change here to pay at invoice instead). `/steel/commissions` now only counts **collected** deals in the payout worklist + owed/paid rollups + by-salesperson/by-referrer; **open deals surface in a new "Projected — open deals" (forecast, dashed) section** (per-salesperson list, never a payable). Headline "Net after comm." still subtracts all commitments across active deals (fully-loaded net if everything closes). Deal-detail `SteelCommissionsPanel` status pill shows **"Projected"** (neutral) until the deal is collected, then Owed/Paid. IHC at quote now shows Projected, not owed.
- **Pass 2 (custom line items — migration `20260730000001_steel_line_items.sql`, APPLIED to Studio DB + PostgREST restarted; `database.ts` hand-extended):** the fixed 3 services (materials/engineering/assembly) became a list of **custom LINE ITEMS**. `steel_deal_services` gained `description` (free text, falls back to category label) + `commissionable` (bool, the "eligible margin" toggle — pass-through costs like freight untick it → earn no commission); **dropped the `unique(deal_id, service_type)` constraint** so a deal can have multiple lines per category. `service_type` is now the CATEGORY (materials|engineering|assembly|**other**). Existing 3-service deals migrate as-is. `serviceCommission()` returns 0 when `commissionable === false`. New helpers `steelCategory()`, `lineItemLabel()`.
  - **Form (`SteelDealForm`):** dynamic add/remove line rows (description + category dropdown + price, plus cost/commission-%/commissionable checkbox for financials users); the first **materials** line's price still auto-fills from SF×$/SF and its cost from SF×$20 until edited; floor check = sum of materials-line prices ÷ SF. FormData contract: `line_count` + per-line `line_{i}_{id,description,category,price,cost,commission_pct,commissionable}`.
  - **`saveServices()` rewritten (`actions.ts`):** reconciles by row **id** (upsert existing on PK / insert new / delete removed), not by service_type. Non-financials save still preserves cost/commissionable/commission_pct/commission_paid from the existing row (a sales user can't wipe or see them); empty lines dropped.
  - **Detail panel + commissions rollups** render the line description (+ category sublabel); non-commissionable lines show **n/c** and $0 commission. "By service" rollup keys on category (now includes Other).
- **Verified:** tsc + eslint + full build clean (both passes); live round-trip against the Studio DB — two same-category `materials` rows insert (constraint gone), non-commissionable freight line → $0 commission, math correct ($245k rev → $90k margin → $9k commission → $81k net), cascade delete leaves 0 orphans; both deployed (health 200). **Eyeball when convenient:** create a deal with a few line items as admin (incl. one non-commissionable), confirm the projected-vs-owed split on /steel/commissions once it's marked Paid, and that a steel_sales session sees description/category/price but no cost/commission.

**Done 2026-07-26 (steel — $30/SF pricing floor + management-approval flag; DEPLOYED + MIGRATED):**
- **Richard's ask: the Price/SF example should read $45 (was $24.50), and anything under $30/SF must be flagged as needing management approval ($30 = the floor).** His pick (AskUserQuestion): **persisted flag + badge** (not just an entry-time warning) so execs spot below-floor deals without opening the form.
- **Floor logic in `src/lib/utils/steel.ts`:** `STEEL_PRICE_FLOOR_PER_SQFT = 30`, `effectiveSteelPricePerSqft(materialsPrice, sqft, pricePerSqft)` (materials price ÷ SF — covers both entry modes; falls back to the entered Price/SF), `isPricingBelowFloor(...)`. **Floor is strict `< 30`** — exactly $30 is allowed.
- **Persisted:** new `steel_deals.pricing_below_floor boolean not null default false` (migration `20260729000001_steel_pricing_floor.sql`, **APPLIED to the Studio DB** + supabase-rest restarted; `database.ts` hand-extended). Set at save time in `actions.ts parseFields` for **every** author (sales enters prices, so the flag triggers on their low quotes too). **Backfilled** the 3 existing deals: IHC Steel ($20/SF) flagged; LA deal ($30, at floor) not; Architect Referral (no pricing) not.
- **Surfaces:** form Price/SF placeholder → `e.g. 45` + a live amber warning under Building Size when the effective rate is below floor; amber "Below floor · needs approval" chip on the list cards (`SteelDealsClient`); amber banner on the deal detail page above the money band (shows the effective $/SF). Flag/badge shown to everyone (it's a pricing-policy signal, not confidential financials).
- Verified: tsc + eslint + full build clean; migration applied + column live via PostgREST (select `*` works, /steel 307 unauthed); deployed (health 200). **Note:** pre-existing below-floor deals only flag on next save unless backfilled (done here); an eyeball on next login is worth it — create a deal under $30/SF and confirm the warning + badge + banner.

**Done 2026-07-25 (steel — Invoiced stage + commission payouts moved to their own worklist; DEPLOYED, no migration):**
- **Richard's follow-up to the commissions build:** (1) deals vary — "simply delivered steel" vs "framed steel" — so add a stage for **invoice status**; (2) marking commissions paid should be a **separate place**, not buried on each deal. His pick (AskUserQuestion): **keep Frame Assembly as a stage AND add Invoiced** (over dropping Assembly for a scope tag). Deals table was **empty** (0 rows) so the stage reshape needed no data migration.
- **New `invoiced` stage** (app-level text, no DB constraint → no migration): Quote → Engineering → Order Placed → Delivered → Frame Assembly → **Invoiced** → Paid (+ Lost). Added to STEEL_STAGES/PIPELINE/LABELS/INDEX(shifted paid→6)/BADGE(amber)/BORDER in `src/lib/utils/steel.ts`; list-page **backlog** bucket now includes `invoiced`.
- **Payouts moved to `/steel/commissions`:** the deal-detail `SteelCommissionsPanel` is now **read-only** (dropped 'use client'/router/fetch; shows Paid/Owed **status pills** + a "Payouts →" link). New **`SteelPayoutList`** (client) on the commissions page = the payout workbench: every **unpaid** salesperson commission + referral fee as line items **grouped by person** (deal name, service detail, amount), each with **Mark paid**, a per-person **Pay all**, total owed in the header, and a collapsible **Paid** list with **Undo**. Uses the existing `PATCH /api/steel/services/[id]` and deal-PATCH `referral_fee_paid` (both financials-guarded). Commissions page builds the `PayoutItem[]` server-side in the same rollup loop.
- Verified: tsc + eslint + full build clean; deployed (health 200). Same eyeball-when-convenient as the parent build (create a deal, mark a payout paid on /steel/commissions, undo).

**Done 2026-07-25 (steel commissions & margin engine — service line items, per-service commission, referral fees, paid tracking, report; DEPLOYED + MIGRATED):**
- **Richard's ask: track commissions + referral fees on steel deals and report them.** His business: a deal is composed of commissionable **services** — **Steel/Materials, Engineering** (markup over the engineer's fee), **Frame Assembly** — each with **price − cost = margin**; the **salesperson earns a % of each service's margin** (always), and the **lead-source referrer earns a flat fee OR % of total margin**. Steel scope ends at **frame assembly** (full home builds live in the construction module). His picks (via AskUserQuestion): **fixed service sections** (not open line items), **per-service commission %**, **track paid vs unpaid**.
- **Model:** new **`steel_deal_services`** table (migration `20260727000001_steel_commissions.sql`, **APPLIED to the Studio DB** + PostgREST restarted; `database.ts` hand-extended, `SteelDealService` alias): deal_id (cascade), service_type (materials|engineering|assembly), price, cost, commission_pct (salesperson % of THAT service's margin), commission_paid + commission_paid_date, `unique(deal_id, service_type)`. **`steel_deals`** gained referral fields: referral_fee_type (none|flat|percent-of-margin), referral_fee_value, referral_fee_paid + date. **Deal `value` is now COMPUTED = Σ service prices** (the old standalone Contract Value input is gone; sqft×$/SF now just *suggests* the materials price). New stage **`assembled` (Frame Assembly)** between delivered and paid (list backlog now includes it). All math in `src/lib/utils/steel.ts` (`serviceMargin`/`serviceCommission`/`referralFeeAmount`/`dealFinancials`, `DEFAULT_SERVICE_COMMISSION_PCT=10`).
- **CONFIDENTIALITY (my call, Richard didn't object): cost/margin/commission/referral + the report are admin/executive-only.** New `canSeeSteelFinancials(viewer) = admin||executive` in viewer.ts. steel_sales users enter deals + **prices** (revenue) but never see cost/commission. `SteelDealForm` gained `canSeeFinancials` + `services` props: three fixed service sections, cost/commission/referral inputs rendered only for financials users; live margin/commission/referral readback. **Server-side preservation:** `saveServices()` in `actions.ts` — a non-financials save updates only prices and **preserves existing cost/commission** (a sales user can't wipe them); commission_paid/date always preserved across a normal save; upsert onConflict(deal_id,service_type), empty services deleted.
- **Detail page:** `SteelCommissionsPanel` (client, financials-only) — services table (price/cost/margin/comm%/commission + **Mark paid** toggle per service), referral row, totals + net-after-commissions. **Mark-paid APIs:** `PATCH /api/steel/services/[id]` {paid} (new route, `canSeeSteelFinancials` guard) + `referral_fee_paid` branch added to the deal PATCH (financials-gated — `/api/steel` is allowlisted for steel_sales so both carry in-route guards).
- **Report: `/steel/commissions`** (linked from the /steel toolbar, financials-only; page redirects steel_sales→/steel since the `/steel` prefix would otherwise admit them): headline band (revenue/cost/margin/net), **owed vs paid** for salesperson commissions + referral fees, rollups **by salesperson**, **by referrer**, **by service**. Lost deals excluded.
- **Verified:** commission math checked by hand against the $350k example ($95k margin → $11,250 salesperson + $4,750 referral → $79k net); upsert-onConflict collapses to one row per service + cascade-delete leaves 0 orphans (live rolled-back psql test on the Studio); tsc + eslint (fixed a `static-components` error by hoisting PaidToggle) + full build clean; deployed (health 200). **Authed end-to-end UI click-through not yet done** — worth an eyeball: create a deal with all three services as admin, mark a commission paid, check the report; and confirm a steel_sales session sees prices but no cost/commission and no Commissions link.

**Done 2026-07-25 (add users without invite + delete deactivated users; steel deals get a payable referrer; DEPLOYED + MIGRATED):**
- **Richard's ask 1: manually add users WITHOUT sending an invite, to track their activity before they gain access.** Reframed the whole user flow: this stack has NO working email (GoTrue placeholder SMTP), so the old "invite email" was fiction. `/settings/users` **"Invite User" → "Add User"** with a **"Give sign-in access now" checkbox, off by default** → creates a trackable `team_members` row (name, role, optional email) with **no login** (`auth_user_id` null). Checkbox on → `admin.auth.admin.createUser({email,password,email_confirm:true})` sets a password directly (no email), shows copyable credentials. A login-less member's card gains a **"Grant access"** button (PATCH now creates + links the auth account when `auth_user_id` is null; still resets password when it exists). `POST /api/admin/users` gained `create_login`/`password`; `PATCH /api/admin/users/[id]` gained the grant-access branch + optional `email`.
- **Ask 2: permanently delete deactivated users** (declutter the admin screen). New **`DELETE /api/admin/users/[id]`** — guarded: member must be **inactive first** (deactivate → then Delete, red button + ConfirmDialog), can't delete self. Removes the auth login (`admin.auth.admin.deleteUser`, non-fatal) then the row. Safe because every FK to `team_members` is `ON DELETE SET NULL` (tasks.assignee_id/waiting_on_id, steel_deals.salesperson_id + new lead_source_id, objectives.owner_id, investors.relationship_owner_id) or CASCADE (access_grants) — no orphans, no blocks.
- **Ask 3 (steel): "let me select any user as a lead source" — track/pay the people who refer deals.** His pick (over a combined picker): **category + referred-by**. Lead source stays the free-text self-maintaining category; new **"Referred By"** dropdown on the deal form links the specific person to a real user via **`steel_deals.lead_source_id` → team_members** (migration `20260726000002_steel_lead_source_referrer.sql`, **APPLIED to the Studio DB** + PostgREST restarted; `database.ts` hand-extended; index + FK `on delete set null`). Nullable (channels like Trade Show have no referrer). Salesperson dropdown was already every active `team_member`, so an Add-User (even login-less) shows up as a salesperson automatically. Detail page shows a "Referred By" fact.
- **Commission tracking is the DELIBERATE NEXT PIECE (Richard flagged it, said "later"; NOT built).** Plan told to him: per deal, two payees — salesperson (**always % of profit**) and lead-source referrer (**flat fee OR % of profit** for marketing). Two gaps beyond the now-shipped payable-identity link: (1) **commission terms per payee** (rate/type/amount), and (2) **a profit/cost figure on the deal** — `steel_deals.value` is contract REVENUE, not profit, so "% of profit" has no basis yet (add a cost or profit column). Build these together as one focused pass when he's ready; the `salesperson_id` + `lead_source_id` links are the forward-compatible foundation.
- Verified: tsc + eslint (touched files clean) + full build clean; both migrations already applied; deployed to the Studio (health 200). **Reminder given to Richard: hard-refresh (⌘⇧R) after deploys — Chrome caches old chunks** (he initially saw the stale "Invite User" button). Authed end-to-end click-through not yet done — worth an eyeball on next login.

**Done 2026-07-25 (steel lead sources self-maintaining; DEPLOYED + MIGRATED):**
- **Richard's ask: add lead sources (and reps/fields) without hard-coding — "we will have many lead sources."** Lead sources are no longer a fixed vocab: the deal form's Source field is a **type-ahead text input (`<datalist>`)** suggesting `DEFAULT_LEAD_SOURCES` + every source in use (`leadSourcesInUse()` in new `src/lib/steel/lead-sources.ts`); typing a new one creates it. **Display text is stored directly** in `steel_deals.lead_source` (plain text, no constraint); saves canonicalize case-insensitively against the vocabulary (`canonicalLeadSource` — "trade show" reuses "Trade Show", legacy slugs map to labels) so casing variants can't split filter entries. List-page filter builds from distinct values in the data; chip colors **hash from the label** (`leadSourceBadge`, fixed 8-tone palette, Other stays neutral). Old exports (`LeadSource`/`LEAD_SOURCES`/`LEAD_SOURCE_LABELS`/`LEAD_SOURCE_BADGE`/`leadSource`) replaced by `leadSourceLabel`/`leadSourceBadge`/`canonicalLeadSource` across all 6 consumers. Migration `20260726000001_steel_lead_source_labels.sql` (**APPLIED to the Studio DB**, PostgREST restarted; UPDATE 0 — table was empty) converts the original slugs to labels + column default → 'Other'.
- Sales reps were already dynamic (`team_members` — add via /settings/users or Manage Team); genuinely new fields stay code changes by design (no custom-field builder — golden rule).
- Verified: 14 pure-function unit checks; tsc + eslint + full build clean; **authed end-to-end on prod** (minted session: test deal with never-seen source "Golf Tournament" → suggested in the /steel/new datalist, appears in the list filter + card chip, `?source=` narrows correctly; test deal deleted); /steel still 307 unauthed.

**Done 2026-07-25 (Prefab Steel CRM — /steel module + steel_sales role; DEPLOYED + MIGRATED):**
- **Richard's ask: a CRM for the prefab steel plant** (deal name, lead source, salesperson, sqft, $/SF, stages quote→engineering→order placed→delivered→paid), shared with 4-10 non-executive sales users. Evaluated Google Sheet vs in-BI; his picks: **build inside BI** (exec revenue visibility native, no sync plumbing, data stays on the Studio), sales users get **Tailscale invites + a locked-down role**, and **everyone on the sales team sees all deals** (one shared pipeline). Clones the investors-module pattern throughout.
- **Schema** (migration `20260725000001_steel_crm.sql`, **APPLIED to the Studio DB** + supabase-rest restarted; `database.ts` hand-extended, `SteelDeal`/`SteelDealNote` aliases): `steel_deals` (name/customer/building_type, lead_source vocab + free-text detail, salesperson_id FK→team_members, stage, square_feet/price_per_sqft/value, expected_delivery_date, next_step/date, description) + `steel_deal_notes` (author stamped server-side from getViewer — never client). Plain-text vocab in `src/lib/utils/steel.ts`; no log_activity() (investors precedent). Same migration **widens `team_members_role_check`** for the new role.
- **NEW 5th ROLE `steel_sales`** — sees ONLY `/steel` + `/api/steel` (permissions.ts union + every `Record<Role,…>` map; executives also gained /steel). **The landing-redirect trap:** DEFAULT_LANDING=/tasks would infinite-loop a role that can't see /tasks — new `landingFor(role)` in permissions.ts ('/steel' for steel_sales) now backs middleware's disallowed-page redirect. LoginForm's push('/dashboard') stays (middleware chain-bounces steel users to /steel). `canWorkSteel(viewer)` in viewer.ts = admin|executive|steel_sales (deal DELETE stays admin-only). No access_grants involvement — role-prefix gating is the whole isolation. `/settings/users` picks up the role automatically (its dropdown iterates ROLES).
- **Surfaces:** `/steel` list (stats band re-scoped by filters: Quoting = quote+engineering $, Backlog = order_placed+delivered $, Collected = paid $, deal count + open SF; stage/source/salesperson filters; stage-bordered cards), `/steel/new` + `/steel/[id]/edit` (`SteelDealForm` — **contract value auto-computes sqft×$/SF until the user types their own number**, live money readback catches magnitude typos, DatePicker everywhere), `/steel/[id]` detail (5-stage pipeline bar, lost banner, money band, key facts, next-step callout w/ overdue amber, deal log). API: PATCH (whitelist + stage validation) / DELETE / POST notes under `/api/steel/deals/[id]`. Nav: one nav.ts item (Factory icon); ⌘K search gained a `steel` result type (name OR customer ilike) + palette scope chip.
- **Verified end-to-end with a real minted steel_sales session against a local prod build** (admin createUser → @supabase/ssr cookie jar → curl; `next dev` skips middleware so role tests need `next start`): /steel + detail 200 with ONLY "Steel CRM" in the rendered nav; /tasks, /dashboard, /investors all 307→/steel (no loop); /api/search + /api/tasks 403; stage PATCH 200; invalid stage 400; **note author stamped from the session, spoofed body author ignored**; DELETE 403 for steel_sales; unauth 307/401. Test deal/member/auth user deleted, notes cascade verified (0 orphans). tsc + eslint (20 documented survivors) + full build clean; deployed.
- **Onboarding runbook for the sales team (Richard's manual steps):** (1) Tailscale admin → invite each sales user; apply the drafted ACL (`deploy/security/tailscale-acl.hujson`) so non-admin devices reach only 443/8443. (2) `/settings/users` → Invite with role **Steel Sales** (no email sends — expected) → **Reset password** → share it directly. (3) Move the prefab plant project to won = set its stage to `award` in the project UI (data action, no code).
- **Deliberate fast-follows (NOT built):** Ber AI wiring (embedSteelDealSnapshot + list/query agent tools), daily-brief steel section, dashboard/attention wiring, customer→parties linking, per-salesperson scoping. The /steel stats band is the exec revenue view for v1.

**Done 2026-07-24 (Ber AI reads whole documents + iPhone chat fix; DEPLOYED, data repaired):**
- **Richard's report: asking Ber AI to "review all the documents on this project" returned only snippets.** Two real gaps, both fixed: (1) **there was no tool to list a project's documents** — "review everything uploaded" had no supported path; new **`list_documents`** agent tool (project scope from args or conversation context, `scope='company'` for the KB, `include_children` for programs; returns id/file_name/doc_type/ai_summary/`has_full_text` without ever pulling the text, + a hint to chain into get_document_content). (2) **`search_knowledge_base` hid `document_id`** — match_chunks always returned it but the tool layer dropped it, so the agent could never follow its own instruction to fetch full text after a snippet hit; results now carry `document_id` + a "these are snippets, fetch the full document" note. Agent prompt gains a **Reviewing documents** rule (list → get_document_content, several per turn → answer from full text; snippets are pointers). Verified live against the Studio DB: list on a real 11-doc project, company scope, no-scope error, get_document_content chain, and all 8 KB results carrying document_id.
- **Data repair while testing: 5 project docx files had NO stored text** (4 `skipped` pre-docx-support uploads incl. the CRISI decision brief + StracNet submission, 1 `complete`-but-empty) — all re-run through `runDocumentAiPass` standalone (chunks wiped per doc first); **0 documents remain without extracted text platform-wide.**
- **iPhone: the chat send button bled off the right edge.** Cause: iOS Safari auto-zooms when focusing any input under 16px (the chat textarea was 14.5px), pushing fixed-width controls off the visual viewport. Fixed: viewport **`maximumScale: 1`** in layout.tsx (kills focus auto-zoom app-wide; iOS still allows pinch), chat textarea `text-base sm:text-sm` + `min-w-0`, messages area `overflow-x-hidden`, bubbles `break-words` (long URLs/tables can't force horizontal overflow).
- **Also committed + shipped: the prior session's in-flight work** found uncommitted in the tree (verified by tsc/lint/build): conversation delete (owner-checked GET/DELETE on `/api/ai/agent`, ConversationList row delete, AgentChat clear control on the intake document reader), task-board **Manage Team dialog** (add/remove members, open-task reassignment; team-members POST dedupes by active name; new `/api/team-members/[id]`), corrected header Drive link.
- Verified: tsc + eslint + full build clean; deployed; live smoke (agent API 401 unauthed incl. DELETE, /intel + /tasks 307). No migration.
- **Follow-up same day (Richard: "Load failed" on iPhone; DEPLOYED + verified live on prod):** the SSE agent stream went **silent for 60s+** while the local model reasons (reasoning tokens go to `reasoning_content`, never forwarded) and during tool runs — Safari (esp. iOS) kills idle connections → "Load failed". Fixed both ends: (1) **10s heartbeat** (`{type:'ping'}`) from the agent route's SSE branch while runAgent works (client ignores unknown event types); (2) **client recovery in AgentChat** — the server keeps running and persists the answer after a drop (it always did), so on a network-layer stream failure the chat now polls the conversation (5s interval, 4 min cap; fresh chats re-found by scope + title) and renders the persisted answer instead of erroring; server-reported failures carry a `noRecover` marker and surface immediately. **Scoping architecture confirmed correct as-is**: dock scopes to the project from the URL, no scope = portfolio-wide. **Verified against PRODUCTION with a real minted session** (GoTrue admin generateLink → verifyOtp → `@supabase/ssr`-serialized cookies — a reusable pattern for authed prod API tests): project-scoped ask autonomously called the new `list_documents` tool and streamed the doc list (max silent gap 6.7s, pings at 10s/20s); portfolio ask used `list_projects` (gap 6.1s); both test conversations deleted through the live DELETE endpoint (200).

**Done 2026-07-21 (Meeting intake — reliable task assignees + unify owners with contacts; DEPLOYED + MIGRATED):**
- **Two-part follow-up to the 07-20 meeting module, both from Richard's review.**
- **Part 1 (assignee routing):** the meeting prompt now gets the **active team roster** injected (`buildMeetingSystemPrompt(memberNames)` in `meeting-intake.ts`; `analyze-meeting.ts` fetches active `team_members` names) so the AI normalizes each task's owner to a real member's name. The review screen's assignee **free-text box → team-member dropdown**, pre-selected via a fuzzy matcher (`bestMemberId`: exact → substring → shared name token, e.g. "Eric"→"Eric Tuaone"); unmatched AI guesses show an amber "not a team owner" hint instead of silently landing on nobody. Confirm resolves by the confirmed id, name-match only as fallback.
- **Part 2 (unify people — Richard's pick over "keep separate"):** closes the "is a task owner also a contact?" gap. **`team_members.party_id`** (migration `20260721000001_team_member_party_link.sql`, **APPLIED to the Studio DB** + supabase-rest restarted; `database.ts` hand-extended; backfill linked existing owners by email then unambiguous name — Eric linked, Richard/Dennis/Nancy left unlinked b/c the owner name isn't an exact contact-name match, link on next use). **Quick-add team member now find-or-creates a linked contact** (`/api/team-members` `resolveOwnerParty`: explicit id → email → unambiguous name → create) so an owner is never an orphan.
- **Meeting attendee step is now fully editable + dedupe-safe:** inline name/role/company/email, a type-ahead **`ContactPicker`** over the whole directory to link ANY existing contact (not just the AI's single guess), add/remove attendee, Org toggle. New **"Can own tasks"** checkbox promotes an attendee to a task owner — confirm find-or-creates a `team_member` linked to their contact (`ensureOwner`, dedupe by `party_id`) and they're assignable to THIS meeting's tasks via `owner:<attendeeRef>` refs (existing owners resolve by id; AI name still the last fallback). The meeting page now also fetches `parties` (id/full_name/company/email/is_organization) + `team_members.party_id`.
- **Rule going forward:** task owners and contacts are one person — `team_members.party_id` ties them; adding/using an owner maintains the contact. Full merge of the two tables still deferred (tasks keep `assignee_id → team_members.id`), but they're no longer disconnected. Verified: `tsc` + eslint (20 documented survivors, none in touched files) + full build clean; migration applied + column live via PostgREST; deployed; live boundary smoke (meeting review 307, both APIs 401 unauthed).
- **Part 4 (same day — five polish improvements, Richard "proceed with all"; DEPLOYED, no migration):** (1) **Owner↔contact reconcile:** the 3 legacy unlinked owners linked directly in the Studio DB (Richard→Richard White, Nancy→Nancy Thunnel, Dennis→new contact); all 4 owners now carry `party_id`. (2) **Calendar prefill:** `MeetingIntakeForm` "From calendar" picker → `GET /api/meeting-intake/calendar-events` (admin-only; `fetchCalendarEvents` last-21d→+2d, America/Denver labels, degrades to `{events:[],error}` if Graph token stale) seeds title/date + attendees; `analyze` accepts `seed_attendees` → `analyzeMeetingNotes` merges them into the extraction (dedup by email/name). (3) **Re-draft:** `extractMeeting()` refactored out of `analyzeMeetingNotes` (runs the prompt WITHOUT staging a session); new `POST /api/meeting-intake/redraft` re-runs over edited minutes; review's "Re-suggest from minutes" replaces tasks + appends new attendees. (4) **Meetings log:** `/intake` Meeting Recent list now shows date · N records · M tasks for confirmed sessions (reads `created_record_ids`). (5) **Direct-record push:** decision chips → "Add to steering board" (`POST /api/objectives` bucket `soon`); per-task **"Blocked? / Waiting on {owner} for {what}"** writes the task **handoff triple** (`waiting_on_id/what/since`) on confirm, self-block guarded, owner refs (`owner:<ref>`) resolved like assignees. Verified: tsc + eslint (1 purity misfire fixed) + full build clean (107 routes); deployed; smoke (calendar-events/redraft/objectives 401 unauthed, /intake + review 307). Calendar picker depends on a live Graph token — if it errors, reconnect the mailbox on `/settings/health`; paste still works.
- **Part 3 (same day, from Richard's first real processing attempt; DEPLOYED, no migration):** (a) **Targets are now optional** — a pure executive-team meeting processes with no project/opportunity (the blocking "add at least one target" validation is gone); tasks default to the general list (`project_id`/`opportunity_id` null) and confirm redirects to `/tasks`. (b) **"Ber Wilson (company)" is a taggable target** — Richard flagged it was missing among projects/opportunities. New `'company'` `TargetKind` in `confirm-helpers.ts`; the minutes doc saves to the **company Knowledge Base** (`documents.is_company=true`, folder `company/…`) and is **embedded via `embedDocument(...isCompany=true)`** so Ber AI can retrieve it (project/opp targets embed via their update/note feed); company-tagged tasks resolve to the executive list (no `company_id` on tasks). The fan-out loop now branches project / opportunity / company (company = doc-only, no players/notes). Verified: tsc + eslint + full build clean; deployed; smoke (confirm 401, company 307). Full authenticated end-to-end (real extraction → owner promotion → assignment → company doc) still worth an eyeball on the next live meeting.

**Done 2026-07-20 (Meeting Notes Intake — paste a meeting, fan it out to many records; DEPLOYED + MIGRATED):**
- **Richard's ask: paste raw notes/transcript from a long meeting and have Ber AI summarize + create tasks + UPDATE the projects and opportunities it touched** (attendees, takeaways, follow-ups). His picks: **multi-target fan-out** (route onto multiple EXISTING records + optionally create new), **no browsable meetings table** (content lives on the records it touched + a saved doc; the intake session is the audit trail), **third tab on `/intake`** (Email · Meeting · Proposal).
- **Why the existing paste box was the wrong door:** the email-intake prompt is email-thread-framed (no attendee/decision concept) and its confirm creates exactly ONE new record — the opposite of a meeting recap. So: a **parallel meeting pipeline** reusing ~80% of the email machinery (party matcher, project matcher, task/assignee resolution, embeddings, doc save, the review-screen patterns, the staging/dismiss/Recent-list machinery).
- **Schema:** reuse `email_intake_sessions`, discriminated by a new **`intake_kind` column** (migration `20260720000001_meeting_intake.sql`, **APPLIED to the Studio DB** + supabase-rest restarted; text default `'email'` + check `in ('email','meeting')` + `(intake_kind,status,updated_at)` index; `database.ts` hand-extended). Both Recent lists filter by kind; 5 legacy rows backfilled to `email`.
- **New meeting prompt** (`src/lib/ai/prompts/meeting-intake.ts`, `MeetingIntakeExtraction`): title, date, summary, narrative **minutes**, **attendees** (EmailIntakePerson shape), **decisions[]** (folded into minutes/doc, NOT separate records), **referenced_records** (project/opportunity the meeting touched — advisory pre-match), tasks with a **record_hint**. `analyze-meeting.ts` mirrors `analyzeEmailReport`: reuses `matchExtractedParties` for attendees + `findMatchingProjects` (over name stubs) for project refs + a name-ilike for opportunity refs; stages a `pending` session with `intake_kind='meeting'`. No fit assessment (meetings aren't pursue/pass).
- **Multi-target confirm** (`/api/meeting-intake/confirm`): resolves each chosen target (existing id OR create-new), and per target fans out — project → `updates` row (`manual_paste`/approved) + `embedUpdate` + `project_players` for attendees; opportunity → `opportunity_notes` + `embedOpportunityReport`/`embedOpportunitySnapshot` + attendees note; **every** target gets the meeting-minutes `.md` document (via shared `saveReportDocument`) + `storeExtractedText`. Tasks insert with `project_id`/`opportunity_id` from each task's chosen target + assignee-by-name.
- **Shared refactor:** extracted `createRecordFromFields` + `saveReportDocument` (+ `str`/`num`) into `src/lib/email-ingestion/confirm-helpers.ts`; the email confirm route now uses them (no behavior change).
- **UI:** `MeetingIntakeForm` (title/date/paste/upload → `/intake/meeting/[id]`), `MeetingIntakeReview` (editable meeting header + decisions; attendees create/link/skip; **Records-to-update** section: pre-checked matches + search-add existing + create-new inline + "mentioned, not matched" suggestion chips; per-task **target dropdown**). Meeting tab + Recent list on `/intake` (kind-filtered); nav palette row + keywords. New routes admin-only by default-deny (not in any `permissions.ts` allowlist) — no permissions change.
- **Verified:** `tsc` + eslint (20 documented survivors, none in new files) + full `next build` clean; migration applied + PostgREST sees `intake_kind` (REST returns `[]` not a column error); **DEPLOYED** — live boundary smoke test on the Studio: `/intake/meeting/[id]` 307, both `/api/meeting-intake/*` 401 unauthed, `/intake` 307 (no server error → the kind-filtered query works). Full authenticated end-to-end (real AI extraction → multi-record fan-out → ask Ber AI) not yet exercised — worth an eyeball on the next real meeting. (`.next` numbered Finder-duplicate artifacts broke `tsc` again — deleted; recheck at session start.)

**Done 2026-07-19 (header search covers every area; DEPLOYED):**
- **Richard's report: the header search felt scoped to certain areas with no "all."** Root cause: `/api/search` (the ⌘K palette backend) only covered 4 record types (projects, opportunities, contacts, vendors) — tasks, investors, objectives, and documents silently returned nothing, so searching those areas looked unsupported. All four added (ilike on title/name/file_name, limit 6 each; objectives active-only; documents link to the surface they live on — project Documents tab / `/company` / vendor detail — with the project name as subtitle). Palette gained the 4 type icons/labels; placeholder now says "Search everything — …". **Task results deep-link: `/tasks?task=<id>` opens the detail sheet on mount** (new `initialOpenTaskId` prop on TeamTaskBoard; page reads searchParams). Gotcha honored: the tasks embed uses the hinted `team_members!tasks_assignee_id_fkey` form (dual-FK ambiguity). Verified: all 4 query shapes run live against the Studio DB via alias-loader; `tsc` + eslint + full build clean; deployed (unauthed search 401s). (The `.next/types/* 2.ts` Finder duplicates reappeared — deleted again.)
- **Follow-up same day (Richard: still no visible "All" option):** the palette gained an **area chip row** under the input — **All (default)** + Projects/Opportunities/Contacts/Vendors/Tasks/Investors/Objectives/Documents. A type chip filters the records client-side (one fetch covers all types) and hides the Pages section; All restores everything. Verified live in prod via browser automation after a hard refresh: chips render with All active, "mark" under All returns contacts+task+investor, Tasks chip narrows to the task, and clicking a task result deep-links to `/tasks?task=<id>` with the detail sheet open. **Remind Richard to hard-refresh (⌘⇧R) after deploys — Chrome caches old chunks.**
- **Richard's refinement (same day; DEPLOYED, verified live):** the full Pages list on open read as a second copy of the area chips and confused him — **the palette no longer lists pages by default**; page rows appear only when the typed query matches a page name (e.g. "timeline" → Timeline — still the nav path for non-chip destinations). Empty state is now a hint line ("Type to search everything, or pick an area above"). His verdict: chips across the top are the right presentation.

**Done 2026-07-19 (beauty pass — token-level polish; DEPLOYED, verified live in prod):**
- **Richard's ask: "make it more inviting and clean" — four token-level moves, all shipped:** (1) **`--primary` is a real navy-blue now** (light `oklch(0.40 0.105 262)` ≈ #274580, dark `oklch(0.72 0.10 262)` light-blue) — every button/link/focus state app-wide reads branded instead of charcoal; `--ring` and a new `::selection` rule tinted to match (both themes). Ripple accepted deliberately: OrgChart hero cards and print exports now royal blue rather than near-black navy. (2) **Newsreader display serif on every `h1` + `DialogTitle`** via the pre-existing `--font-heading` token (loaded in layout.tsx next to Geist; self-hosted by next/font at build — zero runtime external requests). App-bar title and dialog titles bumped `text-base` → `text-lg` (serif x-height compensation). Dashboard greeting, page titles, and print letterheads all inherit it. (3) **`--radius` 0.625rem → 0.75rem** app-wide. (4) **Warm cream accent** (`--accent` bumped to `oklch(0.962 0.014 85)` so it's perceptible) now backs interaction states: ui/table row hover/selected, ⌘K palette selected rows, NeedsAttention/RiskOverview/ClosingSoon rows, ConversationList, activity table, calendar rows — gray `hover:bg-muted` kept only on gray `bg-muted/30` sub-blocks and ghost icon buttons where cream would clash.
- **GOTCHA recurrence: the Finder-duplicate artifact had SILENTLY REVERTED the 07-19 calendar timezone fixes in the working tree** — `CalendarView 2.tsx` held the committed fixed version, working `CalendarView.tsx` was the stale pre-fix copy. Restored from HEAD, duplicate deleted, went out with this deploy. **Check `git status` for `* 2.*` siblings at session start; diff against HEAD before assuming the working tree is the newest state.** (The `.next/types/* 2.ts` artifacts also reappeared — deleted again.)
- Verified: `tsc` + eslint (20 documented survivors) + full build clean; new primary values confirmed in built CSS (`#274580`/`#82a5e4` hex fallbacks) + Newsreader @font-face and 14 self-hosted woff2 files; prod `next start` boundary smoke (login 200 / dashboard 307 / API 401); deployed and eyeballed live over the tailnet (serif greeting + titles, blue segmented controls + New Project button, dark-mode login button light-blue).

**Done 2026-07-19 (calendar/Graph fixes + UI warmth pass; DEPLOYED, verified live in Richard's session):**
- **Calendar was dead end-to-end — two stacked bugs, both fixed.** (1) `fetchCalendarEvents` hit `/users/{upn}/calendarView`, which this tenant 403s even for the token owner — **`/me` works** (verified live; the daily brief's meetings block had NEVER worked since it shipped 07-11, and /calendar showed no meetings). Fixed: `/me` for TARGET_EMAIL, `/users/{email}` kept for shared mailboxes. (2) CalendarView rendered meetings **a day early and hours off**: Graph returns naive datetimes + `startTimeZone` (UTC) which were parsed as local, and the date cards/week grouping used `new Date('YYYY-MM-DD')` (= UTC midnight, the formatDate bug class). Both meetings now render on the right day at the right local time (verified in prod: Jul 20 · 10:00 AM Operations Meeting). **The daily-brief meetings catch now logs the error** — it silently swallowed a week of failures. Also diagnosed: the Graph token had been stale since 07-13 (probe refreshed+rotated it; refresh works from both machines — cause of the stall unreproducible, watch cron-daily-brief.err.log now that failures log). **Note: /calendar's meeting fetch is client-side and slow (~20-30s incl. token refresh) with no spinner — meetings pop in late; consider a skeleton or server fetch if it bugs anyone.**
- **UI warmth pass (Richard: "looks sterile although functional" — evaluated every major surface live in the browser, then fixed the top offenders):** (1) **Login rebuilt** — navy radial + dot-grid canvas, elevated card with the real logo, app-idiom fields (was an unbranded gray wireframe). (2) **Light-mode canvas deepened** (`--background` 0.992 → 0.976) — background vs card separation is what makes panels lift; don't converge them. (3) **Directory contacts get hashed-color initial avatars** (new shared `src/lib/utils/avatar.ts` — same palette as task board; use it for any person surface); org/vendor tiles get a primary-tinted mark instead of gray. (4) **Dashboard opens with a time-of-day greeting** ("Good morning, Richard" + date, America/Denver, name from `getViewer().teamMemberName`) and the **KPI band gains soft-tinted icon tiles** (navy/violet/emerald/amber). (5) **Ask Ber AI empty state**: branded navy tile + clickable example-question chips (portfolio- and project-scoped variants) — fills the input on click. (6) Investor stat band accent dots echo the tranche-bar legend (amber/indigo/emerald). (7) Overdue task rows get a red left edge. (8) **/company/structure dropped its `max-w-3xl`** — the chart now uses the full canvas (edit board keeps its own width).
- **Non-admins can now find the org chart:** `NavItem` gains a `fallback` destination (`resolveNavItem` in nav.ts) — when a role can't access `/company` but can access `/company/structure`, sidebar + mobile nav render an "Org Structure" item instead (verified in the dev member view).
- **GOTCHA hit live: the Finder-duplicate artifact renamed `CalendarView.tsx` → `CalendarView 2.tsx` MID-EDIT** (a src file this time, not just .next artifacts) — recovered by `mv` back; edits survived. If a file "disappears" mid-session, check for a `* 2.*` sibling before assuming loss.
- `tsc` + eslint (survivors unchanged) + full build clean; deployed twice (UI pass, then calendar fix); everything verified against production in Richard's live session via browser automation.

**Done 2026-07-18 (structure chart visual redesign — presentation-grade; DEPLOYED):**
- **Richard's verdict: Present mode + the exported PDF were "dry and boring."** Redesign, same architecture (OrgChart stays a pure component; print inherits everything automatically):
  - **Arms = navy hero cards** (`bg-primary text-primary-foreground`, elev-2, icon in a soft tile, on-navy Chip) — the parent entities now visually crown the tree. Management LLC keeps the dashed "liability wall" card, now with a ShieldCheck icon on `bg-muted/30`.
  - **Per-vertical division accents** (`divisionAccent()` in OrgChart.tsx — keyword-matches vertical+name; **Richard's picks (same day): energy→green (emerald), military/federal→red, health→blue, real estate/develop→yellow**; tech→sky, steel/prefab→violet; indexed cycle fallback leading with unclaimed hues). Each division card gets a colored top strip + accent-colored vertical label; the accent flows down its SPV rail (tinted connectors) and each SPV card's left edge strip. All literal class strings w/ dark variants; verified emitted in built CSS.
  - **People render as initial avatars** — leadership band rebuilt as a header-banded panel with size-7 hashed-color avatars (same palette as the task board; hashed from name since org people are free text), stacked name/role/detail; staff lines in cards get size-5 avatars; open roles get a dashed amber UserPlus circle.
  - **Legend** (bottom of chart, screen + print): navy swatch = parent entity, Series = internal capital, Standalone = outside capital, Open (only at people depth).
  - **Screen canvas + Present mode as a slide:** the chart sits on a faint dot-grid canvas (`color-mix` on `var(--border)` — theme-tracking, local inline style, not a global utility); Present mode adds a centered "Ber Wilson / Entity Architecture" title block over the full-bleed dot grid and nudges zoom to 110% on entry.
  - **Print/PDF:** `print-color-adjust: exact` added (browsers drop backgrounds by default — without it the navy cards + accent strips would export gray); letterhead gains a label-caps eyebrow. Rendered per-depth in dev against the Studio DB: navy/amber/blue/rose/emerald all present, avatars + legend at full, no SPV strips at high, legend depth-aware.
- `tsc` + eslint + full build clean; deployed. Visual check was markup + built-CSS level — eyeball Present mode and a real print preview when convenient.

**Done 2026-07-18 (org structure chart — entity architecture on /company/structure; DEPLOYED + SEEDED):**
- **Richard's ask: an interactive, fully editable chart of the holding-company architecture** (liability-mitigation structure, per-project capital raises), rebuilt natively from his Claude-artifact prototype (`ber-wilson-entity-architecture.jsx`). His picks: lives as a tab in the company area; people are FREE TEXT (deliberately not linked to team_members/parties — an org allocation ≠ a task assignee/contact); seed all prototype content; **viewable by every signed-in role, editable admin-only**.
- **Two new tables** (migration `20260718000001_org_structure.sql`, **APPLIED to the Studio DB**, supabase-rest restarted; `database.ts` hand-extended; `OrgNode`/`OrgPerson` aliases): `org_nodes` (self-referential — kind arm|management|division|spv, parent_id cascade, entity_type: free text on arms / series|standalone on divisions+SPVs, vertical/location/note, sort_order) + `org_people` (node_id null = leadership roster w/ tier leadership|director; node_id set = division/SPV staff; status active|open — open positions have no name). Vocab in `src/lib/utils/org.ts` (badge maps in-module, investors idiom). No log_activity() (project_id-dereference precedent). Deliberately NOT the `entities` table — that's the external vendor directory.
- **Seeded in the migration itself** (guarded on empty table, hardcoded division UUIDs): 2 arms (Ber Wilson, Inc. C-Corp; Building Futures 501(c)(3)) + Management Services LLC note (liability-wall explanation) + 4 divisions (Energy / Military Infrastructure / Health & Wellness / Real Estate Development holdings LLCs) + 8 SPVs + 10 roster people (4 leadership w/ detail lines, 3 named directors, 3 OPEN director roles: Legal & Compliance, Controller/Accounting, People Ops).
- **`/company` now has Profile | Structure tabs** (`CompanySectionTabs`, IntelSectionTabs clone; sub-route not `?tab=` because canAccessPage is a pathname prefix matcher). `/company/structure` server page fetches flat lists w/ admin client, passes `canEdit=viewer.isAdmin`; non-admins get a pure read-only render (no drag/forms/menus) and no Profile tab. **The only permissions edit: `/company/structure` added to all three ROLE_PAGE_PREFIXES.** Known limit: non-admins have no sidebar/palette discovery path (nav item gated on /company; palette admin-only) — direct URL only; wire an affordance when non-admin teammates join.
- **`OrgStructureBoard`** (first real consumer of the `Panel`/`PanelHeader` primitives): summary strip (entities/divisions/SPVs/people), editable arm Panels + management `bg-muted/30` note, Leadership Panel with **cross/within-tier drag-and-drop** (ObjectivesBoard mechanics cloned: dragId/dropTarget/preDragRef snapshot, midpoint before/after, optimistic 0..n resequence, revert on failure; move up/down menus for mobile), collapsible division Panels (vertical label-caps, series/standalone Chip, division staff, **SPV drag-reorder scoped to its own division**, division move up/down via menu, add-division/SPV/person inline forms, cascade-warning ConfirmDialogs).
- **APIs: 5 mutation routes under `/api/org/`** (nodes POST, nodes/[id] PATCH/DELETE, people POST, people/[id] PATCH/DELETE, reorder POST `{nodes?, people?}` — people items carry tier for cross-tier drags). **NO read API and zero ROLE_API_PREFIXES changes** — the page reads server-side, and `canAccessApi` is prefix-based NOT method-aware, so allowlisting any /api/org read path would open the mutations. All routes also carry the in-route getViewer() admin guard (investor-requirements pattern).
- **Verified:** tsc + eslint (20 = documented survivors) + full build clean; seed counts + SPV parentage checked in the Studio DB; dev round-trip against the Studio stack (person create/PATCH incl. tier change/reorder/invalid-item 400/delete; SPV create + staff + **cascade delete leaves 0 orphans**; node reorder); both tab strips render; deployed (health 200, `/company/structure` 307 unauthed, `POST /api/org/nodes` 401 unauthed).
- **SAME DAY — presentation chart + PDF export (Richard: "visualize like a Lucid chart, presentable, exportable, high level then drill down"; DEPLOYED):** `/company/structure` now opens on a **Chart | Edit** switcher (`StructureView`, choice persisted via `useStoredState('bw.org.view')`; non-admins see Chart | Details with the board read-only). **`OrgChart.tsx` is a PURE component (no 'use client')** rendering the Lucid-style tree — arms + dashed management card on top, division cards in a connected row (CSS half-border elbow connectors, no SVG), SPV rails with elbow stubs, people lines + leadership band — consumed BOTH by the interactive client wrapper and the server print route. `OrgStructureChart` (client) adds: **depth presets** (High level = arms+divisions / Entities = +SPVs / Everything = +people), per-division click drill-down, zoom (60–125%, CSS `zoom`), and a **fullscreen Present mode** (fixed overlay, Esc exits). Shared grouping logic extracted to `groupOrg()` in `src/lib/utils/org.ts` (board still has its own memoized copy). **Export = `/company/structure/print`** (objectives-print pattern: chromeless via the layout's `endsWith('/print')` exclusion, screen-only toolbar with the same 3-depth picker, browser print dialog = Save as PDF; landscape `@page` + print zoom 0.8; `ForceLightTheme` strips the dark class so a dark-mode session can't print a dark document; tab title = suggested PDF filename). Verified per-depth server renders (high has no SPVs/people, entities +SPVs, full +people — grepped clean, meaningful because print is a pure server render), tsc + build clean, deployed (both routes 307 unauthed). Browser extension was disconnected — visual check was markup-level, not screenshot; eyeball the chart + a real print preview when convenient.

**Done 2026-07-17 (UI polish + simplification pass — "simplification is sophistication"; DEPLOYED):**
- **Context: Richard's verdict was the platform didn't look polished; approved direction = visuals + structural simplification, refined evolution of the navy identity (not a rebrand).** Full audit first (Explore agent + hand verification), then 4 phases.
- **THE font bug: Geist Sans was never applied.** `globals.css` had `--font-sans: var(--font-sans)` (self-referential) while next/font loads `--font-geist-sans` — the whole app rendered in the OS fallback since forever. Fixed (`--font-sans: var(--font-geist-sans), ui-sans-serif, system-ui, sans-serif`); verified `font-family:var(--font-geist-sans),…` in built CSS. **Dark mode neutrals now carry the same hue-260 cool tint as light** (was pure gray — the two themes finally share a temperature).
- **One design language, enforced by primitives:** `ui/card.tsx` rewritten as **`Panel`/`PanelHeader`** (the REAL app idiom `rounded-xl border-border bg-card elev-1`; old shadcn Card had zero importers); new **`ui/chip.tsx`** (`<Chip tone={*_BADGE[...]}>`) — the one badge shell, consuming the constants.ts color maps (which gained the 4 missing dark-variant sets: pwinBadge, BID_DECISION, ENTITY_CATEGORY, FEDERAL_STANDARD); dead `ui/badge.tsx` deleted; new **`@utility label-caps`** in globals.css = the canonical 11px small-caps label (replaced ~120 drifting spellings app-wide — text-[10px]/[11px]/text-xs × wide/wider/widest all normalized); **raw `shadow-sm/md/lg/xl` swept to `.elev-*`** (~26 files; map marker internals + variant-prefixed forms excluded); `tabular-nums` → `.tnum` everywhere; raw `text-blue-600` links → `text-primary`. Decorative CSS deleted: `.bg-depth` radial gradients, `.divider-fade`, `.stagger-children`, per-band inline animationDelays (one page-level fade remains). **Rules for new UI: Panel/Chip/label-caps, `.elev-*` never `shadow-*`, nested sub-blocks are `rounded-md bg-muted/30` (no border/shadow).**
- **Nav has ONE source of truth: `src/lib/nav.ts`** (items + groups + titles + palette keywords + `alsoMatches` highlighting) consumed by AppSidebar, AppHeader (titles), MobileNav (primary = `mobilePrimary` flag, More = the rest), CommandPalette. Role gating stays per-consumer (`canAccessPage` at render time). The old 4 divergent copies are gone.
- **Sidebar 17 → 11 + gear (Richard's picks):** (1) **`/intake`** = one Intake destination with Email | Proposal tabs (`?tab=` idiom); `/email-ingestion` + `/proposals/intake` are redirect() stubs (the `/email-ingestion/[id]` review routes stay, breadcrumbed to /intake); admin-only by default-deny like both old routes. (2) **Calendar folded under Intel** — shared `IntelSectionTabs` (Ask Ber AI | Calendar) on both pages, `/calendar` route kept for deep links, nav entry removed. (3) **System group (Review Queue/Activity/Users/Health) tucked behind a gear** at the sidebar bottom — expands in place, auto-expands when on a system page, amber dot when review queue is non-empty; still in mobile More + ⌘K.
- **Dashboard morning read decluttered (5 full-width bands → 3):** **AlertsBanner deleted** — it rebuilt the same dd/milestone/cert data the NeedsAttention rail already shows; unique content (mailbox-broken alert, expiring certs) moved into a red **Critical section at the top of NeedsAttention** (props `mailboxAlert`/`expiringCerts`). **VerticalRollup moved to the right rail** as a compact list (Chip + count + value). **DailyBrief is collapsed by default and NEVER auto-generates** (local model = 30–60s; cached brief renders instantly, fresh one is an explicit Generate/refresh click; expansion persisted via `useStoredState('bw.brief.expanded')`). Stack now: Now objectives → KPI band → brief line → projects + rail.
- **ProjectCard calmed (7 info rows → ~4):** stage text badge dropped (stage = left border color + StageIndicator bar, told once not thrice); top row = sector Chip (+status if ≠active, +Program) with the bid-due date inline right (red ≤7d, amber ≤21d, muted beyond); pwin is muted text under the value, not a third badge; counts + deadline + blockers merged into one meta line; flat p-4. Projects list: ProgramBanner violet one-off → neutral card idiom. Project detail header → Chips. Tasks board: one `fieldClass`/`filterFieldClass` (h-8 rhythm), workload chips' nested pills → colored text, buttons h-8.
- Deliberately left: print-page letterheads (`tracking-[0.15em]` slate — PDF-tuned document styling), chip-style uppercase severity badges, map marker internals, AVATAR_CLASSES (distinct role).
- **Verified:** tsc + eslint (20 = the documented survivors, unchanged) + full build clean; prod `next start` boundary smoke (login 200, pages 307, API 401); dev renders against the Studio DB (dashboard bands, intake tabs both ways, intel↔calendar cross-tabs); per-role nav composition checked against real `canAccessPage` (admin 15, executive 2, PM 3, member 1 — identical to before); Geist + label-caps + tinted dark palette confirmed in built CSS; old intake routes 307.

**Done 2026-07-16 (company KB open/download + delete removes indexed data; DEPLOYED):**
- **Richard's report: company-profile Knowledge Base files couldn't be clicked open/downloaded, and he wanted certainty that deleting a file removes its indexed data (no stale proposals tainting RAG).** Both were real bugs.
- **Open/download:** `CompanyKnowledgeBase.tsx` was missed in the 07-08 signed-URL pass — filenames rendered as plain `<p>`, no view/download wiring at all (the server route `GET /api/documents/[id]` already worked). Now: filename click → `viewDocument` (inline for PDF/image/text, download fallback for docx), plus a Download icon button; `mime_type` added to the company page select + `CompanyDoc`.
- **Delete was broken for EVERY indexed document, app-wide:** `chunks.document_id` FK was `ON DELETE SET NULL`, and the cascaded SET NULL violates `chunks_source_check` (needs ≥1 source id) → the DB delete errored → "Delete failed" toast. Silver lining: no orphaned chunks could ever exist (verified zero), the deletes just failed. Migration `20260716000001_chunks_delete_cascade.sql` (**APPLIED to the Studio DB**, supabase-rest restarted) flips `document_id` AND `update_id` FKs to `ON DELETE CASCADE` (every other chunk source FK already cascaded) — **deleting a document/update now provably removes its chunks** (verified in a rolled-back live transaction: delete succeeds, 0 orphans). `extracted_text`/`ai_summary` live on the row itself, so they die with it too.
- **Stranding hazard fixed in both delete routes** (`api/documents/[id]`, `api/opportunities/documents/[id]`): they removed the storage object BEFORE the DB row, so a failed row delete (which was happening every time, see above) would have left a row pointing at a missing file. Order flipped: DB row first (cascades chunks), storage cleanup after, non-fatal. Verified zero already-stranded rows in prod.
- `tsc` + eslint + full build clean; deployed (health 200, doc route 401s unauthed, /company 307s).

**Done 2026-07-14 (lender support — private_lender type, investor requirements checklist, Ber AI assist; DEPLOYED + SEEDED):**
- **Context: Mark Urbina (Noir Capital Group LLC, St. Petersburg FL — commercial lending broker) sent a per-project underwriting documentation checklist plus an SBA Form 1244 / SBA 504 advisory memo; Richard is strategically pursuing multiple funding sources, each with their own doc demands.** Decisions (his picks): lenders stay INSIDE the investors module (one pipeline, lightweight distinction — no structured loan fields, terms stay in `terms_notes`); requirements track status + link to existing platform documents (no per-investor upload surface / data room); template + per-project shape.
- **New table `investor_requirements`** (migration `20260714000001`, **APPLIED to the Studio DB**, supabase-rest restarted; `database.ts` hand-extended, `InvestorRequirement` alias): investor_id (cascade), nullable project_id (**null = standing/template requirement, set = specific deal package**), category corporate|project|sponsor|other, status needed|in_progress|have|submitted|waived|n_a, `evidence_doc_id` FK→documents (the file that satisfies it), notes, sort_order. Plain-text vocab in `src/lib/utils/investors.ts` (+ `REQUIREMENT_OPEN_STATUSES`); no log_activity() (investors precedent).
- **`private_lender` investor type** ("Private Lender / Debt Fund", teal) + `isLenderType()` (bank_lender|private_lender) → muted "Lender (debt)" chip on list cards + detail header. Flows through form/filters/detail automatically (they iterate the vocab maps); the one manual enum was the agent's `list_investors`.
- **Requirements section on `/investors/[id]`** (`InvestorRequirements.tsx`, clones the ComplianceSection idiom): category pills w/ counts, deal filter (All / Standard / per-project), inline status quick-select w/ optimistic rollback, evidence-document picker, outstanding count in the heading. API `POST /api/investor-requirements` + PATCH/DELETE `[id]` — **admin-only by default-deny** (getViewer guard; NOT in permissions.ts); DELETE selects `investor_id` back so the snapshot re-embeds.
- **Ber AI knows lender checklists:** requirements folded into `embedInvestorSnapshot` (`Documentation requirement (corporate) [needed]: … — standard (every deal)`) and `query_investor` (per-row category/status/project/evidence_on_file); agent prompt gains a **Lenders** paragraph — debt not equity, pull the checklist and cross-reference company profile/`get_company_qualifications`/stored docs → report **HAVE vs MISSING** by category; master project pipeline lists draft from `list_projects`.
- **Mark Urbina seeded live** (alias-loader script): directory party + investor (private_lender, in_conversation, email/phone/address/website) + **18 standing requirements** (his 13-item underwriting package + the 5-item SBA Form 1244 package, notes marking the SBA set) + contact-log note (submission-file framework, Master Portfolio List, 1244 memo) + **the Form 1244 memo PDF uploaded to the company Knowledge Base** (AI pass complete — summary + chunks; mentions EPC/OC structure w/ Myton Development LLC / SmartBuild Steel LLC, 50/40/10 stack, ASEA Global).
- **Verified:** 10-check live round-trip (insert/update/trigger/project-embed/snapshot/`match_chunks` ranks the lender chunk #1 for "what documents does our lender need"/cascade x2); `query_investor` tool returns all 18 requirements; `tsc` + eslint + full build clean; deployed (health 200; live POST 401s unauthed, /investors 307). FK note: `investor_requirements` adds a third investors↔projects join path — never write `from('investors').select('projects(...)')`.

**Done 2026-07-13 (weekly-report review pass — milestone query bug + NOW markers; DEPLOYED):**
- **Review of the same-day handoffs/report build at Richard's request. One real bug found: the report's "Dates that move" milestones query selected `milestones.title`, but the column is `label`** — PostgREST errored (verified live: `column milestones.title does not exist`), `milestoneRows` came back null, and the section could **never** render once milestone data lands. It slipped through because the table is empty (the query error is indistinguishable from no data). Fixed (select + render `label`); the milestone window also now starts at `today` like bids, matching the "next two weeks" heading.
- **The priority spine is now legible on every line:** tasks and handoffs tagged to a **Now** objective carry a small solid **NOW** mark (in `taskLine` + the per-person "Owes others" rows). The report's whole premise is "priority is mechanical — anything under a Now objective," but nothing at the task level showed that linkage before.
- **Letterhead gains an at-a-glance line** (`N open tasks · X overdue · Y handoffs (Z stale ≥7d)`), scoped to the person when `?person=` is set — the first thing the eye lands on in the emailed PDF. Also guarded: a null `waiting_on_since` (possible via direct DB writes; the API always sets it) rendered the handoff age as "nulld" — now an em dash.
- Verified: fixed milestones select runs clean against the Studio DB and the old one provably errors; both report views render in dev against real data (4 NOW chips = the 4 Now-tagged open tasks; person view titles the tab with the name for the PDF filename); `tsc` + eslint + full build clean; deployed; live route 307s unauthenticated. Reviewed clean, no changes needed: handoff triple logic (`resolveWaitingOn`/`selfBlockError`), effective-value self-block check in PATCH, FK-hinted embeds, admin-only default-deny, timezone-safe `dayString`, closed-last-week timestamp compare.

**Done 2026-07-13 (task handoffs + printable weekly report; DEPLOYED):**
- **Context: Richard's team can't reach the tailnet-only platform, so he routes everything by hand.** Two pains: the team can't see priority, and he's the human router for "Eric owes Dennis the survey before Nancy can file". Decision (his picks): a **blocker on the task** (not task→task dependencies, not free text) + a **print-to-PDF weekly report he emails himself** (not a public share link — that would mean re-exposing the Studio, with the token as the only boundary in front of a service-role app; and not platform-sent email yet — revisit once the format has settled).
- **Tasks gain a waiting-on triple** (migration `20260713000001_task_handoffs.sql`, **APPLIED to the Studio DB**, supabase-rest restarted; `database.ts` hand-extended): `waiting_on_id` (FK→team_members), `waiting_on_what`, `waiting_on_since`. Written and cleared **as a triple** — shared logic in `src/lib/tasks/handoff.ts` (`resolveWaitingOn` — setting a blocker requires saying what they owe, `since` defaults to today; `selfBlockError` — checked against the *effective* post-patch values so it catches both "block it on its own assignee" and "reassign it to the person it's already waiting on"). Wired into tasks POST/PATCH (400 on a self-block) + GET filters `?blocked=1` / `?blocking=<member>`. UI: Waiting-on row in `TaskDetailSheet` (person + what, aged, Clear button), amber "waiting on {name} · {n}d" chip on board cards (stale ≥7d), a **Blocked** filter, and a second count on the per-person workload chips — **holding up N** — which is the follow-up list.
- **`/reports/weekly/print`** — chromeless print-to-PDF document (clones the `/objectives/print` pattern; no PDF library). Organized **objective → person → handoff**, which makes priority mechanical (anything under a Now objective) instead of a weekly judgment call. Five sections: Now/Soon objectives w/ health + task counts, **Handoffs** (`{blocker} → {assignee}  {what}  {age}`, oldest first), Each person's week (overdue / due this week / **owes others** / waiting on others / no date), Dates that move (milestones + bid dates ≤14d), Closed last week. **`?person=<id>` narrows it to one teammate's page — that's the artifact that actually gets emailed.** Admin-only by default-deny (`/reports` is in no `permissions.ts` allowlist; link from the /tasks toolbar is gated on `viewer.isAdmin`). `layout.tsx`'s shell exclusion is now `pathname.endsWith('/print')` (was the hardcoded objectives path).
- **Fixed while here: `task_notes.author` was never populated.** The column existed and the API accepted it, but `TaskDetailSheet` posted only `{ body }` — every note in the system was unattributed. It's now stamped server-side from `getViewer()`, **never** taken from the client (otherwise a note could be posted under someone else's name).
- **GOTCHA (cost a whole class of runtime breakage): adding a second FK from `tasks` to `team_members` made every existing `assignee:team_members(...)` embed AMBIGUOUS** — PostgREST returns `PGRST201`, `tsc` catches only one of them. Every task surface (board, /tasks page, project tasks tab, opportunity + investor detail, agent `query_investor`) would have 500'd. All 7 call sites now hint the constraint: `assignee:team_members!tasks_assignee_id_fkey(...)`. **Rule: adding a second FK to an already-embedded table breaks every existing embed of it — hint the constraint name in all of them.**
- **Verified:** handoff helpers unit-tested (9 cases); live against the Studio stack — both embeds resolve, the un-hinted form confirmed to fail with PGRST201, insert/join/`?blocked`/`?blocking`/clear round-trip, test rows deleted; report rendered against real data with seeded handoffs (all 5 sections, correct ages + sort, chromeless with the middleware `x-pathname` header, `?person=` scoping showing both directions of a handoff); board HTML shows chip + Blocked filter + "holding up N". `tsc` + eslint (20 = the documented pre-existing survivors) + full build clean; deployed; live route 307s unauthenticated (the report is NOT publicly readable). All test data removed.
- **Note for Richard:** "Dates that move" will render empty until there's data — the `milestones` table is **empty** and **no project has a `bid_due_date`**. Deliberately deferred: an AI-written narrative on the report (the daily-brief cron already assembles this data and writes markdown to `stored_briefs` that **nothing renders** — there's no `/briefs` page; layer it in once the factual format has proven itself) and platform-sent email (would need `Mail.Send` on the Graph scopes + one Azure re-consent).

**Done 2026-07-12 (debugging + cleanup pass — zero `any`, dead code deleted, dep patches):**
- **Every `any` escape in `src` is gone (23 → 0).** The `project_dependencies`/`stored_briefs` cast clusters (dependencies routes, daily-brief cron, meeting-prep, attention.ts, agent-tools) were stale — the generated types already covered those tables. Jsonb inserts in proposals intake now use `as unknown as Json` (email-intake's `as unknown as never` normalized to match); agent `list_projects` filters cast to the real `Database['public']['Enums']` types; CalendarView's polymorphic wrapper is `ElementType`; the activity page query builder is fully typed. §7/§9 updated — the type-drift debt item is closed.
- **4 orphaned components deleted:** `ui/slider.tsx`, `projects/PlayersTab.tsx` (already a `return null` stub — the players page renders inline), `dashboard/ReviewQueueBadge.tsx`, `dashboard/ActionItemsSummary.tsx` (also a stub). Verified nothing imports them.
- **Dependency security patches:** `npm audit fix` (semver-compatible only) cleared 8 of 10 advisories (@babel/core, brace-expansion, fast-uri, hono, ip-address/express-rate-limit, js-yaml). The 2 moderate survivors are postcss vendored *inside* next itself — only a future Next upgrade clears them; do NOT run `audit fix --force` (it downgrades next to 9.x). Gotcha hit: `npm audit fix --omit=dev` prunes devDependencies (@types/react-dom vanished, tsc broke) — plain `npm install` restores; don't pass `--omit=dev` to audit fix.
- **Swept clean:** no Finder-duplicate `* 2.*` artifacts, no raw `<input type="date">`, no `nextUrl.origin` misuse, no browser-side `createSignedUrl`, no console.log in src, no stale n8n/Anthropic/Perplexity/NEXT_PUBLIC_SITE_URL refs, no tracked secrets, no unused npm deps. Lint survivors recounted: 20 (2 documented dashboard purity misfires + 12 set-state-in-effect + 6 img warnings — all documented accepted classes).
- Verified: `tsc` + eslint + full `next build` clean; production `next start` smoke test (login 200, unauth dashboard 307, unauth API 401 — middleware boundary intact).

**Done 2026-07-12 (admin password reset on /settings/users; DEPLOYED):**
- **Context: Eric (tuaone@) couldn't log in post-cutover.** Diagnosis: his auth account was fine — (a) the tailnet contains ONLY Richard's devices, so he can't reach the platform URL at all (fix: Tailscale admin console → Users → invite external user; apply the drafted ACL from `deploy/security/` when he joins), and (b) the self-hosted GoTrue has placeholder SMTP (`supabase-mail:2500`), so invite/reset/magic-link emails NEVER send — and the login UI has no forgot-password flow anyway. **The paused cloud Supabase dashboard is NOT a user-management surface** — everything lives on the Studio. Eric's password was reset via the GoTrue admin API and verified with a live password-grant sign-in; Richard has it to share.
- **`/settings/users` now does password resets.** `PATCH /api/admin/users/[id]` accepts `password` (min 8 chars; 400 when the member has no login yet) → `admin.auth.admin.updateUserById` — no email involved, which is exactly right for this stack. Member cards with a login get a **Reset password** button → modal pre-generates a memorable `Word-Word-####` password (crypto.getRandomValues; editable, regenerate button), then a copy-it-now confirmation ("no email is sent, won't be shown again").
- Verified: exact supabase-js call path exercised live against the Studio stack (alias-loader script: updateUserById OK + sign-in OK, net no-op password); `tsc` + eslint + full build clean; deployed; live route 401s unauthenticated. If self-service resets/invites are ever wanted, wire real SMTP into GoTrue's env — until then this button is the password path.

**Done 2026-07-12 (world overview basemap + composite tile route; DEPLOYED):**
- **Richard: world zoom looked like blank ocean (Pacific islands invisible) — wanted the low-zoom worldwide layer.** Now a second archive `~/berwilson-data/maps/world.pmtiles` (whole planet z0-7, only 186MB) renders continents/countries everywhere at world zoom; full detail still only in the region boxes.
- **Tile serving switched from pmtiles-protocol byte ranges to a per-tile route** `GET /api/map/tiles/{z}/{x}/{y}` that composites server-side: **z≤7 from the world archive, z8+ from the regions archive** (world missing → low zooms degrade to regions-only; regions missing → 503 which still triggers the map's "basemap not installed" notice). Archives are cached with open FileHandles and **reopened automatically on mtime/size change — replacing an archive still needs no restart**. Out-of-coverage tiles → 204 (maplibre renders empty). Client: `style.ts` uses a standard `tiles` URL template; the pmtiles `Protocol`/`ensureProtocol` client code is gone from `MapView.tsx` (the `pmtiles` dep now serves the route server-side).
- `setup-map-data.sh` extracts the world archive alongside the main one; the deploy script push-once rule covers `world.pmtiles` too; env `MAP_WORLD_PMTILES_PATH` optional override. Two new pins created at Richard's request (his picks: new records, not reusing "Ber Wilson Pre Fab Steel"): **Tonga Development Project** (real_estate/pursuit, Nukuʻalofa) and **Pre-Fab Steel** (prefab/pursuit, Tirana) — deep links verified live in his session.
- Verified: route semantics curled (z2 Pacific 200 w/ Fiji/Samoa/NZ labels in the MVT, z5 Europe 200, z12 Nukuʻalofa 200, z12 Fiji + z8 mid-Pacific 204, bad coords 400); `tsc` + eslint + full build clean. (Browser extension disconnected mid-session — final world-zoom visual check was tile-decode-level, not screenshot.)

**Done 2026-07-12 (international map coverage — Tonga + Albania; DEPLOYED):**
- **Richard: projects are expanding to Tonga (certain) and possibly Albania — the map must cover them.** Chosen approach (his pick): targeted-regions, not whole-world — one archive, full street-level detail in US + Tonga + Albania (+~100MB vs CONUS-only). DB lat/lng checks and the projects PATCH validation were already world-valid; no migration.
- **Camera unlocked worldwide:** `MapView.tsx` `maxBounds` (the CONUS clamp that physically blocked panning to Tonga/Albania) removed; `minZoom` 3 → 1.5. Everything else (place/draw/clusters/tour/flyTo) was already coordinate-agnostic.
- **Basemap coverage is now defined by committed region files** `scripts/map-regions-{full,dev}.geojson` (GeoJSON MultiPolygon — `pmtiles extract --region` supports it; single-bbox scopes kept for back-compat). `setup-map-data.sh`: `SCOPE=dev` (default, Utah+Tonga+Albania, ~478MB) / `SCOPE=regions` (CONUS+Tonga+Albania, production). **Adding a country = add a box to both files + names to `INTERNATIONAL` in geocode.ts + re-extract on the Studio** (deploy/README.md has the exact runbook: dry-run → extract to a new file → `pmtiles show` → `mv`; no service restart — the tiles route opens the file per request).
- **Geocoder international assist:** `geocode.ts` gains a small hand-kept `INTERNATIONAL` lookup checked before the US logic (Tonga/Nukuʻalofa → -21.14,-175.2; Albania/Tirana) with apostrophe/okina normalization. Capitals = `city` precision; bare country names = `state` precision (bulk auto-place correctly skips them; single warns "use Reposition" — copy updated to say "state or country"). Everything else stays manual pin, per Richard.
- **Verified:** 16-case geocode test (internationals + okina/curly-apostrophe forms + US regressions + "Suva, Fiji"→null); tile bytes present at Nukuʻalofa/Tirana/SLC and absent outside coverage; live browser render — Tongatapu fully drawn (villages/roads/lagoon), Tirana 591 rendered features, camera pans worldwide, geocode API resolves over HTTP; `tsc` + eslint + full build clean. Note for future browser checks: maplibre in a background tab throttles painting — `map.resize()+triggerRepaint()` before judging a gray canvas.

**Done 2026-07-12 (read-aloud button on Ber AI answers; DEPLOYED):**
- **Richard asked for local speech: a button that reads a Ber AI answer out loud.** Chosen engine (his pick): **Web Speech API** (`window.speechSynthesis`) — 100% on-device macOS system voices, zero deps, zero server changes. Deliberately deferred upgrade path: a local Kokoro-82M service on the Studio behind the same button if voice quality ever bugs him; free interim upgrade = download an Enhanced/Premium English voice in macOS System Settings → Accessibility → Spoken Content (the button auto-prefers Premium/Enhanced local voices; only `localService===true` voices are ever used — locality guarantee).
- **New `src/components/shared/ReadAloudButton.tsx`** (play/stop toggle, Volume2↔Square, ghost-icon idiom): `speakableText()` strips `[N]` citations, markdown (bold/headers/bullets/code/table rows) and orphaned punctuation; `chunkText()` splits into ≤~200-char sentence chunks queued as separate utterances (Chrome silently stalls on long single utterances); ownership-tracked lifecycle so unmount/a second button never kills someone else's playback; hidden when the API is unavailable; hydration-safe capability check via `useSyncExternalStore` (not setState-in-effect — keeps that lint class at zero for new code).
- Wired as the first item in the existing assistant action row in `AgentChat.tsx` → the ⌘J dock and /intel Agent tab both get it. Verified live on the deployed Studio via browser automation: play (speaking+pending true, queued chunks), mid-playback stop, natural-end auto-reset; cleanup regexes unit-tested (extracted verbatim from the file). `tsc` + eslint + full build clean; deployed.
- **Follow-up same day (history fix + document read-aloud + dead-code sweep; DEPLOYED, verified live):**
  - **/intel History panel now works.** Root cause: `IntelTabs` tracked the selected conversation and remounted the chat, but never passed the id down (and `AgentChat` had no prop for it). `AgentChat` gained `conversationId` (loads that conversation's messages on mount) + `onConversationCreated` (fires when a fresh chat persists); `IntelTabs` passes the selection, keys the chat by a selection counter (so the mid-stream id assignment doesn't remount), and bumps a `refreshToken` on `ConversationList` so new conversations appear without a reload. Portfolio-wide placeholder fixed ("Ask about your portfolio…" — was "Ask about this project...").
  - **Documents can be read aloud.** `GET /api/documents/[id]?text=1` returns stored `extracted_text` (fallback `ai_summary`; same viewer guards as the signed-URL path; 404 when neither). `ReadAloudButton` gained `getText` (lazy fetch, spinner while loading) + `iconSize` + `cn()` class merging; client helper `fetchDocumentText` in `document-links.ts`. Speaker button wired into project DocumentsTab rows and company Knowledge Base rows — reads the same text Ber AI indexes. No-text files toast a pointer to the Mac's built-in reader.
  - **Dead code deleted:** `IntelClient`, `GroundedAnswer`, `QueryInput`, `QueryHistory`, `CitationCard`, `AgentTab` (~730 lines) — orphaned since /intel moved to `IntelTabs`; nothing imported them. `/api/ai/synthesize` left untouched.
  - Verified live on the Studio: history click loads messages, document speaker fetches + speaks extracted text, stop works. (Reminder: hard-refresh after deploys — Chrome caches old chunks.) The Finder-duplicate `.next/types/* 2.ts` artifacts reappeared and were deleted again.
  - **Data sweep (same day): every broken document summary fixed in prod.** 22 of 41 docs were in a bad state: 4 stored the raw model JSON (fenced ```json {"summary"…}) as `ai_summary` — parsed out in place; 2 had truncated JSON fragments; the rest were maxTokens-era empty summaries plus pending/error strays. 21 re-ran through `runDocumentAiPass` standalone (alias-loader pattern against the Studio stack, chunks wiped per doc first — same as the Reindex button); 1 was an orphaned duplicate whose storage object no longer exists (byte-identical working copy present) — row + 3 stale chunks deleted after confirming the file was gone. Final state verified: 40 docs, 0 broken, 0 chunks containing json junk. No code changes — data only.

**Done 2026-07-12 (document indexing: docx support, honest statuses, reindex button; DEPLOYED):**
- **Richard's report: company Knowledge Base docs stuck on "Indexing…" forever, one PDF failed.** Root cause was NOT the Studio sleeping: **the doc AI pass only handled PDFs + plain text — every `.docx` was silently skipped** and sat at the default `embedding_status='pending'` (which the UI renders as "Indexing…"). 7 of 11 company docs were docx (investor package, FAQ, exec one-pager, brag sheet) with zero AI coverage. The failed PDF (`BerWilson_WalkAway_PlatformEdition.pdf`, uploaded 07-10) was the pre-fix `maxTokens:512` empty-summary bug (verified in `ai_queries`: tokens_out=512, empty response, `ai_summary=''`).
- **Word (.docx) is now a first-class indexed type**: new dep `mammoth` → `extractDocxText` in `document-text.ts` (≥40 chars or null). Legacy binary `.doc` remains unsupported → status `skipped` ("Not indexed" chip), file still stored.
- **New shared `src/lib/ai/document-pipeline.ts`** — `runDocumentAiPass()` (never throws; settles `embedding_status` to complete/error/skipped) + `documentKind()` (mime + extension fallback). Replaced the three near-copies: `api/documents/upload`, `api/documents` (both now fully awaited — no more fire-and-forget embed, so a mid-embed restart can't strand 'processing'), and email-intake `processPromotedDocumentAi` (documents-table branch uses the pipeline; opportunity_documents branch + `api/opportunities/documents` gained docx via `documentKind`/`extractDocxText` by hand — that table has no status column). `embedDocument` now returns boolean. Uploads with `extract_ai=false` set `skipped` instead of dangling at 'pending'.
- **Reindex without re-upload**: `POST /api/documents/[id]/reindex` (maxDuration 300) — downloads from storage, **deletes the doc's old chunks** (no double-index), re-runs the pipeline. Same viewer guard as the [id] GET/DELETE (non-admin: granted projects only; company docs admin-only). Company KB rows that aren't `complete` show a RefreshCw button; `EmbedStatus` gained the muted `skipped` → "Not indexed" state (tooltip explains). Upload hint copy now says "Word (.docx)".
- **All 8 stuck company docs re-indexed live** via repo lib against the Studio stack (script pattern: alias-loader + `--env-file`); verified statuses flipped to complete with real summaries + chunks. `tsc` + eslint + full build clean; deployed to the Studio.

**Done 2026-07-12 (email intake: discard reviewed packages + default-to-project; DEPLOYED):**
- **Richard's asks: (a) a way to delete a reviewed ingestion he doesn't want, (b) intake should default to creating a project — opportunity only for buying/acquiring a company — because switching kinds lost the prepopulated data.**
- **Discard is now first-class.** The existing PATCH dismiss endpoint already allowed dismissing pending sessions (it clears staged attachment files too), but the UI only showed the ✕ on failed rows. Now: pending rows in the Recent list get the same dismiss ✕, and the review screen (`EmailIngestReview`) gains a **Discard** button (ConfirmDialog) that dismisses and returns to `/email-ingestion`. Confirmed sessions stay undismissable by design — they're the record of what was created.
- **Project is the default kind.** Prompt (`email-intake-1.2`): "project" is stated as the default; "opportunity" ONLY for corporate transactions (purchase/acquisition of a company, merger, divestiture, equity investment in a business); partnerships/JVs/teaming around built work = project. Code fallback flipped to match: `normalize()` in `analyze.ts` now defaults `suggested_record` to `project` when the model omits/garbles it (was opportunity).
- **Switching kinds no longer loses data.** The review toggle now goes through `switchKind()`, which carries shared facts into the other record's blanks (name, sector, location, value; description↔objective/thesis; client↔counterparty/target) — never overwriting user edits. This works regardless of how completely the model filled both objects.
- `tsc` + eslint + full build clean; deployed to the Studio.

**Done 2026-07-11 (email intake: attachment picker + research-report document; DEPLOYED):**
- **Richard's ask: keep the prepopulated record creation, but also (a) get a document that fully summarizes the discussions + attachments, and (b) pull the attachments themselves and pick which to upload onto the created project/opportunity.** Both shipped; the run/review/confirm architecture absorbed it as an extension, no rework.
- **Attachments are now staged during the research run.** `/api/email-research/run` uploads every non-inline attachment (ALL file types incl. docx/xlsx — wider than the AI-analysis set; ≤25MB each, 30/run, deduped by name+size across threads) to storage at `email-intake/{sessionId}/…` and records `StagedAttachment[]` on new column **`email_intake_sessions.staged_attachments`** (migration `20260711000002`, **APPLIED to the Studio DB**, supabase-rest restarted; `database.ts` hand-extended). The 3-per-thread AI-analysis cap stays (local Qwen is 30–60s/attachment); report lines now say "kept for review" instead of just "skipped". Failure paths clean the staging files; dismiss (PATCH) does too.
- **Review screen gains an Attachments section** (`EmailIngestReview`): checkbox per file (default all checked), size + source thread + "content in report" marker, View button via new `GET /api/email-ingestion/sessions/[id]/attachment?path=…` (server-side signed URL, same pattern as document-links; validates the path belongs to the session; admin-only by default like the rest of email-ingestion).
- **Confirm promotes the picks + attaches the report.** `confirm` accepts `attachment_paths`, copies selected files into `projects/{id}/…` or `opportunities/{id}/…`, inserts `documents`/`opportunity_documents` rows, then runs the standard AI pass (summary + extracted_text + embedding) **fire-and-forget sequentially after the response** (`src/lib/email-ingestion/attachments.ts` — `promoteStagedAttachment`/`processPromotedDocumentAi`). It also saves the **full research report as a named .md document** on the record ("Email research — {label}.md"), headed by a new AI-written **`discussion_summary`** (multi-paragraph narrative; `email-intake-1.1` prompt) with the full transcript below; `extracted_text` stored so the agent can quote it, deliberately NOT re-embedded (the update-row / embedOpportunityReport paths already index that content). Staging folder cleared after promotion; `created_record_ids` gains `document_ids`.
- **Found + fixed while testing (both pre-existing):** (1) **`maxTokens: 512` on doc summaries returns an EMPTY summary in local mode** — Qwen burns the whole budget on reasoning (`tokens_out: 512`, empty response verified in `ai_queries`). Bumped to 2048 in the new lib AND both existing upload routes (`api/documents`, `api/opportunities/documents`). (2) **Deleting a project failed platform-wide** (`activity_log_project_id_fkey` violation): cascade-deleted children (documents/updates/tasks…) fire their log triggers after the project row is gone and the audit INSERT references the dead project. Migration `20260711000003_activity_log_cascade_delete_fix.sql` (**APPLIED**) retries the insert with `project_id` NULL on FK violation — audit row kept (metadata still snapshots the old row). Verified live: project delete now succeeds, children cascade, audit rows survive (historical rows get project_id nulled by the FK's existing SET NULL, as before).
- **Verified live end-to-end against the Studio stack** (staged session + files via repo lib, real confirm POST through a dev server): report doc (markdown, discussion summary + ai_summary), promoted attachment row + downloadable storage object, unselected file cleaned up with the staging folder, session confirmed with document_ids, updates-tab row unchanged, background AI pass produced a real summary + embedding chunks post-fix, attachment route 401s unauthenticated. All test records deleted. `tsc` + eslint + full build clean.
- **Noticed in ai_queries while testing:** Richard's "Colton" research run TODAY succeeded against tuaone@ (mailbox reconnect worked) but **info@ and moose@ were access-denied** — the grant is missing shared-mailbox access (`Mail.Read.Shared` consent and/or M365 delegation). Reports currently carry a per-mailbox skip note. Note the run staged with the OLD code, so that session has no attachments — the next run will.
- **FOLLOW-UP same day — local AI is now UNBUDGETED (Richard's call: the model is free, let it work as long as it wants):** `localChat`/`localChatStream` no longer send `max_tokens` at all; the `maxTokens` option is accepted but ignored in local mode (kept so callers share one shape with the Gemini path, where it still caps paid output). This permanently kills the empty-summary failure class. Verified live: unbudgeted call returns normally.
- **Shared-mailbox diagnosis (verified live):** tuaone's stored grant now DOES carry `Mail.Read.Shared` (+ ReadWrite.Shared etc. — the Azure re-consent worked), and tuaone@ search returns real threads. **info@ and moose@ still 403 `ErrorAccessDenied / "Cannot find row based on condition"` — that is EXCHANGE-level mailbox delegation, not an Azure API permission.** Fix is in Microsoft 365 admin center → Users → (info@ / moose@) → Mail → Mailbox permissions → **Read and manage (Full Access)** → add tuaone@berwilson.com (propagation can take minutes to ~1h). Azure App-registration permission grants cannot fix this. Also noted: a stale May-era `email_tokens` row for info@ exists — unused (graph-search authenticates only as tuaone@), harmless.

**Done 2026-07-11 (system-health maintenance surface — live probes + self-service mailbox reconnect; DEPLOYED):**
- **Context: Richard asked for a way to see and fix routine breakage himself** (the immediate case: the tuaone@ password change revoked the Graph grant and every mailbox feature died silently — the old health page still showed the token as "connected"). Decision: no separate portal, and NO password entry (OAuth never stores the password — the fix is always re-running consent); instead `/settings/health` became the single maintenance surface with the fix next to each diagnosis.
- **New `src/lib/system-health.ts`** (server-only probes, never throw): `probeGraphConnection()` — a REAL token refresh against Microsoft (success rotates+stores tokens, doubling as keep-alive; failure translates AADSTS codes to plain English: 50173 password-changed, 700082 inactivity, 7000222/invalid_client secret-expired → "reconnect will NOT fix, rotate the secret"), `probeLmStudio()` (pings `/models`, verifies both configured model ids are served), `probeBackups()` (stats newest file under `~/Backups/berwilson`, fail >30h; warn-not-fail when dir missing = dev machine), `probeDisk()` (`fs.statfs('/')`, fail <15GB / warn <50GB), `mailboxLooksBroken(expires_at)` (cheap DB-only heuristic: expiry >30h past — healthy worst case is ~23h because the daily brief refreshes daily).
- **`/settings/health` rebuilt around the probes:** checks gained an `action` button — the Microsoft check renders **Reconnect Mailbox → `/api/email/oauth`** with instructions (sign in as tuaone@ with the CURRENT password) and the exact redirect URI to add in Azure if Microsoft rejects (computed from forwarded headers). New checks: Local AI Engine (LM Studio), Nightly Backup, Disk Space, and optional **Azure client secret expiry** via new env `MICROSOFT_SECRET_EXPIRES=YYYY-MM-DD` (warn <30d — when that secret expires even Reconnect fails, so it's tracked explicitly). All stale Vercel copy replaced with Studio/launchd instructions (`~/Library/Logs/berwilson/`). AI-pipeline check is provider-aware ("Local Qwen").
- **Dashboard alarm:** the morning read now leads with a critical AlertsBanner line ("Mailbox disconnected — … Reconnect from System Health") when `mailboxLooksBroken` — one cheap `email_tokens` read, no network on dashboard load. OAuth callback success page links back to /settings/health; its stale Vercel copy fixed too.
- **Verified live against the real broken state:** probes exercised standalone (repo lib via alias-loader): Graph probe returned the actual AADSTS50173 with the friendly translation; LM Studio ok (both models served); disk 53/245GB; backups correctly "missing" on the MacBook; dev renders of /settings/health (all 9 checks + Reconnect button) and /dashboard (banner present) against the Studio DB. `tsc` + eslint (survivors = the 2 documented dashboard purity misfires) + full build clean; deployed to the Studio. **Richard still needs to click Reconnect Mailbox on /settings/health (sign in as tuaone@) to actually restore calendar/email research** — and if Microsoft shows a redirect-URI error, add the callback URL to Azure first (the page shows the exact URL).
- **FIX (same day): Richard's first Reconnect click failed with AADSTS50011 — the app sent Microsoft `https://localhost:3000/...` as the redirect URI.** Root cause: behind `tailscale serve` the Host header is rewritten to the backend (localhost:3000), so `request.nextUrl.origin` is wrong for anything user-facing; the real host travels in `x-forwarded-host`. New `src/lib/utils/request-origin.ts` (`publicOrigin(headers)`) — used by BOTH oauth routes (authorize + callback must byte-match) and the health page. **Rule: never use `request.nextUrl.origin` for user-facing URLs (OAuth redirect URIs, links) — use `publicOrigin(request.headers)`.** Verified the authorize URL now carries the tailnet redirect URI (standalone run against real env; client_id matches the Azure app 9e2420e3). Remaining Azure-side step for Richard: register `https://richards-mac-studio.tail0e5306.ts.net/api/email/oauth/callback` on the app registration (Entra ID → App registrations → Authentication → Web), then Reconnect.

**Done 2026-07-11 (executive steering pass — verticals, objective health, meeting triage, investor contacts; DEPLOYED):**
- **Context: Richard's mandate is chief of staff for the whole holding company** (manufacturing, development, construction, technology, health verticals). Audit verdict: the objectives/attention/tasks/brief loop was solid; the corporate dimension was missing. This pass closes it.
- **Sectors now cover the verticals: `project_sector` gains `technology` + `health`** (migration `20260711000001_verticals_health_investor_contact.sql`, **APPLIED to the Studio DB**, supabase-rest restarted). Wired through: constants (technology=sky, health=rose), map markers (puck/outline/line colors; icon fallbacks technology→data_center, health→hospital), **every AI prompt that enumerated sectors** (proposal-intake, email-intake, classification, synthesis) and the agent's `list_projects` sector enum — without these an inbound health/tech deal could never be classified into its own vertical. `tsc` confirmed no other exhaustive sector maps existed.
- **Objectives gain a health signal** (`objectives.health` text on_track|at_risk|stalled, default on_track, same migration): 3-way picker in the board's edit form; board cards + dashboard Now strip + `/objectives/print` show an AT RISK/STALLED badge (**on-track stays visually silent** — calm instrument); daily-brief objective lines carry the flag and the prompt says flagged objectives lead the Steering Check. PATCH whitelists `health` (400 on bad value; round-trip verified live).
- **Dashboard gains a "By Vertical" rollup band** (`VerticalRollup.tsx`, after the KPI band): per-sector count / pipeline / weighted value, sector-badge colored, sorted by pipeline; hidden when everything sits in one vertical. Deliberately NOT a resurrection of the removed Portfolio module — sectors ARE the vertical dimension; no owning-entity FK was added.
- **Daily brief gains "This Week's Meetings"**: the cron fetches the next 7 days from the connected Outlook mailbox (`fetchCalendarEvents`, capped 15, times rendered America/Denver) and the prompt ranks them against Now objectives — which need prep, which advance a stated priority, which serve none, and any Now objective with nothing on the calendar. Fails quietly (try/catch → section absent) when Graph is unavailable.
- **FOUND WHILE TESTING: the Microsoft Graph grant is REVOKED** (AADSTS50173 — tuaone@'s password was changed/reset 2026-07-09; the stored refresh token predates it). **/calendar meeting prep, /email-research, AND the new meetings block are all dead until the mailbox is reconnected via `/api/email/oauth`** (remember the Azure redirect-URI item from the cutover notes). Verified live that the brief's meetings path degrades gracefully against the real error.
- **Investors gain `email` + `phone`** (same migration): form fields (Who section), detail Key Facts with mailto:/tel: links, included in `embedInvestorSnapshot` (ask Ber AI for an investor's contact info), and passed into the auto-created directory party on investor create so the contact lands in `/contacts` too.
- Verified: `tsc` + eslint clean (survivors = the 2 documented dashboard purity misfires) + full build; dev render checks (dashboard/objectives/print/investors/map) against the Studio DB; objective-health PATCH round-trip incl. invalid-value 400; investor insert with contact fields; deployed to the Studio (health 200, live routes 307 unauthed).

**Done 2026-07-11 (raise/investor detail crash fix; DEPLOYED):**
- **Both `/investors/raises/[id]` and `/investors/[id]` crashed at render** ("Failed to load investors" — the `/investors` error boundary): `isPastDate` was defined in `InvestorsClient.tsx` (`'use client'`) and imported by those two **server** pages — every export of a client module becomes a client reference in server code, and calling one throws (`Attempted to call isPastDate() from the server…`). Reproduced in dev, fixed by moving `isPastDate` to `src/lib/utils/investors.ts` (plain module); all three call sites import from there. **Rule: helpers shared between server pages and client components live in `src/lib`, never exported from a `'use client'` file** — `tsc`/build DON'T catch this, it only fails at request time. Swept all 108 client modules: the only other lowercase function exports (`toKey`/`parseKey` in `calendar.tsx`) are consumed exclusively by client code — clean.
- Raise page's empty Investors state made actionable: names the raise to pick in an investment's Raise field, with "Go to investors" / "New investor" buttons (previously described the path with no link).
- Verified in dev against the Studio DB (raise page 200, tranche table + money band render); `tsc` + eslint + full build clean; deployed to the Studio (health 200, live route 307 unauthed).

**Done 2026-07-10 (unified date picker app-wide; DEPLOYED):**
- **Every date field in the app now uses one `DatePicker`** (`src/components/ui/date-picker.tsx`, the tasks-era component rebuilt). It's a real text input — hand-type any common format (`7/10/26`, `Jul 10 2026`, `2026-07-10`, `7/10` = current year), committed on Enter/blur (unparseable input reverts; Feb 31-style overflow rejected) — plus the month-calendar dropdown for mouse selection, clear ✕, Escape-to-close. Two modes: controlled (`value`/`onChange` — unchanged contract for TeamTaskBoard/TaskDetailSheet/ObjectivesBoard) and **plain-form** (`name`/`defaultValue` → hidden `YYYY-MM-DD` input, drop-in for server-action forms). Size overrides via `className` (twMerge), e.g. `h-8 text-xs` compact variants.
- **Motivation: the investors tab's native `<input type="date">` boxes wouldn't accept edits for Richard.** All raw date inputs replaced app-wide: investors (InvestorForm ×2, InvestmentsSection ×4, RaiseForm ×3 incl. tranche rows), OpportunityForm ×2, ProjectForm ×4, projects quick-edit overlay, MilestonesTab, DiligenceTab, EntitiesTab, FinancingTab draw rows (borderless variant), ActivityFilters ×2, EmailIngestReview task rows, CompanyProfileClient certs (InputRow renders DatePicker when `type="date"`). Zero `type="date"` left in src. **Rule for new UI: date fields use `DatePicker`, never native `<input type="date">`.**
- Parser unit-tested (19 formats incl. rejects); `tsc` + eslint clean (survivors = documented pre-existing); full build clean; dev-server render verified on 5 pages. Deployed to the Studio (health 200). (The Finder-duplicate `.next/types/* 2.ts` artifacts broke `tsc` again — deleted again.)

**Done 2026-07-10 (raises — named capital raises with tranche schedules; DEPLOYED):**
- **New concept: `raises`** (migration `20260710000003_raises.sql`, **APPLIED to the Studio DB**, supabase-rest restarted) — the container a round lives in: name, target_kind company|project (+project_id, same check as investments), `target_amount`, status planned|open|closed, dates, notes, and **`tranches` jsonb** (`[{label, amount, target_date}]`). Context: the first raise is **$88M in tranches of 25/25/25/13**; subsequent raises will follow — without this, Raise 2's commitments would be indistinguishable from Raise 1's. Plus **`investments.raise_id`** (FK, on delete SET NULL — deleting a raise never deletes money records).
- **Tranches are TARGETS, not ledger rows (Richard confirmed the model):** investors commit to the *raise*; tranches fill **sequentially from cumulative money levels** (waterfall fill). `src/lib/investors/raises.ts` holds the math: `raiseLevels(investments)` collapses the money triple into cumulative levels — per deal `wired=funded`, `signed=max(committed,funded)`, `potential=max(indicated,signed)` (robust to entry style; raw sums kept for stat bands) — and `fillTranches()` slices those levels across the tranche windows. Also `parseTranches` (tolerant jsonb parse) + `parseRaiseFields` (shared API validation). If per-tranche investor earmarking is ever needed, add a tranche tag on investments then.
- **Dashboard surfaces:** `/investors` gains a **Capital Raises band** (card per raise: status badge, target, segmented tranche bar with funded/committed/potential fills + legend, money line, investor count; open/planned before closed) + a New Raise button. **`/investors/raises/[id]`** is the per-raise dashboard: money band (Target / Committed / Funded / **Potential with gap-to-target** — "$X uncovered even if all indications convert"), tranche fill table (amount/committed/funded/to-commit/target date), and the per-investor table (deal stage, instrument, triple, next step, totals footer). `/investors/raises/new` + `[id]/edit` = `RaiseForm` (tranche editor rows, running sum vs target delta). Shared bar: `RaiseTrancheBar` (server-renderable divs; emerald=funded, indigo=committed, amber=indicated). No tranche schedule → the bar treats the whole target as one tranche; raises are admin-only like the rest of `/investors` (`/api/raises` not in any allowlist).
- **Investment tagging:** InvestmentsSection gains a Raise picker (only when raises exist) + raise chip on rows; `parseInvestmentFields` gains `raise_id`; investor detail fetches raises + joins them.
- **Wiring:** `embedInvestorSnapshot` investment lines say `under raise "<name>"`; daily-brief Capital Raise block leads with **per-raise standing** (target, committed/funded/potential, which tranche is filling); agent `list_investors` returns `raises` standings (incl. per-tranche fill) + per-investor raise names, `query_investor` investments include the raise; agent prompt explains raises + sequential-fill semantics. API: `POST /api/raises`, `PATCH/DELETE /api/raises/[id]` (PATCH is partial; name backfilled for status-only changes).
- **Verified live end-to-end** (repo lib via alias-loader against the Studio stack, 18/18 checks): waterfall math on the exact 88M/25-25-25-13 shape, raise+investment round-trip, both page joins, snapshot embedding mentions the raise, delete → raise_id nulls + investment survives, cleanup cascades chunks. `database.ts` hand-extended (`raises` + `investments.raise_id`); `tsc` + eslint + full build clean; deployed. (Note: the Finder-duplicate `.next/types/* 2.ts` files that break `tsc` reappeared and were deleted again — they're build artifacts.)

**Done 2026-07-10 (investors screen polish pass; DEPLOYED):**
- **App-wide date fix found while polishing: `formatDate` (constants.ts) rendered date-only strings one day early** in US timezones (`new Date('YYYY-MM-DD')` = UTC midnight → previous local day). Now pins date-only strings to local midnight; timestamps unchanged. Affected every date-only display app-wide (investor next steps, tranche target dates, milestones, etc.). Verified: old path showed Jul 15 as "Jul 14" in MDT, new path correct.
- **Overdue signals:** investor cards + detail highlight an overdue `next_step_date` amber with an explicit "overdue" marker (suppressed for passed/dormant); the raise dashboard's tranche table flags past target dates on under-committed tranches as "· slipped" (open/planned raises only). Shared `isPastDate` exported from `InvestorsClient`.
- **Money-entry safety:** live dollar readback ("= $88.0M") under the raw amount inputs in RaiseForm (target) and InvestmentsSection (indicated/committed/funded) — catches magnitude typos. InvestorForm's check-size inputs deliberately skipped (uncontrolled server-action form, profile data not ledger money).
- **Smaller polish:** target-kind filter relabeled "All Targets" (old "All Raises" label collided with the real raises concept); raise cards on /investors show "% committed" next to target; investor detail money band gains Soft-circled/Signed/Wired sublabels (matches list-page stats); investor + raise detail pages get `generateMetadata` (record name in the tab title).
- `tsc` + eslint + full build clean; committed, pushed, deployed to the Studio (health 200). (Finder-duplicate `.next/types/* 2.ts` artifacts deleted again.)

**Done 2026-07-10 (investor steering pass — tasks, Ber AI, attention, brief; DEPLOYED):**
- **Tasks tag to investors.** `tasks.investor_id` (migration `20260710000002_investor_steering.sql`, mirrors the opportunity-tag pattern exactly: dual-schema tolerant, written only when chosen). TeamTaskBoard gains investor picker/filter/HandCoins chip + `scopeInvestorId`; TaskDetailSheet gains picker + context-link card; tasks API create/patch/filter (`?investor=`); investor detail page gains a Tasks section (`InvestorTasks`). **Investor options on /tasks are admin-only** (zeroed for every other role incl. executives — capital raise is sensitive; a crafted investor_id write by a member is accepted like objective_id, but they can't enumerate investor UUIDs since /api/investors is admin-only).
- **Ber AI can query the raise.** Two new agent tools: `list_investors` (stage/interest/type/target filters; per-investor + total indicated/committed/funded rollups, targets) and `query_investor` (fuzzy name → full record + investments w/ terms + notes + open tasks). Agent prompt gains a "Investors (capital raise)" section — including "don't conflate indicated/committed/funded" and "money IN, not opportunities of type investment (money OUT)". `search_tasks` gains investor filter.
- **Investors are in the RAG index.** Same migration: `chunks.investor_id` + relaxed `chunks_source_check` + `match_chunks` recreated (identical args, +investor_id in return — `match-chunks.ts` unchanged). New `embedInvestorSnapshot` in embeddings.ts (one delete-and-replace snapshot: relationship fields + every investment w/ amounts+terms + last 5 contact-log notes; source_type `investor`; warn+skip pre-migration) — refreshed on investor create/update/PATCH, note add, investment POST/PATCH/DELETE. Citations labeled `Investor: <name>` in /intel synthesize + agent search_knowledge_base.
- **Steering wiring:** `computeAttention` gains category `investor_followup` (next_step_date overdue → urgency 55+2/day; hot or soft_committed/committed investors with no contact ≥21d → "going cold"; summary count added; tolerant select). Dashboard **Needs Attention gains an Investor Follow-ups section** (overdue next steps, links to /investors/[id]). **Daily brief gains a Capital Raise section**: prompt structure + rule, and a data block with totals (indicated/committed/funded across active investors), overdue/due-in-7d next steps, hot-going-cold flags.
- **Verified live end-to-end** (repo lib code via alias-loader against the Studio stack): task↔investor tag round-trip; `embedInvestorSnapshot` → 1 chunk (source_type investor, amounts serialized); `embedQuery`+`match_chunks` ranks the investor chunk **#1** for "which family office investors are interested in preferred equity…"; investor delete cascades chunks. Migration applied via ssh+psql, supabase-rest restarted. `tsc` + eslint (3 survivors = documented pre-existing classes) + full build clean; deployed.

**Done 2026-07-10 (investors module — capital raise tracking; DEPLOYED):**
- **New top-level area `/investors`** (sidebar primary group, mobile More, ⌘K, header titles; **admin-only by default** — `/investors` + `/api/investors` + `/api/investments` deliberately NOT in `permissions.ts` allowlists). Tracks the raise around the parent company AND per-project SPVs. Two linked concepts, not one: the **investor relationship** (`investors` — pipeline stage identified→…→funded (+passed/dormant), interest level hot/warm/cool/cold, investor type, typical check range, preferred structures + sector interests (text[], vocab from app constants), source/referred-by, relationship owner FK→team_members, next step + date, last contact, background notes) and the **investment/commitment** (`investments` — investor × target where target_kind = company|project (check-constrained: project requires project_id), per-deal stage discussing→…→funded, instrument, the honest money triple **indicated / committed / funded**, equity % / profit share % / pref return %, terms notes, key dates, optional `spv_entity_id` → `entities` for when the SPV is legally formed — raise trackable before any SPV exists). Plus `investor_notes` contact log (adding a note bumps `last_contact_date`).
- **Identity consolidates into `parties`:** investor creation find-or-creates a directory contact by exact name (tagged `investor`, `is_organization` from type) or links an explicitly picked one (typeahead over contacts in the form; mousedown-not-click); detail page links to `/contacts/[id]`. `investors.name` stays denormalized so the record stands alone.
- **Surfaces:** `/investors` list (card grid + stage/type/interest/target-raise filters + **raise rollup band**: indicated / committed (split parent vs projects) / funded / investor count — recomputed over the filtered set), `/investors/[id]` detail (pipeline bar, money band, key facts, inline add/edit investments via `InvestmentsSection`, contact log), new/edit forms (server actions in `app/investors/actions.ts`), and an **Investors section on the project Financing tab** (committed/funded per investor, links back; hidden pre-migration — embed query tolerates the table missing). Follows the opportunities idiom throughout (plain-text enums + `src/lib/utils/investors.ts` constants, notes feed, status control, ConfirmDialog delete).
- **API:** `api/investors/[id]` (PATCH stage/interest/next-step + DELETE), `api/investors/[id]/notes` (POST), `api/investments` (POST), `api/investments/[id]` (PATCH partial — only keys sent are applied, target kind+project travel as a pair; DELETE). Shared validation in `src/lib/investors/parse.ts` (route files can't export helpers).
- **Migration `20260710000001_investors.sql` APPLIED to the Studio DB** (psql via ssh + docker exec; API-role grants verified present via default privileges; supabase-rest restarted). `database.ts` hand-extended (the practiced pattern); `Investor`/`Investment`/`InvestorNote` aliases added. Deliberately NOT attached: `log_activity()` (same project_id-dereference reason as opportunities). Deliberately deferred: task tagging to investors, RAG embedding of investor data, per-investor document attachments (data room), `/api/search` integration. Waterfall math stays out — terms fields are tracking, the SPV operating agreements govern.
- `tsc` + eslint + full `next build` clean; deployed to the Studio.

**Done 2026-07-09 (map presentation calibration — for the master-plan audience; DEPLOYED):**
- Richard's calibration: most projects run **$100M–a few $B**; only the massive rail program reaches ~$100B (and it reads as a corridor line). Marker size tiers recut to spread across that band: sm <$100M, md $100M–$1B, lg $1B–$10B, **xl ≥$10B** (size-13 puck + pulsing sector-colored halo via `animate-ping [animation-duration:3s]` + always-visible name/value label — the mega-tier never hides). `markerVariant` already stamps tier, so MapView's diff rebuilds on value changes; verified `size-13`/`animate-ping` emitted in built CSS (Tailwind v4 dynamic spacing works).
- **MapLegend** (`src/components/map/MapLegend.tsx`, bottom-right above attribution, hidden while placing/drawing, `hidden sm:block`): decodes solid=Awarded/underway vs outlined=Pipeline/bidding + "sized by value" dots (tier thresholds in the title tooltip). Built for present mode — audiences can't read the marker language otherwise.
- **Clusters carry the money:** puck sizes by combined member value (≥$10B → size-12, ≥$1B → size-11) not just count, and a persistent value pill under the puck shows the total — a cluster can no longer silently swallow a big program.
- **Toolbar stats split by phase:** "N projects · $X underway · $Y in pursuit · $Z weighted" (was one lumped pipeline number) — the headline master-plan statement without touching the filter.
- Reduced-motion degrades the halo to a faint static tint (globals.css collapses animations). `tsc` + eslint + full build clean; deployed to the Studio.

**Done 2026-07-09 (auto-place from location — offline gazetteer; DEPLOYED):**
- **Projects can now be placed on /map from their free-text `location`** — no geocoding API, nothing leaves the box. A bundled US Census 2024 gazetteer (`src/lib/map/gazetteer.json`, ~2MB committed: 32k city + 34k ZIP + 52 state centroids; regenerate with `scripts/build-gazetteer.mjs`, needed ~never) backs `src/lib/map/geocode.ts`: ZIP wins > "city, ST" (comma or trailing-state forms, Saint→St alias, street-address prefixes ignored via comma segmentation) > bare city (unique match; ties prefer UT, then largest) > state centroid. Deliberately city-center precision, not street-level — indistinguishable at portfolio zoom; Reposition refines.
- **`POST /api/map/geocode`** (batch, max 500; admin-only via middleware — /api/map not in ROLE_API_PREFIXES; the dataset stays server-side, never in client bundles). Placement Panel: per-row wand button + **bulk "Auto-place N from locations"**. Bulk applies **city/ZIP matches only** (a state centroid is worse than staying on the unplaced list), PATCHes in batches of 5 with optimistic overrides + revert-on-failure, one summary toast. Single auto-place saves, selects (camera flies in), and toasts what matched — a state-precision match gets an explicit "use Reposition" warning. No migration — reuses `latitude`/`longitude`.
- **Verified:** parser tested against 20 realistic location strings (addresses, ZIPs, no-state, county/AFB fallbacks, TBD/Nationwide → no match); `tsc` + eslint + full build clean; deployed to the Studio (health 200; live tailnet check: geocode 401 unauthed, /map 307).
- **Found while testing (pre-existing, NOT fixed): `next dev` (Turbopack) does not load the root `middleware.ts` at all** — every page/API is unauthenticated in local dev (`/dashboard` served 200 unauthed; routes with their own auth like tasks' `getViewer` still hold). Production `next build`/`next start` picks it up fine ("ƒ Proxy (Middleware)") — the deployed Studio is unaffected. Likely fix when it matters: move `middleware.ts` into `src/` (Next 16 expects it beside `src/app`); verify prod again after.
- Opportunities-on-the-map deliberately deferred (many aren't geographic; separate pass if presenting ever demands it).

**Done 2026-07-09 (map completion pass — the full idea list is DONE and DEPLOYED):**
- **Marker clustering:** screen-space greedy grouping (48px radius) below zoom 14 — overlapping markers merge into a count puck (sector-colored when all one sector, slate otherwise; sized by count), click fits the cluster's bounds. Recomputed on `moveend` + data/selection changes in `MapView.refreshClusters`; hidden members get `display:none` (markers aren't destroyed). The **selected project never clusters**, so tour/selection highlights survive; above zoom 14 nothing clusters (same-address escape hatch).
- **Awarded vs Pipeline:** `projectPhase(stage)` in `src/lib/map/constants.ts` (award/mobilization/execution/closeout = awarded; pursuit/capture/bid = pipeline). Toolbar segmented filter (All | Awarded | Pipeline) composes with sector chips; stats + tour follow. **Markers distinguish phase visually: awarded = solid sector puck, pipeline = outlined** (bg-card, sector ring + glyph via `SECTOR_OUTLINE`).
- **Value-scaled markers:** three tiers — <$20M small (size-7), $20M–$250M / unknown mid (size-9), ≥$250M large (size-11). Tier+phase stamped as `data-variant` (`markerVariant()`) so MapView's diff rebuilds stale elements.
- **Deep links:** `/map?project=<id>` (server page reads searchParams → `initialProjectId`; client validates + selects on mount, camera flies in). Project detail header (`projects/[id]/layout.tsx`) gains an admin-only **"View on map"** chip when the project has coords or a route.
- **On-map search:** `MapSearch.tsx` in the toolbar — type-ahead over **visible** (filtered) projects by name/location/client, ↑/↓/Enter or click flies to the pick. mousedown-not-click on rows (beats input blur).
- **Present-mode route animation:** ant-path dash-flow on rail corridors (`project-lines-dash` white overlay layer; 14-frame `DASH_SEQUENCE` cycled ~80ms via rAF — maplibre can't interpolate dasharray). Present-mode only, honors `prefers-reduced-motion`, visibility restored across style reloads via `animateRef` in `ensureOverlays`.
- **Photo lightbox:** sheet photos click to a portaled fullscreen viewer (arrows/Esc/counter; `createPortal` to body because SheetContent's transform would trap `position:fixed`). Esc/outside-click close only the lightbox (guarded in Sheet `onOpenChange`); the tour's key handler pauses while it's open (`onLightboxChange` → `photoLightbox` gate) and skips INPUT/TEXTAREA targets so the search box works in present mode.
- All verified: `tsc` + eslint + full build clean; deployed to the Studio (health 200; `/map` 307 unauthed, tile route 401 unauthed — both correct). **The map idea list is fully shipped**; remaining map work is Richard's data entry (place projects, draw corridors).

**Done 2026-07-09 (map presentation layer — tour, labels, stats; DEPLOYED):**
- **Present-mode portfolio tour:** in Present mode, ←/→ (also PageUp/PageDown, or the on-screen ‹ › bar bottom-center) step through placed projects **biggest value first**, flying to each and opening its sheet — the "replaces the slide deck" flow. Keyboard listener is capture-phase so the maplibre canvas doesn't also pan; wraps around; respects sector filters (tour = `visible`). Placement panel now hidden while presenting.
- **Marker labels:** styled name+value label above each puck — on hover always (with `hover:z-10` so it isn't buried under siblings), persistent for ALL markers past zoom 9. Mechanism: MapView toggles `data-map-labels` on the container (named group `maplabels`); markers use `group-data-[map-labels=true]/maplabels:opacity-100` (verified emitted in built CSS — Tailwind does scan the JS template strings in markers.tsx). Project names are HTML-escaped (`escapeHtml`) — they go through innerHTML.
- **Live stats readout** replacing the "N on map" chip: visible count · total pipeline · weighted value (reuses `formatValue`/`weightedValue`), updates with sector filters — filter to a sector and present "just our government work, $X".
- ~~Remaining from the agreed map idea list~~ — **all shipped later the same day** (see the completion pass above). Deliberately skipped: 3D terrain, heatmaps, time sliders.
- Housekeeping: deleted stale `.next/types/* 2.ts` Finder-duplicate files that broke `tsc` (build artifacts, regenerate). `tsc` + eslint + full build clean; committed, pushed, deployed to the Studio (health 200, /map 307s unauthed over tailnet).

**Done 2026-07-09 (interactive project map — /map):**
- **New top-level area `/map`** (primary nav, mobile More, ⌘K "Project Map", header title): a presentation-grade interactive map of the whole portfolio — Richard's alternative to slide decks. Opens on Utah (US-wide basemap underneath for the nationwide future), illustrated sector-tinted markers per project type (`map_icon`: data_center/rail/hospital/housing/power/industrial/government/office/water/default, sector-derived fallback), click → eased flyTo + right slide-over detail sheet (photos from `media`, sector/stage/status chips, value + weighted value, client/location/delivery, description, key dates, "Open project" link). Rail corridors render as sector-colored glowing lines (`map_geometry` GeoJSON LineString), clickable like markers. Sector legend chips filter; Present mode = chrome-hiding fullscreen overlay for stakeholder demos. **Admin-only by default** (deliberately not in `permissions.ts` allowlists; a comment there explains how to open it to PMs later).
- **Fully offline basemap — zero external requests at view time.** MapLibre GL JS + Protomaps `.pmtiles` served by new `GET /api/map/tiles` (fs byte-range streaming; 206/416 semantics unit-tested incl. pmtiles magic bytes; missing archive → 503 with a setup hint, surfaced in the map UI). Archive at `~/berwilson-data/maps/us.pmtiles` **outside the app dir** (survives the deploy rsync `--delete`); override `MAP_PMTILES_PATH`. **The Studio holds the full-depth continental-US extract (17GB, zoom 0-15, extracted on the Studio itself 2026-07-09 — go-pmtiles binary at `~/.local/bin/pmtiles` there); the MacBook keeps a small Utah extract (275MB) for local dev** (`scripts/setup-map-data.sh`, `SCOPE=utah|conus`). The deploy script pushes the archive **only if the Studio has none** and never overwrites — basemap upgrades are a manual re-extract on the Studio (`deploy/README.md` has the command). Style built in code (`src/lib/map/style.ts`, `@protomaps/basemaps` layers, light/dark flavors — follows the app's dark-mode toggle live via a MutationObserver on `<html>`); fonts/sprites vendored to `public/basemaps/` (gitignored; the deploy rsync ships the working tree). New deps: `maplibre-gl`, `pmtiles`, `@protomaps/basemaps` (rendering libs only — §11 intact); maplibre is SSR-firewalled behind `next/dynamic ssr:false` and stays in the /map chunk.
- **Placement is hand-done on the map** (no geocoding): migration `20260709000001_project_map.sql` (**APPLIED to the Studio DB**, PostgREST restarted — it caches the schema) adds `projects.latitude/longitude` (range-checked) + `map_icon` + `map_geometry`; `database.ts` hand-extended. PlacementPanel (admin) lists unplaced projects with free-text `location` as the hint → click the map to place ("N of X placed" progress); the sheet has Reposition / Draw route (click vertices, Enter or double-click finishes, Esc cancels) / Remove route / marker-style picker. Writes reuse `PATCH /api/projects/[id]` per-field pattern (map fields admin-gated; LineString validated + 100KB cap; returning columns only include map fields when touched, so existing callers are unchanged). Optimistic UI + `router.refresh()`.
- **Components:** `src/components/map/` (MapPageClient orchestrator, MapView raw-maplibre wrapper — no react-map-gl, ProjectMapSheet, PlacementPanel, markers.tsx illustrated SVG pins on the SECTOR_BADGE hue families) + `src/lib/map/` (constants, types, style). Server page clones the projects-page pattern (admin-client fetch → client props; media public URLs computed server-side).
- **gen-types gotcha (post-cutover):** `npm run gen-types` still targets the paused cloud project (`--linked`), and `--db-url` needs a local Docker daemon the MacBook doesn't have. Workaround: run `npx supabase gen types --db-url postgresql://postgres:<pw>@localhost:5432/postgres` **on the Studio** (Colima) — but that output drops the `storage`/`graphql_public` blocks the committed file carries, so `database.ts` was hand-extended instead (the practiced pattern). Fix the script when it next matters.
- **DEPLOYED + verified end-to-end on production:** tile route 13/13 range-semantics checks; magic-link login → `/map` 200 authed / 307 unauthed **over the tailnet URL**; tiles 206 with PMTiles magic bytes from the full-US archive; PATCH place/revert, validation rejects (bad lat/icon/geometry), and LineString round-trip confirmed against the Studio DB; both style flavors pass the maplibre style-spec validator and every referenced font is vendored. `tsc` + eslint + full `next build` clean. **Remaining for Richard: place the ~97 projects** (Place Projects panel, bottom-left of /map — each row shows the free-text location as a hint) and draw the rail corridors (Draw route in a project's sheet).

**Done 2026-07-07 (CUTOVER COMPLETE — the platform is fully local; steps 3+5 done):**
- **Final migration executed:** fresh cloud→local dump/restore + 56/56 file re-sync; **every one of the 38 public tables verified count-identical to cloud**, auth (users/identities/sessions) and storage timestamps match exactly. Two migration subtleties handled: auth/storage truncate+restore must run as `supabase_admin` (not `postgres`), and `TRUNCATE auth.users CASCADE` reaches `public.media` via FK (repaired from a targeted dump; it was the only affected table — verified).
- **Studio app now points at local Supabase** (`https://richards-mac-studio.tail0e5306.ts.net:8443` via a second `tailscale serve` listener → kong :8000); rebuilt (NEXT_PUBLIC_* are build-time) + service restarted, HTTP 200. MacBook `.env.local` flipped too — local dev also targets the Studio stack.
- **Step 3 (re-embed) done:** chunks wiped and regenerated with the local embedding model — **213 chunks** (was 124: all 46 updates now embedded vs 3 historically, 14/16 documents (2 had no extractable text), opportunity snapshot + note). Gotchas hit: PostgREST schema cache goes stale after `DROP SCHEMA public` (restart `supabase-rest` after restores) and documents' summary column is `ai_summary`. Tooling preserved: `deploy/reembed.mjs` + `deploy/alias-loader.mjs` (runs repo lib code standalone: `node --experimental-strip-types --import ...register(alias-loader)... --env-file=.env.local <script>`).
- **Verified fully local end-to-end:** `embedQuery`→`match_chunks` returns real content; full `runAgent` loop (tool call → grounded answer, 6.9s) against the local stack; cron route auth OK (tomorrow 6:30am generates the first fully-local brief).
- **Step 5 (decommission) done:** cloud Supabase project **paused** via Management API (data retained — the rollback safety net; restore from the Supabase dashboard if ever needed), **Vercel project `berwilson-platform` deleted** (deployments + env vars gone; all values live on the Studio + MacBook env files). **`git push` no longer deploys** — it's GitHub backup only; deploying = `zsh deploy/deploy-to-studio.sh`.
- **Manual items for Richard:** (1) revoke the Gemini API key in Google AI Studio — zero Gemini traffic remains (or keep it and set `LOCAL_ALLOW_WEB_RESEARCH=true` if vendor-enrichment web research is wanted); (2) when reconnecting Microsoft 365 from the new origin, add `https://richards-mac-studio.tail0e5306.ts.net/api/email/oauth/callback` to the Azure app's redirect URIs (existing Graph tokens migrated and keep refreshing server-side); (3) Eric's devices need Tailscale to reach the platform; (4) offsite backup target still pending (enable SSH on the Mac mini).

**Done 2026-07-08 (documents open/view/download again — post-cutover storage repair):**
- **Every document download in the app had been broken since cutover — two stacked causes, both fixed.** (1) All three document surfaces (projects DocumentsTab, VendorDocuments, OpportunityDocuments) created signed URLs **browser-side with the anon key**; the self-hosted storage has **zero storage RLS policies** (they don't survive a data-only dump/restore), so every request died with "Object not found". (2) Even server-side signing would have 500'd: the final cutover restored `storage.objects` metadata from cloud (cloud version UUIDs) *after* the file re-sync had written disk files under locally-generated version UUIDs — all 56 migrated objects pointed at nonexistent disk paths (`ENOENT` on `/var/lib/storage/stub/stub/<bucket>/<name>/<version>`).
- **Code fix: signed URLs are now minted server-side** (admin client, behind auth + viewer role checks — consistent with §8's "middleware is the boundary"): new `GET /api/documents/[id]` and `GET /api/opportunities/documents/[id]` return `{url}` (300s expiry; `?download=1` adds attachment disposition). Shared client helpers in `src/lib/utils/document-links.ts` (`viewDocument`/`downloadDocument`; popup-blocker-safe pre-open). **Filenames are now clickable → PDFs/images/text open inline in a new tab; non-viewable types (docx…) fall back to download.** The Download icon forces a download. **Rule: never use browser-side `createSignedUrl` against this stack — it cannot work.**
- **Data fix on the Studio (one-time):** `storage.objects.version` realigned to the actual on-disk version for all 56 mismatched objects (deterministic — every object dir held exactly one file; audited 57/57 across documents/media/avatars, zero missing/orphans). Verified live: inline view + forced download + public media + a fresh same-day upload all HTTP 200. **RESTORE GOTCHA for the runbook: after any `storage.objects` restore, metadata `version` must match the on-disk file name — re-sync files AFTER restoring metadata, or realign versions like this pass did.**
- AI summaries were already stored/rendered per document (`ai_summary` under each row) — unchanged. `tsc` + eslint clean; deployed to the Studio and verified (new route 401s unauthenticated, health check 200).

**Done 2026-07-07 (security hardening pass — post-cutover audit):**
- **Live audit of the Studio's posture.** Confirmed already-good: FileVault ON, application firewall ON, no auto-login, no public Tailscale Funnel, no default Supabase creds, dashboard password strong (24 char), all secret files `600`, only 3 real users.
- **CRITICAL FIX — self-signup closed.** Self-hosted Supabase had `DISABLE_SIGNUP=false` + `ENABLE_EMAIL_AUTOCONFIRM=true`, and every RLS policy grants full access to any `authenticated` role (`auth.role() = 'authenticated'`, `WITH CHECK true`). Combined, anyone reaching the tailnet-exposed Supabase API on `:8443` could self-register (auto-confirmed) and read/write the **entire** database, bypassing the Next.js middleware (which only guards the app UI, not the API front door). Fixed: `GOTRUE_DISABLE_SIGNUP=true` in `~/supabase-selfhost/docker/.env` (backup `.env.bak-presec-20260707`) + recreated the `supabase-auth` container. Verified signup now returns `signup_disabled`; existing logins unaffected. **New users are added by admin invite only.**
- **Offsite encrypted backup wired (item awaiting one manual step).** `~/supabase-selfhost/backup.sh` (backup `.bak-presec-20260707`) now, after the local `pg_dumpall` + storage tarball, `age`-encrypts both artifacts and pushes them to the Mac mini (`richardwhite@100.74.2.126:Backups/berwilson-offsite`, 14-day retention). **The age PRIVATE key lives ONLY on Richard's MacBook** (`~/.config/age/berwilson-offsite-backup.key`, mode 600) — never on the Studio — so a stolen Studio or mini can't decrypt. Public recipient `age1q85gt646wmguldvruj8xfq25vmc0asj7js4xn67095ykqd9mzqsq4j7zc2` is embedded in the script. `age` installed on both machines (Studio: no-sudo binary in `~/.local/bin`; MacBook: brew). The offsite block is **non-fatal** — a mini outage or missing key just logs `WARN`, local backup always completes (verified: local ok, offsite warned gracefully, no stray `.age`).
- **Tailscale ACL policy drafted** at `deploy/security/tailscale-acl.hujson` (version-controlled copy; the live policy lives in the Tailscale admin console). Currently EVERY tailnet device can reach EVERY Studio port (5432 Postgres, 1234 LM Studio [no auth], 5900 Screen Sharing, 22 SSH, 8000/8443 Supabase). The policy keeps Richard's devices (`group:admins`) full-access and scopes any future non-admin device (Eric, PMs) to only 443 + 8443. Studio left user-owned (untagged) so the backup push is already permitted by the admin rule. Includes an ACL `tests` block that guarantees it can't lock Richard out.
- **Deferred by Richard:** SSH key-only auth (disable `PasswordAuthentication`) — low value while tailnet-only; not applied.
- **Two manual steps remain for Richard:** (1) paste `deploy/security/tailscale-acl.hujson` into https://login.tailscale.com/admin/acls; (2) authorize the Studio's SSH key on the mini so the offsite push works — `ssh -t richardwhite@100.86.79.4 'ssh-copy-id richardwhite@100.74.2.126'` (enter the mini's password once); and **store the age private key in his password manager** (without it the offsite backups are unrecoverable).

**Done 2026-07-07 (step 4 groundwork: self-hosted Supabase live on the Studio, trial migration verified):**
- **A full self-hosted Supabase stack runs on the Mac Studio** under Colima (no-sudo container runtime at `~/.local/bin`; VM: 2 CPU / 3GB / 30GB, autostarts at login via `com.berwilson.colima`). Official compose trimmed to 8 services (`~/supabase-selfhost/docker/docker-compose.lean.yml`) — realtime/edge-functions/supavisor dropped after verifying zero app usage; Postgres 17.6 exactly matches cloud; fresh secrets + self-signed ANON/SERVICE_ROLE JWTs in `docker/.env`. Stack idles at ~1.1GB of the 3GB VM.
- **Trial migration passed end-to-end:** cloud → local via pg_dump inside the db container (public schema+data; auth+storage data). All counts match (3 users, 97 projects, 20 tasks, 124 chunks, 56/56 storage files synced with a supabase-js script). Explicit API-role grants applied post-restore. Verified live: GoTrue health, PostgREST returning real rows, buckets listing, `match_chunks` vector search returning real chunks. The 3 restore errors were all empty tables (verified in cloud) — zero data loss.
- **Cloud DB password was reset** via the Management API (needed for pg_dump; nobody had it stored; app unaffected — it authenticates with API keys). Stored at `~/supabase-selfhost/.cloud-db-password` on the Studio.
- **Backups live:** nightly 2:30am `pg_dumpall` + storage tarball → `~/Backups/berwilson/`, 14-day retention (`com.berwilson.backup`); first run verified (1MB db + 141MB storage). **Offsite copy pending** — needs SSH on the Mac mini or another target; do before cutover.
- **NOT cut over:** both apps (Vercel + Studio) still use cloud Supabase. Cutover runbook is in `deploy/README.md` (freeze → fresh dump/restore → point Studio app at local via a second `tailscale serve` listener on :8443 → step-3 re-embed → verify → decommission). Awaiting Richard's go after he's driven the Studio instance.

**Done 2026-07-07 (step 1 of the full-local migration: app self-hosted on the Mac Studio):**
- **The platform now runs on the Mac Studio, tailnet-only: `https://richards-mac-studio.tail0e5306.ts.net/`** (via `tailscale serve` → localhost:3000; also `http://100.86.79.4:3000` inside the tailnet). Vercel keeps running in parallel until cutover (step 5). Deployed and verified: HTTP 200 on /login over tailnet HTTPS, middleware auth redirect working, `com.berwilson.platform` launchd service running, both cron agents registered (daily-brief 6:30am local, risk-scores 1:00am local — these hit localhost with the Studio's own CRON_SECRET; Vercel's crons still run too, so the brief may generate twice until cutover).
- **Deploy kit: `deploy/`** — `deploy-to-studio.sh` (idempotent, run from the MacBook: rsync source → generate Studio `.env.local` from the MacBook one with LM Studio flipped to localhost → npm ci + build → install 3 launchd plists → tailscale serve → health check), plist templates, README with prerequisites + caveats (Microsoft OAuth redirect URI must be added in Azure before connecting the mailbox from the Studio origin).
- **Studio box facts:** SSH enabled (MacBook key authorized), **Node v26 installed no-sudo at `~/.node/bin`** (no Homebrew on the box; plists get the path via `__NODE_BIN__`), LM Studio serving on 1234. **A stale public Tailscale Funnel (n8n-era) was found active on the Studio and got removed** when serve was enabled — the box is no longer internet-exposed. Stray `~/package.json`+lockfile (nodemailer/vercel, June 22) trigger a harmless Next workspace-root warning at build; left in place pending Richard's OK to delete.
- **Studio env = local AI active** (`AI_PROVIDER=local`, LM Studio on localhost, `EMBEDDINGS_PROVIDER=gemini` until step 3). Remaining manual settings on the Studio: Energy → prevent sleep, auto-login (LaunchAgents need a logged-in session after reboot), LM Studio launch-at-login.
- **Remaining steps:** (3) re-embed all chunks locally — do this AT cutover, not before (prod Vercel still embeds queries with Gemini; re-embedding early breaks prod search), (4) self-host Supabase on the Studio + backup regime, (5) decommission Vercel + revoke Gemini key.

**Done 2026-07-06 (local AI provider — step 2 of the full-local migration):**
- **Decision (Richard): move the entire platform off frontier models and cloud hosting onto his own hardware for absolute security.** Agreed 5-step plan: (1) self-host the Next.js app on the Mac Studio behind Tailscale (launchd + `tailscale serve`, no public exposure; crons become launchd jobs hitting the routes with CRON_SECRET), (2) swap runtime AI to local Qwen via LM Studio — **DONE, flag-gated**, (3) re-embed the entire chunks table with the local embedding model, (4) migrate Supabase to self-hosted (official Docker stack on the Studio; sanctioned exception to the no-Docker rule; nightly encrypted pg_dump + storage sync to a second machine REQUIRED before cutover), (5) decommission Vercel + revoke the Gemini key. Steps 1/3/4/5 remain.
- **New `src/lib/ai/local.ts`** — OpenAI-compatible client for LM Studio (plain `fetch`, no SDK; §11 single-provider rule intact): `localChat`, `localChatStream` (SSE parsing + tool-call delta accumulation), `localEmbedding` (truncate >768-dim vectors to 768 + L2 renormalize — MRL — so the pgvector schema is untouched), `extractPdfText` (via new dep `unpdf`), and a stateful `<think>`-tag stream filter (Qwen thinking variants) — filter unit-tested incl. tags split across chunks.
- **Branch points, all gated on `AI_PROVIDER=local` (unset = byte-identical Gemini behavior):** `callGemini`/`callGeminiWithFile` (PDF → local text extraction + text call; image → OpenAI vision content, needs a VL model loaded), `generateEmbedding`, `runAgent` (full agentic tool loop rebuilt in OpenAI format — `agentTools` were already plain JSON Schema; Gemini-format history converted; streaming preserved), `transcribePdfText` (local mode = direct extraction, no model pass), `researchQuery` (**blocked in local mode** with a clear error unless `LOCAL_ALLOW_WEB_RESEARCH=true` — web research/enrichment is inherently external; only the search query would leave). Health page requires `LOCAL_AI_BASE_URL` instead of `GEMINI_API_KEY` when local. `ai_queries` logging kept on all paths (refactored into shared `logAiQuery`).
- **Env (documented in `.env.local`, commented out):** `AI_PROVIDER=local`, `LOCAL_AI_BASE_URL=http://100.86.79.4:1234/v1`, `LOCAL_AI_MODEL` (must match LM Studio's model identifier), `LOCAL_EMBEDDING_MODEL`, `EMBEDDINGS_PROVIDER` (overrides; defaults to AI_PROVIDER — **CRITICAL: query embeddings must match stored chunk embeddings; flipping to local without the step-3 re-embed silently breaks retrieval. Staged cutover: set `EMBEDDINGS_PROVIDER=gemini` until the re-embed runs**), `LOCAL_ALLOW_WEB_RESEARCH`.
- **LIVE-TESTED 2026-07-07 — works.** The Studio came online; actual model is **`qwen/qwen3.6-35b-a3b`** (Qwen 3.6 35B A3B, a hybrid-reasoning MoE) with `text-embedding-qwen3-embedding-0.6b` alongside (36GB fits both). `.env.local` is configured live: `AI_PROVIDER=local` + `EMBEDDINGS_PROVIDER=gemini` (until the step-3 re-embed). Verified end-to-end from lib-level scripts against the real DB: (1) `runAgent` full loop — company context injected, `list_projects` tool call executed, grounded streamed answer, 12.7s, ~12k tokens in; (2) real extraction prompt — correct assignees/risks/confidence, valid JSON, **~48s** (the model spends ~3k reasoning tokens; generation itself is ~75 tok/s); (3) embeddings 768-dim unit-norm; (4) `ai_queries` logging captured it all with the local model id. **Latency expectation: flash-equivalent tasks run ~30-60s locally vs ~5-15s on Gemini — that's the price of local.** Tested `/no_think` (Qwen soft switch): no meaningful token/latency reduction on real extraction — deliberately NOT adding a knob (no demonstrated benefit). LM Studio separates reasoning into `reasoning_content`, so the `<think>` filter is defense-in-depth only.
- **`.env.local` now also holds the Supabase URL/anon/service-role keys** (pulled via `supabase projects api-keys` — Vercel marks them Sensitive so `vercel env pull` leaves them empty). Local `npm run dev` is fully functional for the first time; Richard can log in and drive the app in local-AI mode in the browser.
- `tsc` + eslint + full `next build` clean. PDF extraction verified with a real file; think-filter unit tests pass.

**Done 2026-07-06 (directory pass: contact tags, vendor wipe, mass delete):**
- **Vendor data wiped in prod (Richard's call):** 150 of 151 `entities` deleted (bulk auto-created 2026-05-17/18 by enrichment/intake — federal agencies, tribes, orgs — with 2,507 `entity_projects` links, all cascaded away). Kept the one manual entry ("Dino plumbing and HVAC", added 2026-07-04, zero links).
- **Contact tags:** `parties.tags text[]` + GIN index (migration `20260706000001_party_tags.sql`, **APPLIED**; gen-types run). Free-form, self-maintaining vocabulary — no tags table, no admin screen; mirrors `entities.specialties`. New shared `TagInput` (chip input, autocomplete from tags in use via `GET /api/parties/tags`, Enter creates; exact match reuses canonical casing). Wired into: ContactForm (create), contact detail (new "Company & Tags" card, saves per change), contact tiles (clickable tag chips → filter), a tag-filter dropdown with counts on the contacts toolbar, and `parties/[id]` PATCH (`tags` allowlisted + sanitized).
- **Contact ↔ vendor link editable:** new `CompanyLinkEditor` on contact detail + `PUT /api/parties/[id]/company` (link existing entity / find-or-create by name / unlink; keeps `parties.company` text and the `party_entities` primary link in sync). Create-time linking already existed in `contacts/actions.ts`.
- **Contacts search broadened:** now also matches tags, email, phone, and relationship notes (page ships `tags`+`relationship_notes` to the client; still client-side filtering — fine to several thousand contacts, move to server-side tsvector only if payload/latency ever hurts).
- **Mass delete from both tile views:** Select mode on ContactsClient + VendorsClient (toggle → tap tiles, Select all (filtered), Delete (N) via ConfirmDialog). `POST /api/parties/bulk-delete` (archives, same soft-delete semantics as single delete) + `POST /api/entities/bulk-delete` (hard delete, 409s if a child entity outside the batch blocks the parent FK); both use `actorAdminClient()`, both admin-only by default (not in any role allowlist). "Delete inactive contacts" = sort by Recently Active / filter, Select all, Delete.
- `tsc` + full `next build` clean; lint survivors on touched files are the documented pre-existing classes.

**Done 2026-07-05 (code review + cleanup pass):**
- **Security hardening: role resolution now fails closed.** Both `getViewer()` (`src/lib/auth/viewer.ts`) and the middleware defaulted to **admin on any DB error**, not just the missing-column pre-migration case. Now only PostgREST error `42703` (column doesn't exist = migration not applied) keeps the admin default; any other failure (transient DB error) resolves to `member`. Worst case is an admin briefly seeing the member view during a Supabase blip — never an escalation.
- **Real React bugs fixed:** `useSuggestion` was a plain function named like a hook in `QueryInput` + `ResearchPanel` (rules-of-hooks violation; made the React Compiler skip both components) — renamed `applySuggestion`. `DailyBrief` + `ProjectNarrativeBrief` referenced `generateBrief` from an effect above its declaration (blocked compiler memoization) — reordered; both also nested the refresh `<button>` inside the header `<button>` (invalid HTML, hydration errors) — headers restructured as a div with sibling buttons (same look/behavior). `TimelineView` called `Date.now()` inside `useMemo` — now captured once at mount via lazy `useState`.
- **Type-safety:** removed 12 stale `as any` casts (ProjectForm ×9, project detail, vendor detail ×2) — the generated types have covered those columns (capture fields, `entities.category`) since the last gen-types run. ~23 `any` escapes remain (§7 count updated).
- **Dead code swept:** unused `formatValue`/`formatPct`/`formatDate`/`startUploads`/`ScoreDisplay` helpers, ~25 unused imports/vars/params across 22 files, stale Perplexity migration notes in `research.ts` header. Also fixed: expression-as-statement in `MeetingPrepButton`, missing `onSuccess` dep in `CompanyProfileClient`'s cert form effect, unescaped apostrophe.
- **Reviewed clean (no changes needed):** cron auth (fails closed), middleware gating, `permissions.ts` allowlists, `admin.ts` actor stamping, rate limiter, `vercel.json` crons; no dangling references to removed modules. Lint 76 → 21 problems; every survivor is a documented accepted class (§9). `tsc` + full `next build` clean.

**Done 2026-07-03 (production-readiness pass: cron fixes, agent self-fetch refactor, system health):**
- **The daily brief never ran in production — three compounding failures, all fixed.** (1) `/api/cron/daily-brief` was never in `vercel.json`'s crons (only risk-scores was) — now scheduled at 12:30 UTC (6:30am SLC summer). (2) It wasn't on the middleware public allowlist, so even an external trigger bounced to the auth wall — added (it self-guards via CRON_SECRET). (3) `CRON_SECRET` was never set in Vercel, so risk-scores has been 401ing daily too — generated and set in all three envs. **Security fix:** both cron routes' auth was `authHeader !== 'Bearer ${undefined}'` when the secret was unset — a literal `Bearer undefined` header passed on a public route. Both now fail closed (`!process.env.CRON_SECRET || …`).
- **Agent self-fetch antipattern removed.** `draft_email`/`draft_agenda`/`draft_status_report` and `get_attention_items` fetched `NEXT_PUBLIC_SITE_URL ?? 'http://localhost:3000'` — broken in prod (env var never set; and cookie-less self-fetches would have hit the auth middleware anyway). The attention engine moved to `src/lib/attention.ts` (`computeAttention()`); draft generation moved to `src/lib/ai/draft.ts` (`generateDraft()`). Routes (`api/attention`, `api/ai/draft`) are thin wrappers keeping auth/rate-limit; agent tools call the libs directly. **Rule: agent tools never fetch the app's own HTTP routes — extract shared logic into `src/lib` instead.**
- **New `/settings/health` (admin-only, sidebar System group + mobile More):** live checks on every load — daily-brief + risk-scores cron freshness (fail >36h), AI pipeline (last `ai_queries` call + 24h count), Microsoft Graph token (connected mailbox, flags a missing `Mail.Read.Shared` scope → tells you to reconnect), failed email-research runs (7d), and required-env presence. Server component, direct admin-client queries, card idiom. This is the "background work fails silently" alarm surface.
- **Hygiene verified:** `npm run gen-types` run — generated `database.ts` byte-identical to the committed hand-extended file (user-access reconciliation done). Vercel env audit: retired vars already gone; `SYSTEM_USER_ID` no longer referenced in code. `tsc` + eslint + full `next build` clean.

**Done 2026-07-03 (polish pass: research-run visibility, fit persistence, pursuit profile):**
- **Email research runs are visible immediately.** `api/email-research/run` stages a `running` `email_intake_sessions` row up front (label = search term), so navigating away no longer means minutes of "nothing happened"; every failure path (and a new top-level catch) flips it to `failed` with the error stored in `extraction_result.error`; success updates the same row to `pending` (new `sessionId` input on `analyzeEmailReport` — update-in-place instead of insert). Status values `running`/`failed` are app-level only (plain-text column, no migration). UI: Recent list renders running rows (spinner, non-clickable) and failed rows (error text + dismiss ✕); `SessionsAutoRefresh` re-fetches every 15s while a run is live; the `[id]` review page guards running/failed sessions (auto-refreshing "still running" card / error card); stale `running` rows (>15 min, function cap is 5) display as failed. New `api/email-ingestion/sessions/[id]` PATCH = dismiss (blocked for confirmed); dismissed sessions are hidden from Recent.
- **Fit assessments persist.** `proposal_intake_sessions.fit_assessment jsonb` (migration `20260704000007`, APPLIED; gen-types run) — `api/proposals/intake` now stores what it returns. Email-intake sessions already stored theirs.
- **Backfill verified clean** in prod (0 missing across all four targets); the BackfillCard was already gone from `/intel`. The admin endpoint `api/admin/backfill-embeddings` is kept as a recovery tool.
- **Pursuit profile: derivable gaps filled in prod** (via `db query`): `website`, `hq_address` (South Jordan, UT — from the brief), draft NAICS (236220/332311/238120/237990 — confirm vs SAM.gov), **disqualifiers** (no CUI/CMMC-L2 work pre-GovCloud per §8; no set-aside bids until certs verified in hand; no deals shifting catastrophic risk onto host communities), **past_performance** (honest: no completed references yet — lists the White House Quantum Rail proposal, CQM/EM 385-1-1 posture, active UT pipeline; tells the assessor to treat past performance as unproven). **Deliberately still empty:** project size range (live pipeline spans sub-$1M municipal to $2B+ programs — a made-up floor/ceiling would distort pursue/pass), annual revenue, bonding. Delivery methods/vehicles remain assumed-unconfirmed. All remaining asks are listed inside `pursuit_notes` on `/company`.

**Done 2026-07-03 (executive design pass — dashboard):**
- **Design thesis: calm executive instrument, not consumer dashboard.** `HealthPanel` rebuilt as a 4-stat KPI band (Pipeline Value, Weighted Pipeline, Active Projects, Needs Attention w/ color-coded breakdown) — the synthetic 0–100 health score, gradient ring, animated count-up numbers, per-row progress bars, and stacked bar were all removed (real numbers only; component is now a server component). **One card language everywhere:** `rounded-xl border border-border bg-card elev-1`, panel headers = 11px small-caps muted label + tabular count (`ClosingSoon`, `NeedsAttention`, `RiskOverview`, `NowObjectives` aligned; `ProjectCard` uses standard `.lift`). Dead CSS idioms deleted from `globals.css`: `.glass-panel`, `.card-hover-glow`, `.animate-count-up`, `.chart-gradient-fill`. **Rule for new UI: use the Card idiom + small-caps panel header; no glassmorphism, no animated numbers, color only for status meaning.** Build clean, pushed.

**Done 2026-07-03 (readability pass):**
- **Type scale bumped at the token level** (`globals.css` `@theme`): `text-xs` 12→13px, `text-sm` 14→14.5px — the two workhorse sizes (~1,450 uses combined), so every screen got larger text from a 10-line change, zero per-file edits. `muted-foreground` darkened in light mode (0.505→0.46) / lightened in dark (0.708→0.75) for small-text contrast. Sidebar + mobile nav inactive items brightened (60%→75% opacity), sidebar icons 15→16px, group labels 10px→11px. "Intake Proposal" renamed "Proposal Intake" everywhere (sidebar, mobile More, ⌘K palette, header title, page + metadata). `tsc` + `next build` clean; pushed.

**Done 2026-07-03 (activity-log user attribution + email-intake copy fix):**
- **The activity log now shows WHO did it.** App writes go through the service-role client, so `auth.uid()` was null inside `log_activity()` and every entry logged as "system". Fix: `createAdminClient(actor?)` can stamp `x-actor-id`/`x-actor-email` headers onto the PostgREST request; new `actorAdminClient()` (`src/lib/auth/viewer.ts`, built on the request-cached `getViewer()`) resolves the signed-in user and returns a stamped client; migration `20260704000006_activity_actor_attribution.sql` (**APPLIED to prod**) teaches the trigger to fall back to those headers when `auth.uid()` is null. Swapped into every user-initiated mutation route on tracked tables (tasks create/patch/delete, projects delete + server actions, updates save, documents insert/update/delete, certification docs, review approve/reject, proposals intake/confirm/upload-chunk, email-ingestion confirm). Routes already on the session-scoped server client (compliance/dd-items/financing/milestones/stage/updates PUT) were always attributed — untouched. Crons/AI/background writes still log as "system", which is accurate. **Rule for new code: user-initiated mutations on tracked tables use `actorAdminClient()`, not `createAdminClient()`.** Verified end-to-end (trigger test in a rolled-back transaction + a PostgREST header-forwarding echo test). No schema change — gen-types not needed.
- **Email Research copy fixed:** the loading state said "Keep this tab open" — wrong. The run is a synchronous server function (`maxDuration` 300s) that completes even if the browser navigates away; the pending session is staged at the end and shows under Recent sessions for later review. Copy now says leaving is safe.

**Done 2026-07-03 (user management + role-based access — foundation):**
- **Four role presets** (`src/lib/auth/permissions.ts`): `admin` (everything), `executive` (Tasks + Objectives, full edit), `project_manager` (granted projects/opportunities + tasks within them; grants on a parent project cascade to children), `member` (own task list only). Anything not explicitly allowed for a role is admin-only by default — new routes are born private.
- **Schema** (migration `20260704000005_user_access.sql`): `team_members` gains `auth_user_id` (unique, links to Supabase Auth), `email`, `role` (existing Richard/Eric rows set to admin); new `access_grants` table (member × project|opportunity). `database.ts` hand-extended.
- **Enforcement, three layers:** (1) middleware resolves the signed-in user's role and gates page/API path prefixes (redirect to `/tasks` / JSON 403); (2) `src/lib/auth/viewer.ts` — `getViewer()` (request-cached), `canAccessProject` (walks parent chain), `canAccessOpportunity`, `filterTasksForViewer`, `canCreateTask` — wired into: tasks API (list filter, create/patch/delete guards, notes), team-members POST, projects list/detail-layout/new/actions/PATCH/stage/DELETE/parents, documents upload/insert/delete/signed-url, milestones POST/PATCH, opportunities list/detail/edit/actions/PATCH/notes/docs; (3) RLS unchanged — still defense-in-depth (§8); move to grant-keyed RLS when external partners get accounts.
- **Safety rules in `getViewer` (mirrored in middleware):** pre-migration (columns missing) → everyone admin (today's behavior, code ships first); linked → their role; bootstrap (no linked, active **admin** exists yet) → unlinked users are admin (fixed same day — the first version ended bootstrap on ANY link, so inviting a PM first demoted the unlinked executive logins to member mid-session); unlinked once an admin is linked → member. Last-active-admin demotion blocked server-side.
- **People-data rule (Richard's call):** executives = full edit on tasks/objectives; PMs get a **limited players list** on granted projects (name/role/company/email — no `/contacts` profile links, no add/edit/remove, no DD notes). Directory/contacts stay admin-only.
- **AI + cross-portfolio surfaces are admin-only** for now: Ask Ber AI dock, ⌘K palette/search, quick upload hidden for non-admins in the shell; `/api/ai/*`, `/intel`, dashboard, review, activity, email intake, proposals all outside non-admin allowlists. Per-grant RAG scoping is deliberate future work.
- **`/settings/users` (admin-only; sidebar System group + mobile More):** role legend, member cards (role dropdown, activate/deactivate, grant chips + editor for PMs), invite modal (new person or link an existing unlinked member; email invite via `admin.auth.admin.inviteUserByEmail` → existing `/auth/confirm` → set-password flow; already-registered emails just link instead of failing). API: `api/admin/users` (GET/POST, 409 pre-migration), `api/admin/users/[id]` (PATCH role/active/grants).
- **Known v1 limits:** PM mutations on updates/diligence/financing/players/parties are blocked by the API allowlist (read-only on their project pages); executives see project names as task tags (by design — they run the whole board). `tsc` + eslint + full `next build` clean. Local runtime smoke impossible (Supabase env vars are marked Sensitive in Vercel and pull empty; pre-existing situation).

> **DB push status (2026-07-03, user access): DONE.** Migration `20260704000005_user_access.sql` applied; `info@berwilson.com` → Richard and `tuaone@berwilson.com` → Eric linked as admins directly in prod (via `db query --linked`); the first PM was invited via `/settings/users` and correctly sees only their granted projects. Remaining: `npm run gen-types` to reconcile (hand-extended types match). Note: Dennis/Nancy rows show role=admin from the migration's blanket update but are unlinked (no login) — the invite flow overwrites role at invite time, so this is cosmetic.

**Done 2026-07-03 (chief-of-staff pass: objectives wired in, one Directory, task truth completed):**
- **Tasks can be tagged to an objective.** New nullable `tasks.objective_id` (FK → `objectives`, on delete set null) via migration `20260704000003_task_objective_tag.sql`, mirroring the opportunity-tag pattern exactly: dual-schema tolerant (tag written only when chosen; objective controls render only when objectives exist), `database.ts` hand-extended. Board add-form picker + filter + Target-icon chip; TaskDetailSheet picker + steering-board link card; API create/patch/filter (`?objective=`). `/tasks` fetches active objectives sorted Now → Soon → Possibly.
- **Objectives lead the morning read.** Dashboard opens with a `NowObjectives` strip (rank, owner initials, target date, open-task count from the new tag; links to `/objectives`). The daily-brief cron injects Now/Soon objectives into its prompt and gained a **Steering Check** section (urgent items lined up against stated priorities; calls out Now objectives with no movement). Both fail quietly pre-migration.
- **One Directory (nav 14 → 13).** `/contacts` is now "Directory" with **Contacts | Vendors & Contractors** tabs (`?tab=vendors`); the old `/vendors` list is a redirect stub (detail/new routes stay). Sidebar (item: "Contacts & Vendors", highlights on `/vendors/*` too), mobile More, ⌘K palette (both tabs reachable), header titles updated.
- **Extraction action items → real tasks (column dropped).** `api/updates/save` converts human-confirmed action items into `tasks` rows (assignee resolved by name against `team_members` via new `src/lib/tasks/from-action-items.ts`); review-queue approval converts legacy pending items then clears them (no-ops post-drop). Action-item display/editing removed from UpdatesTab/UpdateEditModal/ReviewEditModal + the PATCH toggle route; dead `AssigneeInput.tsx` deleted. Migration `20260704000004_drop_action_items.sql` drops `updates.action_items`; `database.ts` hand-trimmed.
- `tsc` + eslint clean on touched files (remaining errors are the documented pre-existing ones); full `next build` clean. Three commits pushed to main (auto-deployed).

> **DB push status (2026-07-03, chief-of-staff pass):** two new migrations to apply — `20260704000003_task_objective_tag.sql` (needs `20260704000002_objectives.sql` first) and `20260704000004_drop_action_items.sql` (independent, safe any time). After pushing all pending migrations: `npm run gen-types`. Pre-migration degradations are all graceful: objective tagging errors only if an objective is actually picked, the Dashboard strip/brief section stay hidden, and everything else works.

**Done 2026-07-03 (objectives steering board):**
- **New top-level area `/objectives`** — the chief-of-staff priority board: company objectives sorted into **Now / Soon / Possibly** columns, drag-and-drop to re-prioritize (order = priority; cards are rank-numbered), quick-add per column, click-to-edit (title, note, owner from `team_members`, target date), per-card menu (move up/down, move to bucket, archive, delete via ConfirmDialog). Archive is a collapsible section below the board — done/parked objectives keep their record. Native HTML5 DnD, no new deps; the card menu covers mobile where drag is impractical.
- **PDF export via a print view:** `/objectives/print` (chromeless — excluded from the app shell in `layout.tsx` the same way login is) renders a letterhead document (logo, "Strategic Objectives", prepared date, numbered sections); a screen-only toolbar picks Everything or a single bucket and triggers the browser print dialog (Save as PDF). No PDF library — `@react-pdf/renderer` stays uninstalled.
- **Schema:** one tiny table via migration `20260704000002_objectives.sql` — `objectives` (title, note, bucket text now|soon|possibly, sort_order, owner_id FK→team_members, target_date, status active|archived). Plain-text bucket/status + app constants (`src/lib/utils/objectives.ts`), `updated_at` trigger only (not `log_activity()`), RLS like opportunities. `database.ts` hand-extended; `Objective` alias in `lib/supabase/types.ts`.
- **API:** `api/objectives` (GET/POST — create appends to bucket bottom), `api/objectives/[id]` (PATCH whitelist / DELETE), `api/objectives/reorder` (POST — persists a drag result as full bucket re-numbering; board is tiny, per-row updates).
- **Nav 13 → 14:** sidebar primary group (after Dashboard, Target icon), mobile More, ⌘K palette, header titles.
- `tsc` + eslint clean on touched files; full `next build` clean.

> **DB push status (2026-07-03, objectives):** `20260704000002_objectives.sql` must be applied (Richard runs migrations), then `npm run gen-types`. Pre-migration, `/objectives` shows its error screen ("make sure the migration has been applied") — nothing else is affected.

**Done 2026-07-03 (simplification pass 2 — cut the off-mission modules):**
- **Equity & Valuation removed** (~6.4K lines): `src/app/equity`, `components/equity`, `lib/equity`, `api/equity`, equity hooks/store/types, nav + middleware + layout references. `formatCurrencyCompact` relocated to `src/lib/utils/format.ts` (TaskDetailSheet imports it from there). Four now-orphaned deps uninstalled: `@tanstack/react-query`, `zustand`, `@react-pdf/renderer`, `recharts`.
- **Portfolio site-hierarchy removed** (~5K lines): `src/app/portfolio`, `components/portfolio`, `api/portfolio`; the two site-scoped agent tools (`get_stakeholders`, `get_funding_sources`) + their prompt guidance; portfolio constants/enums/type aliases; `site_id` handling in `api/documents/upload`. **Kept:** `portfolio_briefs` (the daily whole-company brief), `get_portfolio_summary` (cross-project), risk scoring. Decision context: a CFO hire is coming — if finance tooling is needed, build a fresh section, don't resurrect this.
- **Speculative cuts:** background checks (widget + API + contact-page section), vendor scorecards & reviews (FederalScorecardSection, ReviewForm, API routes, review stats in the vendors list/detail), Procore stub, dead scraper-era Graph webhook/processed-email helpers in `microsoft-graph.ts`, outlook_web_link UI in UpdatesTab.
- **Nav 15 → 13** (Finance group gone). Sidebar/mobile/⌘K/header titles updated.
- **Opportunities gained `on_hold`** ("On Hold", amber) — parked deals no longer masquerade as active or get force-closed. Outside the pipeline bar (detail page shows an On Hold banner). Plain-text status, app constants only (`src/lib/utils/opportunities.ts`), no migration.
- **UI consistency:** new shared `ConfirmDialog` (`src/components/ui/confirm-dialog.tsx`); every `window.confirm()`/`window.alert()` replaced with it + sonner toasts. Dark mode now follows `prefers-color-scheme` when no explicit theme is stored (toggle still overrides).
- Migration **`20260704000001_simplification_drops.sql`** drops it all: equity ×2 tables, portfolio ×12 tables + 9 enums + site/component columns on documents/compliance_items/activity_log/chunks (site-scoped chunks deleted; `documents_scope_check` re-created without site refs), `federal_scorecards`, `entity_reviews`, parties `background_check_*` columns, `processed_emails`, `graph_subscriptions`, `trade_secrets`, `ts_exposure_items`, `document_distributions`, `updates.outlook_web_link`. All `IF EXISTS` — code shipped first, so it can land any time.
- `database.ts` hand-trimmed to match; `tsc` + full `next build` clean.

> **DB push status (2026-07-03, simplification pass 2):** `20260704000001_simplification_drops.sql` must be applied (Richard runs migrations), then `npm run gen-types`. Nothing breaks pre-migration — the dropped tables/columns simply sit unused. **Data in the dropped tables (equity scenarios, portfolio sites/stakeholders/funding, scorecards, reviews) is permanently deleted — confirmed OK 2026-07-03.**

**Done 2026-07-03 (simplification + ambient AI — "simplification is sophistication" pass):**
- **Task truth unified.** All "what needs me" surfaces now read the `tasks` table instead of legacy `updates.action_items` JSON: `/api/attention`, `/calendar`, project + portfolio briefs, drafts, meeting prep, the daily-brief cron (which also gained a "due in 7 days" section), and the agent's `get_open_items`. Shared helper: `src/lib/tasks/queries.ts` (`fetchOpenTasks` + prompt formatters, dual-schema tolerant, no embeds).
- **IA consolidated (19 → 15 nav destinations).** `/attention` → Dashboard (Needs Attention panel gained an Overdue Tasks section; sidebar red badge moved to Dashboard and now includes overdue tasks). `/capacity` → `/tasks` (per-person workload chips: open + overdue counts, tap to filter the board). `/email-research` + `/email-ingestion` → one **Email Intake** page at `/email-ingestion` (sweep form first, manual paste behind a disclosure, recent sessions below). Timeline left primary nav, linked from the Projects toolbar. All three old routes are `redirect()` stubs. Sidebar/mobile nav/⌘K palette/header titles all updated.
- **Agent streams.** `/api/ai/agent` gains SSE mode (`stream: true`): live tool-activity events ("Checking search knowledge base…") + token-by-token answer streaming; legacy JSON mode kept. `runAgent` now uses `generateContentStream` with optional `onToolCall`/`onTextDelta` callbacks; `maxDuration=300`.
- **Ambient Ask Ber AI.** New `AskBerAIDock` (global slide-over, mounted in the shell): header button, **⌘J/Ctrl+J**, or `window` event `open-ber-ai`; stays mounted when closed so the conversation survives reopen. Context-aware — auto-scopes to the project on `/projects/[id]*`. The ⌘K palette has a pinned "Ask Ber AI: <query>" first row that hands the typed query to the dock. The two inline `AskBerAIPanel` embeds (dashboard, project layout) were deleted — one surface now.
- `tsc` + eslint clean on touched files; full `next build` clean. Four commits pushed to main (auto-deployed). No DB changes.

**Done 2026-07-02 (universal RAG + in-platform Email Research — n8n retired):**
- **Feature A — universal cross-CRM query.** The RAG architecture was sound but coverage had holes; closed them:
  - **Full PDF text is now indexed.** All three document-upload routes (`api/documents/upload`, `api/documents`, `api/opportunities/documents`) run a second Gemini transcription pass (`src/lib/ai/document-text.ts`, PDFs ≤15MB, `maxTokens 60000`), store it in new `documents.extracted_text` / `opportunity_documents.extracted_text`, and embed the FULL text instead of the 2-3 sentence summary (summary remains the fallback for oversize/failed extraction). `maxDuration=300` on those routes.
  - **Opportunities are searchable.** New chunk columns `opportunity_id` / `opportunity_document_id` / `source_type`; new embedders in `embeddings.ts` (`embedOpportunityDocument` / `embedOpportunitySnapshot` (delete-and-replace on create/edit/PATCH) / `embedOpportunityNote` / `embedOpportunityReport`), all built on a shared `insertChunkTolerant` helper that drops unknown columns (PGRST204) and detects the old check constraint (23514) → warn+skip pre-migration. Wired into opportunity create/edit/PATCH, notes POST, doc upload, and email-ingestion confirm.
  - **4 new agent tools** (`agent-tools.ts`): `list_opportunities`, `query_opportunity` (fuzzy name resolve + notes + doc metadata), `search_tasks` (assignee/project/opportunity/status filters, latest note), `get_document_content` (reads `extracted_text` so the agent can quote sources; tolerant retry). Agent prompt now explains the opportunities-vs-projects split. `search_knowledge_base` + synthesize citations label opportunity chunks (`Opportunity: <name>`).
  - **One-time backfill:** `api/admin/backfill-embeddings` (batched; targets `project_documents` / `opportunity_documents` / `opportunities` / `opportunity_notes`; 409 with a clear message pre-migration) + `BackfillCard` on `/intel` that loops until done (stall guard). Remove the card after the backfill runs clean.
  - Migration **`20260703000001_universal_rag.sql`**: chunk columns + relaxed `chunks_source_check` + `extracted_text` columns + `match_chunks` DROP/recreate with **identical args** and extended return shape (so `match-chunks.ts` needed no change; the old RPC pre-migration just returns rows without the new fields).
- **Feature B — Email Research moved fully in-platform (n8n / Mac Studio / local Qwen retired).**
  - New `src/lib/integrations/graph-search.ts`: `searchConversations` (Graph `$search`, KQL-escaped, client-side date filter — `$search` can't combine with `$filter`/`$orderby`; groups hits by `conversationId`, newest-first, cap 15) and `fetchConversationMessages` (`$filter=conversationId eq`, **no $orderby** — Graph rejects the combo; JS sort, most recent 30 kept). **Multi-mailbox (added same day):** every run sweeps `RESEARCH_MAILBOXES` = tuaone@ + info@ + moose@berwilson.com using tuaone's single OAuth grant via `/users/{mailbox}/…` — requires the `Mail.Read.Shared` scope (added to SCOPES; **reconnect via `api/email/oauth` once to pick it up**) plus tuaone having delegated read access to the shared mailboxes in M365. Per-mailbox failures degrade to a note in the report; threads/messages deduped across mailboxes by `conversationId`/`internetMessageId`.
  - New `api/email-research/run` (`maxDuration=300`, auth-gated): search → per-thread transcript (`extractPlainText`, 6k chars/msg) → attachments (deduped name+size across reply chains, max 3/thread; inline/>10MB/non-PDF-image skipped with a note line in the report) → Gemini flash extraction per attachment → one markdown report (~190k char cap, oldest threads trimmed with a note) → `analyzeEmailReport` → `{session_id}` → client redirects straight to the `/email-ingestion/[id]` review screen. No auto-confirm — human review stays mandatory.
  - `EmailResearchForm` reworked: synchronous run with a "takes 1–4 minutes" loading state + time-range select (90d/1y/all); redirects to review on success. Manual paste on `/email-ingestion` still works as a fallback.
  - **Removed:** `api/email-research/trigger`, `api/email-ingestion/inbound` (+ its `middleware.ts` public-allowlist line), the `n8n/` directory, and the three n8n env vars (see §7 — Richard deletes them from Vercel). `SYSTEM_USER_ID` kept as the auth fallback.
  - **Confirm-time RAG tie-in:** on email-ingestion confirm, project-kind stores the report as an approved `updates` row (visible on the project's Updates tab) + `embedUpdate`; opportunity-kind runs `embedOpportunityReport` + snapshot. The handoff document itself becomes queryable from `/intel`.
- `tsc` + eslint clean on all touched files; `next build` clean. Both features pushed to main (auto-deploys).

> **DB push status (2026-07-02, universal RAG):** `20260703000001_universal_rag.sql` must be applied (Richard runs migrations), then `npm run gen-types`. **Pre-migration degradations (all silent, nothing breaks):** PDF full text is embedded but not stored; opportunity snapshot/note/doc/report embedding warn+skips; `get_document_content` returns summary-only; backfill returns 409. **Post-push checklist for Richard:** `supabase db push` → `npm run gen-types` → open `/intel` and run all four backfill targets → spot-check by asking the agent about a PDF's contents and an opportunity. Also: the **first `/email-research` run may need a Microsoft reconnect** (`api/email/oauth`) if the stored token predates the Mail.Read scope.

**Done 2026-07-02 (Email Research trigger + automatic ingestion) — SUPERSEDED same day: the n8n trigger/inbound plumbing described below was replaced by the in-platform run above; kept for history:**
- **Closed the loop both ways** so reports arrive automatically instead of being copy-pasted. Two distinct secrets: `N8N_WEBHOOK_SECRET` (outbound trigger) and `INGESTION_INBOUND_SECRET` (inbound delivery) — never reused, never client-side.
- **Trigger:** new authenticated page **`/email-research`** (`EmailResearchForm`) — search term + optional export label + "Run Email Research". Fire-and-forget: it POSTs to `api/email-research/trigger`, which (auth-gated) makes a server-to-server POST to `process.env.N8N_WEBHOOK_URL` with header `X-Webhook-Secret: N8N_WEBHOOK_SECRET` and body `{ searchTerm, exportLabel }`, returns as soon as the webhook is dispatched (10s abort cap), and never leaks the URL/secret. Confirmation tells the user the report will show under Email Ingestion > Recent.
- **Inbound:** new **`api/email-ingestion/inbound`** (called by n8n, not a browser) — added to `middleware.ts`'s public allowlist (exact path `=== '/api/email-ingestion/inbound'`); its only auth is header `X-Ingestion-Secret === INGESTION_INBOUND_SECRET` (401 otherwise). Accepts `{ raw_text, label }` and runs the **exact same shared processing path** as the manual paste flow. Lands as a `pending` session under Recent — **no auto-confirm**; the human review/confirm step stays mandatory.
- **Refactor (no behavior change):** the analyze core was extracted into `src/lib/email-ingestion/analyze.ts` (`analyzeEmailReport({ rawText, label, userId })`, `EmailIntakeError` with HTTP status, `SYSTEM_USER_ID`). Both `api/email-ingestion/analyze` (manual paste/upload — user resolution + storage read stay in the route) and `api/email-ingestion/inbound` call it, so there's one source of truth. The manual paste/upload flow is unchanged (same request/response contract).
- **Env:** `N8N_WEBHOOK_URL`, `N8N_WEBHOOK_SECRET`, `INGESTION_INBOUND_SECRET` documented in §7 (values supplied separately, server-only). **Nav:** `/email-research` added to sidebar Intelligence group, mobile More, command palette. `tsc` + eslint clean.

**Done 2026-07-02 (Email Ingestion module):**
- **New top-level area `/email-ingestion`** (in the sidebar **Intelligence** group, mobile More, command palette). Closes the "email an opportunity → Ber AI assesses → you decide → records created" loop for existing Outlook threads. Richard's external **n8n workflow on his Mac Studio** (local Qwen) gathers threads+attachments and produces a markdown research report; the platform digests it. **This does NOT re-add the forbidden email scraper (§11)** — all Graph scraping stays external in n8n; the platform only accepts a document a human pastes, and creates records only after human review.
- **Delivery = paste/upload** (chosen over auto-POST). The report is pasted (or a `.md`/`.txt` dropped, read client-side as text). Auto-POST is a documented future add (`n8n/README.md`): a token-gated `/api/email-ingestion/inbound` on `middleware.ts`'s allowlist.
- **Flow:** `POST /api/email-ingestion/analyze` (`maxDuration=300`) runs one Gemini pass with `EMAIL_INTAKE_SYSTEM_PROMPT` (`src/lib/ai/prompts/email-intake.ts`) → unified `EmailIntakeExtraction` (suggested opportunity **and** project fields + people + tasks). It **reuses** `findMatchingProjects`/`matchExtractedParties` (`proposal-matching.ts`) and `assessFit` (`fit-assessment.ts`) via a `toProposalExtraction` adapter, then stages an `email_intake_sessions` row (status `pending`). Review screen (`EmailIngestReview`) shows the fit card, a **Project | Opportunity toggle** (user picks per package), editable record fields, matched-vs-new people, and an editable task list. `POST /api/email-ingestion/confirm` creates the opportunity **or** project, matches/creates `parties` (+ `project_players` when project-kind), and `tasks` (tagged with `project_id` or `opportunity_id`, assignee resolved by name against `team_members`).
- **Schema gap noted:** opportunities have **no player link table** — when opportunity-kind, people are recorded in an `opportunity_notes` entry (+ `counterparty`/`lead` free-text) rather than a first-class link. Add `opportunity_players` later if needed.
- **Shared refactor:** the fit-assessment card was extracted from `ProposalIntakeWizard` into `src/components/proposals/FitAssessmentCard.tsx` (single source; both surfaces import it).
- New migration `20260702000001_email_intake.sql` (`email_intake_sessions`, `updated_at` trigger only — **not** `log_activity()`). `database.ts` hand-extended. `tsc` + eslint clean (the pre-existing `ProposalIntakeWizard` setState-in-effect lint error at ~L139 is untouched).

> **DB push status (2026-07-02):** `20260702000001_email_intake.sql` must be applied to the live Supabase DB (Richard runs migrations). `/email-ingestion` renders and the form works, but analyze/confirm 500 until the table exists. Run `npm run gen-types` after the push to reconcile the hand-extended types. **n8n side:** apply the two in-place edits in `n8n/README.md` (enrich the Qwen prompt; add the paste-into-platform step) — do NOT re-import the workflow JSON (it would wipe the Outlook OAuth credential + model id).

**Done 2026-06-27 (task → opportunity tagging):**
- Tasks can now be tagged to an **opportunity** as well as (or instead of) a project. New nullable `tasks.opportunity_id` (FK → `opportunities`, `on delete set null`) via migration `20260627000002_task_opportunity_tag.sql`. `database.ts` hand-extended (Row/Insert/Update + FK relationship).
- **UI:** `TeamTaskBoard` add-form gets a separate **Opportunity** dropdown next to Project, plus an opportunity filter and an opportunity chip on each task card. `TaskDetailSheet` gets an Opportunity picker + a link card to the opportunity. The opportunity controls only render when opportunities are passed in, so project-scoped task tabs (`TasksTab`, which passes none) are unchanged. `/tasks` page fetches non-closed opportunities and passes them down.
- **Dual-schema tolerant:** opportunity names are resolved from the passed-in list (no PostgREST embed), and the API only writes `opportunity_id` when one is actually chosen — so creating/listing tasks still works before the migration is applied. `tsc` clean.

> **DB push status (2026-06-27):** `20260627000002_task_opportunity_tag.sql` must be applied to the live Supabase DB before opportunity tags persist (tagging an opportunity errors until then; everything else degrades gracefully). It depends on `20260627000001_opportunities.sql` being applied first. Run `npm run gen-types` after the push.

**Done 2026-06-27 (opportunities module):**
- **New top-level CRM concept: `/opportunities`** — strategic pursuits that aren't construction projects (acquisitions, partnerships, JVs, equity investments, mergers, divestitures, teaming agreements, market entry). Modeled like `projects` but lighter and self-contained. Migration `20260627000001_opportunities.sql` adds `opportunities` (name + opp_type + status pipeline + priority + objective + thesis + target/counterparty + deal terms + value/stake/probability + dates + next_step), `opportunity_documents` (white papers / teasers / CIMs / financials — kept **separate** from `documents` so it doesn't touch the RAG/chunks pipeline; files live in the same Supabase `documents` storage bucket under `opportunities/{id}/`), and `opportunity_notes` (progress feed). Type/status/sector are plain `text` + app constants (`src/lib/utils/opportunities.ts`), no Postgres enums. Only the `updated_at` trigger is attached — **not** `log_activity()`, which dereferences `new.project_id` (this table has none).
- **UI:** `/opportunities` list (type+status filters, card grid `OpportunitiesClient`), `/opportunities/new` + `/[id]/edit` (`OpportunityForm`, server actions in `app/opportunities/actions.ts`), `/opportunities/[id]` detail (pipeline progress bar, key-facts grid, objective/thesis, inline `OpportunityStatusControl`, `OpportunityDeleteButton`, white-paper upload via `OpportunityDocuments`, notes feed via `OpportunityNotes`). Optional Gemini AI summary on uploaded white papers (best-effort).
- **API:** `api/opportunities/[id]` (PATCH whitelisted fields / DELETE + storage cleanup), `api/opportunities/[id]/notes` (POST), `api/opportunities/documents` (POST multipart upload), `api/opportunities/documents/[id]` (DELETE).
- **Nav + search:** added to `AppSidebar` (after Projects), `MobileNav` (More), and the command palette + `/api/search` (new `opportunity` result type).
- **Types:** `database.ts` hand-extended with the 3 tables; aliases `Opportunity`/`OpportunityDocument`/`OpportunityNote` in `lib/supabase/types.ts`. `tsc --noEmit` + eslint clean.

> **DB push status (2026-06-27):** `20260627000001_opportunities.sql` must be applied to the live Supabase DB (Richard runs migrations — Claude can't `supabase db push`). `database.ts` was hand-extended; run `npm run gen-types` after the push to reconcile. `/opportunities` pages are dynamic (await searchParams/params), so `next build` won't try to prerender them, but the routes will 500 at runtime until the tables exist.

**Done 2026-06-25 (task system rebuild + email scraper removal):**
- **Tasks are now a real schema**, not `action_items` JSON on `updates`. New migration `20260625000001_team_tasks.sql` adds `tasks` (title + what/why/how + assignee_id + project_id + due_date + status open|done + completed_at), `task_notes` (the updates/notes feed), and `team_members` (lightweight assignee list, seeded Richard/Eric — deliberately separate from `parties`). Completed tasks set `status='done'` (archived, not deleted).
- **`/tasks` is the new landing screen** (`app/page.tsx` and the login redirect now point to `/tasks`; Tasks moved to top of sidebar + mobile primary nav). `TeamTaskBoard.tsx` is the board (avatar chips, project tags, due pills, quick-add teammate, status/assignee/project filters, Open vs Archive); `TaskDetailSheet.tsx` is a right slide-over with editable what/why/how, a project context card (name/sector/value/players, links to the project), and the notes feed. Project tab reuses the board scoped via `scopeProjectId` (`components/projects/TasksTab.tsx`).
- **New primitives:** `components/ui/calendar.tsx` + `date-picker.tsx` (custom month grid in an outside-click dropdown — no new deps).
- **API:** `api/tasks` (GET/POST), `api/tasks/[id]` (GET detail+players+notes / PATCH / DELETE), `api/tasks/[id]/notes` (POST), `api/team-members` (GET/POST).
- **Email scraper ripped out:** deleted `lib/email/`, `api/email/{webhook,subscribe,backfill}`, `api/cron/renew-subscriptions`, `email-log/`, `components/email/`. Removed the cron from `vercel.json`, the webhook/cron public exceptions from `middleware.ts`, and Email Log from all navs. **Kept** `api/email/oauth(+callback)`, `email_tokens`, and `lib/integrations/microsoft-graph.ts` because `/calendar` + party enrichment use them. Shared extraction (`lib/ai/prompts/extraction.ts`, `api/ai/extract`) kept — still used by proposal intake + manual paste.
- **Core rewire:** Dashboard per-project action counts + deadlines and the contact "Tasks" tab now read the `tasks` table (contact tab matches a `team_member` by name). Attention/Capacity/Calendar/AI-briefs still read legacy `updates.action_items` — left for a later pass.
- **Gemini API key rotated** across Vercel production/preview/development + `.env.local` (2026-06-25). Note: the supplied key starts `AQ.` not the usual `AIza` — verify if AI calls 401.

> **DB push status (2026-06-25):** the `20260625000001_team_tasks.sql` migration must be applied to the live Supabase DB (`supabase db push`). Generated types in `database.ts` were hand-extended for `tasks`/`task_notes`/`team_members`; run `npm run gen-types` after the push to reconcile. A full `next build` will fail to prerender `/tasks` until the tables exist.

**Done 2026-06-22:** Company profile fleshed out with a structured **pursuit profile** (`company_profile` migration `20260622000002`), editable on `/company`, and wired into the AI — the executive agent injects it via `getCompanyContext()`, and proposal intake runs `assessFit()` to give a pursue/consider/pass recommendation in the wizard.

**Done 2026-06-22 (UI lift):**
- **Design system refinement** in `globals.css`: light neutrals carry a faint cool tint (hue 260) to harmonize with the navy sidebar; added a layered elevation scale (`--elev-1/2/3` + `.elev-*`), `.lift` (hover elevation), `.stagger-children`, `.tnum` (tabular figures), and a `prefers-reduced-motion` block. `Card` now defaults to `elev-1` and accepts an `interactive` prop for hover lift+glow on clickable cards.
- **Dark mode** is now wired (pre-paint no-flash script in `layout.tsx` + `ThemeToggle` in the header) **and the color audit is done** (2026-06-23). The portfolio module was already `dark:`-aware; an idempotent transform (`scripts/dark-tints.mjs`) added `dark:` variants to ~1,100 light tint utilities (`bg/text/border/divide/ring-{color}-{50|100|200…}`) across 91 files, preserving `hover:`/`focus:` prefixes. Bare `bg-white` survivors fixed by hand (slider thumb → `bg-background`; login/set-password inputs → `dark:bg-slate-900`; milestone dot left white — it sits on an emerald pill). Dark mode now renders cleanly everywhere via the toggle. It is still **opt-in by default** — the no-flash script only applies `.dark` when `localStorage.theme === 'dark'`; it does NOT follow the OS yet. Flipping it to follow `prefers-color-scheme` is now safe (one-line change in `layout.tsx`) but left as a deliberate choice.
- **Sidebar consistency:** Portfolio/Equity folded into `NAV_GROUPS` (was a hand-coded block with a different active style).
- **Command palette (⌘K / Ctrl+K):** `CommandPalette.tsx` + `AppHeader` host it; new `GET /api/search?q=` does cross-entity ilike search over projects/parties/entities. Replaced the old mobile-only page-search. Desktop search box + mobile search button both open it.

**Done 2026-08-24 (debug pass — mail intake unstuck, AI reaches the whole app, steel cost basis fixed):**
- **The mailbox sweep had silently stopped ingesting new mail.** `fetchAllMailboxes` skipped any mailbox whose `state` was `complete`, and both mailboxes reached `complete` on 2026-08-24 at 04:44 — so from that moment the hourly cron re-summarized the existing backlog and fetched nothing, forever. The file's own docstring claimed the opposite ("re-running after completion picks up only new mail"), which is what the fix now makes true. A finished backfill drops into a **catch-up pass**: page one of a window derived from `completed_at` (elapsed days + 2, capped at 30), max 3 pages, stopping at the first page with nothing new — Gmail lists newest-first, so that page proves everything below it is already stored. Counters accumulate, `started_at` is preserved, and `completed_at` becomes the marker the next window measures from (no migration needed). Verified live: both mailboxes caught up, skipped 14 + 71 known threads, and picked up 2 genuinely new ones (533 → 535).
- **Follow-ups on a staged deal no longer propose a second record.** `clusterUnassigned` only ever attached new threads to `open` clusters, so a reply on an already-staged deal formed a fresh cluster and would have been staged as a SECOND pending project/opportunity for the same deal. Staged clusters now absorb follow-ups (and stay staged, so nothing is re-proposed); `dismissed` is still excluded, since a thread on a rejected deal deserves reconsideration. This never bit only because the fetch bug meant no new mail was arriving.
- **A sales rep's deal saved with no cost basis at all.** In `saveServices`, cost came from the form only when `canSeeFinancials` — and the form itself gates cost derivation the same way — so a `steel_sales` user creating a deal produced line items with `cost = null`. That reads as **100% margin**, and commission is paid on commissionable margin, so the rep was credited a commission on the full sale price. Cost for per-SF services (materials/assembly) is now derived **server-side** from `cost_per_sqft ?? DEFAULT_STEEL_COST_PER_SQFT` × square feet whenever nothing survived the form, and the basis used is persisted. A deliberate `0` from a financials user is still respected.
- **The AI could not see most of the app.** `agentTools` covered projects/opportunities/investors/tasks/documents and nothing else — asked about steel, priorities, meetings, or correspondence it would answer from an empty result. Added **14 read-only tools** in `src/lib/ai/agent-tools-modules.ts`, spliced into `agentTools` and `executeToolCall`: steel (`list_steel_deals`, `query_steel_deal`, `get_steel_summary`, `get_steel_payouts`, `get_steel_marketing`), `list_objectives`, `search_meetings` + `get_meeting_content` (confidential minutes withheld), `search_email_threads` + `get_email_thread`, `get_intake_status`, `get_recent_activity`, `list_team_members`, `get_dino_summary`. The sweep tables go through `sweepDb()` — they post-date the last type generation. The local path builds its declarations from the same array, so all 36 tools work on Qwen as well as Gemini. `prompts/agent.ts` gained routing lines plus two paragraphs: steel's commission-payable-only-when-collected rule, and the fact that correspondence often exists for a deal with no CRM record yet (search email before concluding nothing exists).
- **Data purged** (backed up first to `~/berwilson-data/backups/purge-*.sql.gz`): 4 exact-duplicate contacts (same email, zero references anywhere — verified against all 11 FKs into `parties`); 4 placeholder steel deals (`Architect deal 1/2`, `LA deal 2`, `Playground housing extension` — no customer, no SF, no price); and **IHC Steel's missing cost basis** backfilled at the $20/SF default. That one deal was the single biggest distortion in the app: 100,000 SF at $2M with no cost read as $2M of pure margin and projected Richard a ~$200k commission. Steel now reports **4 deals, $510k margin on $3.6M revenue (14%)** instead of 8 deals at 70%, and total projected commissions fell from $260k to $60k.
- **Also corrected:** two definitions of "won" were being mixed in one answer (`isOpenStage` = not paid/not lost, vs marketing's `isWon` = order placed or beyond), so the steel summary reported a deal as both open and won. It now reports `in_pursuit` / `won` / `collected` / `lost` with the definitions stated inline. Stale build cache (macOS ` 2`/` 3` duplicate files) was breaking `tsc`; removed, and the stale `CLAUDE 2.md` snapshot (which still claimed Microsoft Graph) deleted. The "Reality" and "Working" lines above were themselves out of date — corrected to Mac Studio + Gmail.
- **Verification:** `tsc` clean, `next build` clean, eslint unchanged from baseline (14 errors / 6 warnings, all pre-existing `react-hooks/set-state-in-effect`, `react-hooks/purity`, and `no-img-element` — data-loading effects and `<img>` usage, not runtime faults). Every new tool was exercised against the live database through a temporary route, which was removed and the middleware allowlist restored byte-identical.
- **Merge with the Studio's Aug 18 push (same session).** `origin/main` carried two commits that never reached the MacBook (the Studio working-tree sync + the weekly task digest), while the local branch carried the Gmail migration — so the histories had diverged and `git fetch` was itself broken by a stale duplicate ref (`.git/refs/remotes/origin/main 2`, the same macOS ` 2`/` 3` duplication that broke `tsc`). Removed the ref, then merged: kept the Gmail-era side everywhere the two disagreed (mailbox health, the disabled `gen-types` guard, the middleware allowlist minus the dead Microsoft OAuth callback), and kept the digest work. **The digest's outbound email was the one real casualty** — `src/lib/notify` imported `sendMail` from the deleted `microsoft-graph.ts`. Ported it: `sendMail()` now lives in `google-workspace.ts` on Gmail `users.messages.send`, and `gmail.send` (send-only; it cannot read or modify) was added to `SCOPES` and to `scripts/setup-google-oauth.mjs`. Existing refresh tokens are unaffected — the refresh call does not send `scope` — but they were minted without it, so **the digest cannot actually send until each mailbox re-consents**: `node scripts/setup-google-oauth.mjs --only <addr>`. `sendMail` detects that case and returns that instruction instead of a bare 403, and the Task Digest health card says the same.
- **Not done / open:** the 12 `set-state-in-effect` findings are a real refactor across 12 components and were left alone deliberately. ~~The Studio still runs the previous build~~ **DEPLOYED 2026-08-24 11:28** — the mail catch-up is live and confirmed working (see the entry below).

**Done 2026-08-25 (gap-closing pass after the lead build — silent outages, agent blindness, no push, unbacked secrets; DEPLOYED + MIGRATED):**
- **Mail was dead and NOTHING said so — the worst of the five.** After the crash took the OAuth tokens, `/api/cron/email-sweep` returned 503 *before* the sweep started, so no `mailbox_sync` row was ever written. Both mailboxes still read `state=complete`, the dashboard alert keys on `state='failed'`, and so the platform looked perfectly healthy while no mail had reached the CRM for hours. This is exactly the failure class `/settings/health` was built for in July, reappearing through a path that bypassed it — health is pull-only, and nobody opens it when nothing looks wrong. **New `recordSweepUnavailable(mailboxes, reason)` in `email-sweep/db.ts`**, called from both cron routes' unconfigured branch: upserts every mailbox it owns to `state='failed'` with the reason. Safe against resume — `fetchMailbox` decides where to restart from `completed_at`, never from `state`, so a flagged mailbox still catches up over a short window instead of re-reading years; the next good run overwrites it, so it self-heals. Never throws (recording a failure must not turn a 503 into a 500). **The dashboard now also carries the reason** (`last_error` joined into `mailboxAlert`) rather than sending the reader to another page to find out why — during an outage "why" is the whole question. Verified live: both crons flag their mailboxes, and the dashboard renders "Mailbox sweep failing (moose@…) — New email is no longer reaching the CRM — Google Workspace is not configured."
- **Ber AI could not see leads.** On 08-24 the headline finding was "the AI could not see most of the app" and 14 tools were added to fix it — then this build shipped a whole surface with none, so "what bids are due?" would answer from nothing. Three read-only tools added to `agent-tools-modules.ts`: **`list_leads`** (route/status/recommendation/`due_within_days` filters; marketing rejects excluded unless explicitly asked for), **`query_lead`** (full extraction + requirements + attachments + the whole fit assessment incl. the questions to answer before bidding), **`get_leads_summary`** (awaiting decision, split by destination, pursue/consider counts, overdue and closing bids, how many were filtered as marketing). The agent prompt gained a routing line and a paragraph stating the thing the model must not get wrong: **a lead is NOT pipeline** — nobody owns it, promotion is what makes it real, and it must never be counted in pipeline value or presented as work we are doing. All three exercised live against real rows.
- **Nothing pushed a hot lead at anyone.** The queue only helps someone who opens it; a `pursue` lead with a ten-day bid date sitting unread is the miss the module exists to prevent. New `notify-leads.ts` + a `notify` phase on the lead sweep: one **digest** (never one mail per lead — a Monday burst of eight should read in thirty seconds), cards ordered by bid date with urgency read off that date alone, the fit line, and the first concern. Migration `20260825000002_lead_notifications.sql` (**APPLIED**) adds `leads.notified_at` + a partial index, so each lead is announced **once** — re-sending every morning until someone acts is how a notification becomes noise, and noise is how the next real bid gets missed. A failed send leaves `notified_at` null so the next run retries rather than losing them. **Inert until `LEADS_NOTIFY_EMAIL` names a recipient** — who gets told is a business decision and mailing a guessed address is worse than not mailing. All three paths verified (skips cleanly unset; retries on send failure; renders correctly).
- **`.env.local` and the OAuth tokens were unbacked-up single-copy secrets** — the nightly job covered Postgres and the storage volume and nothing else, and the crash had just demonstrated the loss mode on the one file holding the OAuth client secret and every refresh token. `~/supabase-selfhost/backup.sh` (backup `.bak-presecrets-20260825`) now builds a **secrets bundle** alongside the dump: `.env.local` + the tokens file, tarred under `umask 077`, `age`-encrypted to the same offsite recipient, pushed to the mini, and the **plaintext deleted immediately whatever happens** — unlike the db/storage artifacts it is never kept locally in the clear. Only files that exist are bundled (tar exits nonzero on a missing member, and a missing tokens file — the normal state between a loss and a re-consent — must not stop `.env.local` being backed up). Dry-run verified: 1.7KB tar at mode 600, encrypts, plaintext gone; undecryptable on the Studio, as designed.
- **Promoting a lead now keeps the relationship.** Bid invitations come from the same handful of GCs repeatedly, but promotion recorded the firm as a client *string* while the estimator who sent it existed nowhere searchable. `promoteLead` now find-or-creates **two** directory contacts — the organisation and the person (with their email and phone) — tagged `inbound-lead`, and on a project links both as players with prose roles ("Inviting Contractor", "Bid Contact") matching the free-text voice already in `project_players`, not a slug vocabulary the directory doesn't use. Wholly non-fatal: a directory miss never fails a promotion that already created records. Verified end to end, then all test rows removed (0 residue across projects/parties/players/leads/threads).
- **Also fixed:** `scripts/setup-google-oauth.mjs` requested neither the new `drive.readonly` scope nor the info@ mailbox — re-consenting with it would have minted tokens that 403 on the first Drive call and left the lead sweep with no credential at all. It now mirrors `SCOPES` and unions `GOOGLE_LEAD_MAILBOXES`, with a comment saying the two lists must stay in step.
- **Verified:** `tsc` + full `next build` clean; eslint on every touched file clean (the 2 survivors are the documented pre-existing `react-hooks/purity` `Date.now()` misfires, unchanged in count); deployed and smoke-tested. **Still blocked on credentials** — see the lead-list entry above; the OAuth re-consent gates mail, and `LEADS_NOTIFY_EMAIL` / `GOOGLE_DRIVE_KNOWLEDGE_FOLDER_ID` / `DINO_LEAD_EMAIL` are documented in `.env.local` and §7 but deliberately unset.

**Done 2026-08-26 (Workspace overhaul shipped — the CRM writes back into Gmail, Drive, and Chat; DEPLOYED + MIGRATED):**
- **The problem the whole pass exists to solve: Ber Intelligence is tailnet-only and most of the team cannot reach it.** Everything below pushes information OUT to where people already are, while the platform stays the system of record. Nothing is read back in — one-way by design, so Drive and Gmail never become a second source of truth.
- **Scopes are now TIERED, and the tier is decided in exactly one place.** New `scopesFor(mailbox)` in `google-workspace.ts`: `SCOPES` (all mailboxes) + `PRIMARY_ONLY_SCOPES` (moose@ — Docs write, `drive.file`) + new `LEAD_ONLY_SCOPES` (info@ — `gmail.modify`). `gmail.modify` is the broadest grant in the file (read/write/label/trash), so it is deliberately withheld from the deal mailboxes, which stay read-only. `scripts/setup-google-oauth.mjs` **parses all three arrays out of the app source** and `probeScopeCoverage()` holds each mailbox to its OWN tier — previously it checked every mailbox against one flat list, which would have reported moose@'s Docs write as a gap on info@. **`contacts` is now READ+WRITE**, superseding `contacts.readonly`.
- **Leads → Gmail labels** (`lib/leads/gmail-sync.ts`, `lib/integrations/gmail-write.ts`). The triage verdict is written onto the thread as a `Ber AI/…` label, so an estimator scanning info@ sees which invitations are worth bidding **without a login, a VPN, or the app**. It also makes the marketing filter auditable: 79 of 107 threads were rejected as marketing, and a rejection you can only inspect inside the platform is one nobody checks. Exactly one platform label per thread — the previous one is recorded on the lead and removed when the verdict moves, and **only the label this platform applied is ever removed**, so anything a human filed the thread under is theirs and stays. `refreshLeadLabel` fires on promote/forward/status-change so a decision reaches the mailbox in seconds rather than at tomorrow's cron.
- **Leads → Gmail drafts** (`lib/leads/draft-reply.ts`). A `pursue` invitation needs an answer within a day; that reply was being written late or not at all while the local model sat idle 94% of the time. The platform now writes it and **stops** — it creates a DRAFT and never sends. That is the entire safety model: the §11 "mail never becomes a record/action without human review" invariant is satisfied *structurally*, because an unsent draft does nothing on its own. `pursue`-only (a `consider` lead is one a human still has to think about, and a drafted reply quietly lobbies for it), and `gmail_draft_id` is a one-per-lead-ever latch — a draft a human deleted is not re-created, because deleting it WAS the decision.
- **Records → Drive** (`lib/drive/publish.ts`, `api/drive/publish`). A project's RFP, drawings, and addenda are unreachable for most of the company most of the time; publishing copies them into a Drive folder. Idempotent per document via `drive_published_id`, so re-publishing uploads only what is new. Folders are **created, not looked up by name** — two records may legitimately share a name and merging their bid packages is worse than an untidy folder list. Uses `drive.file`, which grants access to app-created files ONLY, so it is safe to hold alongside `drive.readonly`. Called quietly from `promoteLead` (a Drive hiccup must never read as "promotion failed") and wired as a button on **projects, opportunities, and steel deals** — admin-only, matching the API's own guard, because publishing is a sharing decision.
- **Google Chat as a second notify channel** (`lib/notify/chat.ts`). Chosen over Telegram — the seam's original target — because the space already exists and nobody has to install an app or create an account. Deliberately an INCOMING WEBHOOK, not a Chat app: an app would let people ask Ber AI from the space, but that needs Google to call INTO a tailnet-only platform with no public endpoint. Outbound works today; inbound is blocked by network shape, not effort. The lead digest is **hand-written in Chat's dialect** rather than converted from the email — a space is skimmed on a phone, so it is terser. Threaded per day so a digest reads as one conversation. `notified_at` is stamped once ANY channel delivers; retrying for a second channel would re-announce every lead to the one that already worked, and a duplicate digest is how a digest starts being ignored.
- **Migration `20260826000001_workspace_integrations.sql` — APPLIED.** Every column records what the platform has ALREADY written to something outside itself (a label applied, a folder created, a file published), which is precisely what makes each write idempotent. Without them a daily cron re-labels and re-uploads forever. `meetings.drive_file_id` carries a partial unique index so the (unbuilt) transcript importer is idempotent by construction.
- **BUG FOUND: the Drive button rendered nowhere.** `DocumentsTab` had been given `driveFolderUrl` / `canPublish` props but `projects/[id]/documents/page.tsx` never passed them; both are optional and `canPublish` defaulted falsy, so the entire feature was invisible in the UI. Fixed, and the same wiring added to `OpportunityDocuments` and `SteelDealFiles`. In `SteelDealFiles` the publish control is gated on `canPublish` **alone, not on `canEdit`** — a steel rep may attach files but may not share them out of the tailnet, so neither permission should imply the other.
- **Deliberately deferred (schema exists, no code):** Meet transcript import (`meetings.drive_file_id`) and directory → Workspace contacts sync (`parties.google_contacts` / `google_contacts_hash`). The `contacts` write scope is granted ahead of its code, accepted knowingly to avoid consenting three mailboxes twice.
- **Verification limit worth knowing: there are currently ZERO `pursue` leads** (22 consider, 6 pass, 79 spam), so `draftLeadReplies` is a structural no-op and cannot be exercised by simply running the sweep. Drafting was verified by running it standalone against one `consider` lead with the filter temporarily widened, then deleting the draft and clearing the latch columns.
- **New env:** `GOOGLE_CHAT_WEBHOOK_URL` (+ `_<KEY>` per-space variants) and `LEAD_GMAIL_LABELS=off`. Both unset = the feature is inert, not broken.

**Done 2026-08-26 (bonding capacity recorded — the top-cited gap closed):**
- **Richard: "Keep bonding under 20 million for now."** Recorded as **$20M on BOTH `bonding_capacity` (single-project) and `aggregate_bonding`**. He gave one number without splitting the two; taking it as the ceiling for each is the conservative reading — it can only under-state capacity, never lead to committing to a bond that cannot be placed. Revise when a surety letter states real separate limits.
- **Made it actually bite.** A capacity number the assessor never tests against is decoration, so a disqualifier was appended: work requiring more than $20M in surety bonding (single or aggregate) is out of scope. Bonding was the **most-cited gap in the entire backlog — 55 mentions**, more than double certifications.
- **Written to BOTH sources, because only one of them binds.** Retrieval testing on 08-26 proved the Drive document outranks the profile for credential questions, so the database alone would have been ignored again. The document's bonding line and gap-table row now both state the $20M ceiling, and the file was re-synced (8 chunks re-embedded). Verified in the assembled AI context: `- **Bonding:** single project $20.0M / aggregate $20.0M` plus the disqualifier line.
- **Gotcha worth remembering:** `getCompanyContext()` emits the bonding line **unconditionally**, so before this it read `$0 / $0` rather than being absent — the assessor was being told capacity was zero, which is worse than silence and may explain some of the hedging. Any numeric profile field left null renders as a hard zero, not as "unknown".
- A re-score of all 28 leads is running against the complete profile. **Read the verdicts, not the scores** — the ±20 point variance measured earlier still applies.

**Done 2026-08-26 (knowledge document corrected in place; calendar deadlines live; score precision measured):**
- **Richard confirmed the DBE/MBE/WBE/SBE certifications are real.** The "needs certificate verification" caveat was a conservative placeholder written 2026-07-03 when no certificate had been sighted, and it had propagated into BOTH the database disqualifier AND the Drive knowledge document. Retrieval testing proved the document was the binding half: for a certification query its credential table was the **top hit at 0.642 similarity**, outranking the profile, so correcting the database alone changed nothing.
- **Measured, not assumed: fixing the database by itself did NOT help.** All 28 leads re-scored against corrected certifications: average **54.9 → 53.5 (−2.2)**, 18 down / 6 up / 3 unchanged, still zero `pursue`. Recording 9 credentials (incl. Utah GC #14235441-5501, GSA MAS, SAM.gov/CAGE/UEI) was correct and worth keeping, but certifications were never the constraint.
- **THE IMPORTANT FINDING — fit scores are precise to about ±20 points.** The same leads, same correspondence, same profile, re-scored: Sky Mountain Golf 40→65 (+25), Residences at Millcreek 60→40 (−20), RFQ 2027-06 62→42 (−20). Nine of 28 moved more than 10 points **in both directions on unchanged inputs**. That is local-model sampling variance, not signal. **Do not treat the numeric score as a ranking** — the pursue/consider/pass verdict and the concerns list held steady across runs while the numbers swung. Any future feature that sorts or thresholds on `fit_score` is building on noise.
- **The Google Doc was corrected in place via the Docs API** (7 replacements, one batchUpdate guarded by `requiredRevisionId` so a concurrent human edit could not be clobbered): four credential rows Flagged→Active, the gap-table row, the set-aside disqualifier, and a sharper bonding note. Verified afterwards: 0 remaining occurrences of "Flagged", "Needs certificate verification", "verified in hand", "certificates need verification".
- **The disqualifier was NARROWED, not deleted.** The document itself states the SBA 8(a) pathway is "in development — not yet active", so Buy Indian Act / IIP / tribal 8(a) set-asides genuinely remain out of scope; only the DBE/MBE/WBE/SBE half was wrong. It now reads as tribal/NHO-only and states explicitly that DBE/MBE/WBE/SBE set-asides ARE in scope.
- **New `PRIMARY_ONLY_SCOPES`.** Docs write can edit or delete *any* Google Doc in the account it is granted on — the first genuinely powerful permission here against a codebase of read-only scopes. Only the Drive-sync mailbox needs it, so the consent script requests it for `PRIMARY_MAILBOX` alone: **moose@ gets 8 scopes, tuaone@ and info@ stay at 7.** Re-consenting after removing the entry hands the permission back; that is the intended way to close the window.
- **Calendar deadlines are LIVE.** 10 events written to moose@ (bid dates, site visits, RFI cut-offs) with 3-day and 1-day reminders. **Bug found and fixed by running it:** Google event ids are base32hex — digits and letters **a-v only** — so the obvious `bw` prefix (w is outside the alphabet) produced "Invalid resource id value" on every insert. Now prefixed `lead`. Idempotency proven: run twice, still exactly 10 events, no duplicates.
- **Verified:** doc read back and confirmed correct; Drive re-sync updated the document in place (8 chunks re-embedded, stale caveat no longer retrieved); `tsc` + build clean; deployed.

**Done 2026-08-25 (leverage pass — idle model put to work, five queues become one, deadlines reach the calendar; DEPLOYED + MIGRATED):**
- **The measurement that drove all of it.** Over 7 days the local model ran **856 calls / 10.6 busy hours = 6.3% utilisation** — idle 94% of the time, during a *busy* week with a full mailbox backfill. Meanwhile **104 intake sessions sat pending, the oldest six weeks old**, plus 14 undecided leads and 35 review-queue rows. The free resource was idle and the scarce one — the executive's attention — was the bottleneck. Every change below moves work from the second to the first.
- **Pre-decide phase (`src/lib/email-ingestion/predecide.ts`, migration `20260825000003` APPLIED).** Attaches a recommended **disposition** to each pending session — create / merge / dismiss — with a one-sentence reason and a `headline`: the single fact that would change the reader's mind. `/intake` now sorts create-first, shows the merge target inline, and prints the headline under each row, so most sessions are decidable from the list. **Deliberately NOT a threshold on `fit_score`:** fit measures whether we should PURSUE, which is a poor proxy for whether something is a RECORD. Sampling the backlog found an approved **LOI scored 15** and a live **Utah County IDIQ pursuit scored 30** — a fit threshold would have discarded both. It asks a separate question and only auto-acts on dismissals above **0.85 confidence**; creating anything still requires a human (§11 untouched). Runs as the last sweep phase on leftover budget. Live results were sharp — it caught a merge into an existing West Wendover record and surfaced headlines like "250 ADU units at $105k–$115k, awaiting follow-up call".
- **One `/decide` surface** (`src/app/decide`, `DecideClient`). Inbound bids + staged correspondence + low-confidence extractions in a single ranked list, each row linking into its existing review UI — it replaces the *need to visit five screens*, not the screens themselves. **Ranking differs from the email on purpose:** here a hard deadline outranks quality (`weight()` = overdue 2000 / closing-within-7-days 1000 / pursue-or-create 100 / + score), because a bid can stop being available; the emailed digest is quality-first per Richard's instruction. Verified live: the two leads closing in 7 days lead the list at 72 and 55, ahead of a 74 closing in 15 days. **Deliberately excludes open tasks** — a task is work already decided on, and mixing it in turns a decision list back into a to-do list. Nav: Decide joins `primary` (carrying the review badge), Leads moves to `intelligence`, and the Review Queue row is **removed from nav** but stays routable.
- **Lead deadlines now reach Google Calendar** (`src/lib/leads/calendar.ts`, new `calendar.events` scope). Bid dates, site visits and RFI cut-offs existed **only inside a tailnet-only app most of the team cannot reach** — a mandatory pre-bid site visit was effectively invisible. Written as all-day events keyed `lead:<id>:<kind>` via a caller-supplied Google event id, so a daily sweep never duplicates and **a date that moves MOVES** (409 → PATCH) rather than leaving a stale deadline behind. Reminders at 3 days and 1 day. Only pursue/consider leads get events — calendaring every marketing blast would make the calendar the next thing nobody reads. **Requires one re-consent** (see below); until then it fails softly with a message naming the fix.
- **Emailed digest is now ranked best-first** (Richard's instruction): pursue before consider, then fit score, with the bid date breaking ties only. Sorted at both the query and inside `renderLeadEmail`, so no caller can produce a different order.
- **Nav prunes itself.** New `NavItem.hideWhenEmpty` — a module with no rows is hidden rather than shown as a dead end. Applied to **Dino (0 rows)**: it is mid-acquisition, so hiding beats deleting work about to be needed, and it **un-hides with no code change** the moment the first revenue row lands. (Correcting an earlier overstatement of mine: nav is **18 destinations, not 29** — that count had included `alsoMatches` paths.)
- **Two error messages that would have misdirected during an outage:** `explainTokenError` gained an `insufficientPermissions` branch (was dumping raw Google JSON; now names the re-consent command), and its `accessNotConfigured` branch no longer hard-codes "Gmail, Calendar and People" — it surfaces Google's own message, which names the exact API **and the project number that matters**. That mattered in practice: the Drive API was enabled on two freshly created projects before the right one, because Google checks the API against the project owning the **OAuth client**, not the project on screen.
- **A scope-drift bug I introduced, and the structural fix.** `calendar.events` was added to `SCOPES` in `google-workspace.ts` but not to the SECOND hand-maintained copy in `scripts/setup-google-oauth.mjs`, so Richard's re-consent minted tokens without it — the exact failure the comment above that list warned about, and the second time in one day (drive.readonly was the first). **The script no longer keeps its own list: it parses `SCOPES` out of the app source at run time**, exits loudly if it cannot, and drift is now impossible rather than merely discouraged. Duplicating a constant across a runtime and its setup tool guarantees this eventually; don't reintroduce it.
- **New `probeScopeCoverage()` + a "Google Permissions" health card.** Adding a scope does nothing to tokens already minted — they keep working for everything they had and 403 on the new call, which surfaces as a raw permissions error at the worst moment. The probe refreshes each stored token (Google returns the granted scope list in the same response), diffs it against `SCOPES`, and names both the mailboxes and the missing scopes with the re-consent command. Verified live: it correctly flagged all three mailboxes as missing `calendar.events`.
- **Verified:** `tsc` + full `next build` clean throughout; pre-decide exercised on real sessions (6 → 5 create / 1 merge, ~22s each) then run across the backlog; `/decide` and `/intake` rendered authenticated with chips, headlines and deadline ordering; ranking re-checked against live rows; Dino confirmed absent from the rendered nav while Decide is present. **Still pending Richard: one re-consent for `calendar.events`** — `node scripts/setup-google-oauth.mjs`.

**Done 2026-08-25 (Inbound Lead List — info@ triage → fit score → promote/forward; DEPLOYED + MIGRATED; CREDENTIALS PENDING):**
- **The structural gap: unqualified inbound had nowhere to live except as a `project`.** That is why 10 of 15 projects sit in pursuit/capture/bid carrying ~$50B of notional pipeline into the dashboard, map, and daily brief. A **lead** is the tier BEFORE stage one — it arrived, nobody owns it, and it expires with its bid date. Promotion (assigning an owner) is what turns it into a project, an opportunity, or a steel deal. `info@berwilson.com` receives the bid invitations, ITBs, and plan-room notices mixed with a majority of marketing, and **none of it reached the platform** — the sweep only read moose@ and tuaone@.
- **Two mail pipelines, deliberately kept apart.** `email_threads.pipeline` ('deal'|'lead', default 'deal') records which side a thread came in on, set at fetch time from the mailbox. Deal mail is clustered into multi-thread pursuits and staged as `email_intake_sessions`; lead mail is **one thread = one lead**, triaged and scored on its own and **never entering that queue** (104 pending sessions there already). `MAILBOXES` vs new **`LEAD_MAILBOXES`** (`GOOGLE_LEAD_MAILBOXES`, default info@) is the whole switch; `fetchMailbox`/`fetchAllMailboxes` take a `pipeline` option.
- **Marketing is dropped at the Gmail edge, where it costs nothing.** `leadExclusions()` appends `-category:promotions -category:social -label:bw-filtered` to the sweep query, so an excluded thread is never fetched, stored, or scored — and a human can retire a repeat marketing sender by writing one Gmail filter (`bw-filtered`) instead of shipping code. Override wholesale with `GMAIL_LEAD_EXCLUSIONS`. What survives Gmail is triaged by the model; **rejects are kept as `status='spam'` with a plain-language `spam_reason`, not deleted** — the "Show filtered" toggle over those rows is what makes the filter trustworthy.
- **Migration `20260825000001_leads.sql` — APPLIED to the Studio DB.** `leads` (43 columns: what arrived, what the model extracted, the fit assessment in the same shape `assessFit` returns, staged attachments, where it went) + `email_threads.pipeline` + `documents.drive_file_id`/`drive_modified_at`. `score_state` (pending|scored|failed|skipped) is separate from `status` (new|reviewing|promoted|forwarded|ignored|expired|spam) so a scoring failure is retryable without touching the human decision. **`sector` is deliberately plain text, not the `project_sector` enum** — the model's output is not trusted enough to be constrained at the column; promotion casts and validates. Unique index on `thread_id` (re-triaging updates in place rather than stacking duplicates). `update_updated_at` only — **not `log_activity()`**, which dereferences `new.project_id` (the trap already documented on opportunities and the sweep tables).
- **Own orchestrator, not another phase in `runSweep()`.** `runLeadSweep()` fetches, then scores, then triages, then expires. Two reasons the pipelines don't share a clock: a long deal backfill would starve lead triage indefinitely, and a fault in one must not stall the other. Scoring runs **before** triage (40% of remaining budget) so a long triage pass cannot leave yesterday's leads unscored forever. First sweep reads **90 days**, not all history — a two-year-old ITB is not a lead.
- **Fit scoring reuses `assessFit()`** — same Commercial/Operational/Financial/Compliance lenses and the same output the proposal wizard renders, so a lead and a proposal are judged against one picture. Attachments are pulled from the thread and staged before scoring.
- **Promotion and forwarding are the two exits.** `POST /api/leads/[id]/promote` creates a project, opportunity, or steel deal and stamps `promoted_*_id`. **Dino leads leave by email**: Dino's team has neither platform logins nor Tailscale access, so `POST /api/leads/[id]/forward` sends the brief with the original files attached (`DINO_LEAD_EMAIL`) and records `forwarded_to`/`forwarded_at` — chosen over a `dino_leads` table for exactly that reason. This required **attachment support in `sendMail()`** (new `MailAttachment`, multipart/mixed with a random boundary; capped at 15MB against Gmail's 25MB message limit), plumbed through `notify()`.
- **Nightly Drive → knowledge sync (`/api/cron/drive-sync`, 3:15am).** `assessFit` already asks for a "RELEVANT BER WILSON EVIDENCE" block retrieved from `is_company` chunks; without documents behind it every lead is scored against a paragraph of profile text. Pointing the sync at a Drive folder of capability statements, past performance, and credentials is what turns a generic score into a grounded one. Change detection is `(drive_file_id, drive_modified_at)` — an unchanged file costs one list entry and nothing else; an edited one is re-downloaded and re-indexed **with its old chunks removed first**, so a revision cannot leave both versions in the index contradicting each other. Adds the read-only `drive.readonly` scope. Skips cleanly (200, `skipped:true`) when `GOOGLE_DRIVE_KNOWLEDGE_FOLDER_ID` is unset.
- **Surfaces:** `/leads` (queue: route/status filters, bid-date ordering, fit badge, "Show filtered" over the spam rows) + `LeadDetailSheet` (full extraction, fit card, attachments, promote/forward/ignore). Nav item (Radar icon, primary group). Admin-only by default-deny — `/leads` and `/api/leads` are in no `permissions.ts` allowlist. **Dashboard + attention wiring:** leads with a bid date inside 14 days and a pursue/consider recommendation and no decision surface in **Needs Attention**, and `computeAttention` gained the matching category (so the agent's `get_attention_items` sees them too).
- **launchd:** `com.berwilson.cron-lead-sweep` (**daily 5:30am**, ahead of the workday — overnight invitations are triaged and scored before anyone opens the queue; deliberately off the hourly deal sweep's clock so the two never compete for the local model) and `com.berwilson.cron-drive-sync` (3:15am). Both **installed + loaded on the Studio**; both added to the middleware public allowlist (self-guarding via `CRON_SECRET`) and to `deploy-to-studio.sh`'s service loop.
- **Verified:** `tsc` + full `next build` clean (all 7 lead routes compile); migration confirmed applied column-by-column (43 columns, 5 indexes, RLS policy, trigger); **rolled-back live psql round-trip** — insert against a real thread, the exact dashboard query returns it, the unique-per-thread index rejects a second lead on the same thread, 0 rows after rollback; every `LeadRow` field exists in the DB; deployed (app 200, `/leads` 307 unauthed, `/api/leads` 401, both crons 401 unauthed and degrading correctly when unconfigured).
- **⚠ NOT YET RUNNING — three credentials/config items, all needing Richard:**
  1. **The Google OAuth tokens were lost in the 2026-08-25 crash** (see the entry below). Until the mailboxes are re-consented, the lead sweep returns 503 and reads nothing. **`gmail.readonly` on info@ has never been granted** and `drive.readonly` is new for every mailbox, so this re-consent is required by this build regardless of the crash. `scripts/setup-google-oauth.mjs` was updated to cover both: it now requests `drive.readonly` and unions `GOOGLE_LEAD_MAILBOXES` into its mailbox list (**it previously covered only the two deal mailboxes — a scope or mailbox added to `google-workspace.ts` but not to that script mints tokens that 403 at the first call**).
  2. **`GOOGLE_DRIVE_KNOWLEDGE_FOLDER_ID`** — unset, so the Drive sync no-ops. Needs the id of the Drive folder holding capability statements / past performance / credentials.
  3. **`DINO_LEAD_EMAIL`** — unset, so forwarding a plumbing/HVAC lead 400s until an address is set.
- **Deliberately deferred:** Ber AI agent tools over `leads` (the queue is a human surface first); per-route notification on a high-scoring lead; a lead → contact link in the directory.
- **LIVE 2026-08-25 (evening).** All three mailboxes consented (info@ new; moose@ re-consented for `drive.readonly`, which only IT needs since `PRIMARY_MAILBOX` is what the Drive sync impersonates). **info@ holds 411 threads, 390 new** after the Gmail-side marketing exclusions — 21 were already captured from the other mailboxes, so the cross-mailbox dedupe works on the lead side too. First real triage results: **26 threads → 7 real leads, 19 filtered as marketing** with plain-language reasons (Zillow/Thumbtack/real-estate alerts, an IT penetration-testing bid, a hearing-officer solicitation — all correctly rejected and kept for audit). Scoring is discriminating rather than uniform: a Utah industrial flex building scored **consider 72**, a landscape package on a sewage lift station **consider 55**, and an out-of-state waterline replacement at Detroit Metro Airport **pass 40**. At ~40s/thread the 390-thread backlog drains over roughly five daily runs; steady state is a few threads a day.
- **Ordering bug found and fixed on the first live run:** triage consumed the entire remaining budget, so a lead read this morning could not be scored — and therefore could not be announced — until the NEXT day's run. For a bid invitation with a deadline that is most of the value gone. The orchestrator now holds back `TAIL_SHARE` (25%) of the budget for a **second scoring pass after triage**, so leads reach the queue scored and notifiable in the same run; the two passes' counters are merged, with the tail's `remaining`/`outOfTime` winning since it ran last. Verified live (3 scored in a run whose triage produced the leads).
- **CONFIGURED + LIVE 2026-08-25 (night).** `LEADS_NOTIFY_EMAIL=moose@,tuaone@` (comma-separated — `sendMail` puts the list straight in the `To:` header), `GOOGLE_DRIVE_KNOWLEDGE_FOLDER_ID` set to the "Knowledge Library for Ber Intelligence" folder, and **`APP_URL` set** — it was never set on this box, so until now the digest rendered **no link at all**. Digests verified delivered to both mailboxes and read back out of Gmail: subject "4 new leads", four **per-lead deep links** plus the queue link, all absolute tailnet URLs. `/leads` gained **`?lead=<id>`** support (mirroring `/tasks?task=`) so a link opens that lead's detail sheet on mount instead of dumping the reader on the queue to find it again. Drive sync runs clean (`seen:0` — the folder is real, listable by moose@, and simply empty so far).
- **"Drive isn't working" was indistinguishable from "the folder is empty", so both are now stated out loud.** A clean sync reports `seen: 0`, which reads exactly like a failure. Two additions: **`scripts/check-drive.mjs`** (standalone — reads the token file and calls Google directly, so it diagnoses the credential path whether or not the app is running; four checks ordered so the first failure is the thing to fix: scope → API-enabled-on-the-credential's-project → folder reachable by the impersonated mailbox → contents, each with its exact remedy), and a **Drive Knowledge Sync card on `/settings/health`** with its own `empty` state ("Connected, but the folder is empty") distinct from `failed`. `probeDriveKnowledge()` translates the three failure modes — API not enabled, missing scope, folder owned by another account — into the specific fix rather than a raw Google error.
- **The Drive API had to be enabled on the project that owns the OAUTH CLIENT (247991610437), not whichever project you are looking at.** It was first enabled on two freshly created projects with no effect; Google checks the API against the credential's project. Worth remembering — the error names the project number, so read it.
- **`explainTokenError` named the wrong APIs.** Its `accessNotConfigured` branch hard-coded "Enable the Gmail, Calendar, and People APIs" — written before Drive existed, so the day the Drive sync first ran it pointed the reader at three APIs that were already enabled. It now surfaces Google's own message, which names the exact API and carries a direct enable link for the project.

**Incident 2026-08-25 (Studio froze and restarted mid-build; `~/berwilson-data` is gone — Google access DOWN, offline basemaps lost):**
- **What happened:** the Studio froze during the lead-list build and restarted (~10:24). Everything came back — Colima + all 8 Supabase containers healthy, launchd services loaded, the app serving 200, the database intact and backed up (nightly backup ran normally at 02:30 that morning, 6.5M db / 263M storage, offsite copy pushed). **`~/berwilson-data/` did not.** The whole directory is absent.
- **What lived there, and the blast radius:**
  - **`google-oauth-tokens.json`** — this file holds the OAuth **client id + client secret AND every mailbox's refresh token**. Losing it kills **all** Gmail, Calendar, Contacts, and Drive access at once, and it cannot be regenerated from anything on the box. Confirmed live: the last email-sweep run logged `{"error":"Google Workspace is not configured"}`. So the deal sweep, the lead sweep, `/calendar`, meeting prep, party enrichment, and the weekly task digest are all down until re-consent.
  - **`maps/us.pmtiles` (~17GB) + `maps/world.pmtiles` (186MB)** — the offline basemaps. `/map` will show its "basemap not installed" notice until they are re-extracted (runbook in `deploy/README.md`; go-pmtiles is still at `~/.local/bin/pmtiles` on the Studio). Nothing but the map is affected.
  - **`backups/purge-*.sql.gz`** — the pre-purge snapshots from the 08-24 data cleanup. Superseded by the nightly backups; no action.
- **Evidence, not inference:** the directory is absent from `$HOME`; free space rose from the ~205GB recorded on 08-24 to 224GB, consistent with ~17GB of map archives disappearing; a full-disk `find` located no copy of the tokens file elsewhere. **The cause was not determined.** `~/.Trash` is unreadable from a shell (TCC), so **check the Trash in Finder before re-extracting 17GB of maps** — if the directory is sitting there, restoring it recovers both the tokens and the basemaps instantly and no re-consent is needed.
- **Done in response:** `~/berwilson-data/{maps,backups}` recreated; `scripts/setup-google-oauth.mjs` corrected (it was missing `drive.readonly` and the info@ mailbox — see the lead-list entry); `GOOGLE_LEAD_MAILBOXES` added to `.env.local`. **Recovery is one command once the OAuth client JSON is re-downloaded** (Cloud console → APIs & Services → Credentials → the "Ber Intelligence" OAuth 2.0 Client ID → Download JSON — the copies in `~/Downloads` are for different, unrelated clients): `node scripts/setup-google-oauth.mjs ~/Downloads/client_secret_*.json`, which walks all three mailboxes.
- **Lesson worth acting on: `~/berwilson-data` is unbacked-up single-copy state holding the platform's most load-bearing secret.** The nightly backup covers Postgres + storage and nothing else. The tokens file is ~1KB — it belongs in the nightly `age`-encrypted offsite push alongside the database dump.
- **RESOLVED same day — it was in the Trash.** Richard restored the folder intact: tokens file (mode 600, untouched) plus both map archives (18GB `us.pmtiles`, 178MB `world.pmtiles`). Nothing had to be re-extracted or re-consented for the deal side. Verified after restoring: both stored refresh tokens still mint access tokens, the deal sweep ran and picked up new mail (tuaone 460 → 476 threads) with both mailboxes back to `state=complete`, calendar reads returned a real event, and map tiles render at street level for Salt Lake City, Ogden, Nuku'alofa and Tirana. **The app MUST be restarted after the file returns** — `_oauthStore` is cached in module scope, so replacing the file under a running process changes nothing.
- **The lesson was acted on immediately:** the secrets bundle now runs nightly (see the gap-closing entry above) and was run by hand on restore — `secrets-20260825-1827.tgz.age` (2.6KB) is on the mini. **A repeat of this incident is now a decrypt, not a re-consent.**
- **`scripts/setup-google-oauth.mjs` no longer needs the Cloud console.** The stored token file already carries the OAuth client id + secret, so a re-consent — adding a mailbox, or picking up a newly required scope — reuses it and goes straight to the browser. Only a genuine first run, or a lost token file, needs a downloaded client JSON (still accepted as an argument, which overrides the stored client). This removes the single hardest step from the recovery path.
- **Still open after the restore (both pre-date the crash, neither caused by it):** info@ has never been consented, so the lead sweep cannot read it; and the stored tokens were minted before `drive.readonly` existed, so the Drive knowledge sync would 403 — that scope is only needed on **moose@**, which is the mailbox `driveKnowledgeFolderId`/`listFolder` impersonate (PRIMARY_MAILBOX). tuaone@ needs nothing further.

**Done 2026-08-25 (LM Studio consolidated to one chat model + one embedding model; no code change):**
- **Richard had 4 models loaded/installed and wanted the minimum useful set.** Decided from the real workload, not intuition: over 45 days / 957 local calls, **output tokens exceed input tokens on every task** (thread summary 2,111 in → 2,354 out; email intake 2,073 → 4,852; fit assessment 2,789 → 3,088). Latency here is **generation-bound**, and generation speed on Apple Silicon is memory-bandwidth ÷ **active** params. The MoE reads ~1.7GB/token (~3B active at Q4); a dense 27B reads ~15GB/token — **roughly 3× slower per token on this box**, i.e. ~2min instead of ~40s on every extraction, brief, and agent turn. A dense model reasons slightly better per token and does not remotely pay for that.
- **FOUND: LM Studio had the WRONG model resident.** `qwen/qwen3.8-27b` was loaded at 16GB with no TTL while `.env.local` pins `qwen/qwen3.6-35b-a3b`. Both sets of weights are 36.6GB on a 36GB box, so every platform AI call was forcing a load/evict cycle instead of hitting a warm model. The 27B had **never served a single platform request** (all 957 local calls in `ai_queries` are the MoE).
- **Final state (LM Studio state only — no code, no env, no deploy):** `qwen/qwen3.6-35b-a3b` resident, **context 40960, parallel 1, no TTL**; `text-embedding-qwen3-embedding-0.6b` resident. Context was 61,696 — the largest real prompt measured is ~9.3k tokens (meeting intake), so the reclaimed KV cache is better spent as headroom. Parallel was 4; the sweep is strictly sequential (`for (const row of rows)`, `summarize-phase.ts:134`), so extra slots bought nothing and each cost its own KV cache. **Qwen3.8-27B-MLX-4bit deleted — 15GB reclaimed** (190→205GB free). Memory after: 23% free with both models resident.
- **The embedding model is NOT optional.** All **745 chunks** were embedded with it (1024→768 truncate + renormalize, `local.ts:284-310`). A change means wipe + re-embed; a mismatch breaks retrieval **silently** rather than erroring.
- **Nomic Embed was NOT deleted — it ships bundled with LM Studio** (`~/.lmstudio/.internal/bundled-models/`), so it returns on the next app update and is only 80MB. It has zero references in `src/`. Leave it; deleting it is churn.
- **Verified through the platform's own code** (alias-loader, not synthetic pings): `embedQuery` → 768 dims, L2 norm 1.0000; `match_chunks` → 5 real chunks; a real JSON extraction returned a valid object with the expected keys and correct assignees; `runAgent` autonomously called `get_steel_summary` and answered correctly ($3.6M revenue / $510k margin, matching the 08-24 data purge); `probeLmStudio()` reports ok. Throughput 64-76 tok/s vs the ~72 tok/s baseline — unchanged, as expected since the model itself didn't change. The win is no more load/evict thrash.
- **Confirmed while here (was an open item in the 07-07 cutover notes): LM Studio IS a login item**, so it comes back after a Studio reboot.
- **Known behavior, not fixed:** after a reboot nothing is preloaded, so the first call JIT-loads the model (~25s) and LM Studio then applies its **`jitModelTTL` of 3600s** — i.e. it unloads after an hour idle. The hourly email sweep keeps it warm in practice; the 6:30am brief after an idle night pays ~25s once. Raise that TTL in Developer → Server Settings if it ever matters.
- **Deliberately not done:** no vision model (Richard's call — PDFs already extract text locally via `unpdf`, audio goes through Whisper, so the gap only affects true image uploads, and a VL model competes for the same 36GB); no higher quant (Q5≈25GB / Q6≈29GB leaves no room for KV cache alongside Colima's 1.4GB and Whisper's 1.6GB transcription spikes).

**Done 2026-08-24 (health-check false alarm + Gmail send scope granted; DEPLOYED + VERIFIED):**
- **Richard reported `/settings/health` showing `Missing: GOOGLE_SERVICE_ACCOUNT_KEY`. His config was fine — the CHECK was wrong.** The required-env block hardcoded that variable, but this deployment runs **per-mailbox OAuth (mode 3)**, which `getAccessToken()` selects by the *ABSENCE* of the two service-account vars. So a correctly configured Studio failed that check permanently, and the remedy it printed (set the key, redeploy) would have moved the box to a mode the org policy blocks. Fixed: the check now calls `isGoogleConfigured()` — the same predicate the runtime uses — instead of naming any single var. **Rule: never assert a specific Google env var in a health check; the valid var set is mode-dependent.**
- **Three adjacent messages in the same file assumed service-account mode** and would have sent Richard down the wrong path during a real outage. All now branch on `mailbox.signingMode === 'per-mailbox OAuth'`: (1) the "Connected" detail claimed *"access does not expire, nothing to reconnect"* — untrue for OAuth, where a password change or an admin revoking app access kills a refresh token; it now says so and gives the recovery command; (2) "disconnected" pointed only at `GOOGLE_SERVICE_ACCOUNT_KEY_FILE`; (3) "broken" pointed only at the domain-wide-delegation console. OAuth mode now gets the `setup-google-oauth.mjs --only <addr>` path in all three.
- **`gmail.send` granted — the digest can actually send now.** The refresh tokens were minted 2026-08-23, before `gmail.send` joined `SCOPES`; verified against Google's token endpoint that both mailboxes held only readonly scopes. Richard re-consented both; re-verified live that **both now carry `gmail.send`**. Tokens copied to the Studio and the app **restarted** — `_oauthStore` is cached in module scope, so replacing the file under a running process changes nothing until restart. **Rule: any token-file swap needs `launchctl kickstart -k gui/$(id -u)/com.berwilson.platform`, not just an scp.**
- **Send path proven end to end, not just the scope:** called the platform's own `sendMail()` on the Studio via the alias-loader pattern, `moose@` → `moose@` (inside the Workspace, no outside recipient); Gmail accepted it and a follow-up Gmail query confirmed **1 delivered message**. That is the exact path the weekly task digest uses.
- **`scripts/setup-google-oauth.mjs`: `prompt` is now `select_account consent`** (was `consent`). With only `login_hint` set, a browser already signed into an unrelated account (a personal gmail.com) can sail past the picker onto a consent screen for the WRONG account. `login_hint` happened to auto-fill correctly this time — luck, not guarantee. The picker is now forced. (Consent screen is **Internal**, so no verification prompt and no 7-day refresh-token expiry.)
- **Confirmed healthy while here:** the 08-24 mail catch-up fix is deployed and working — the sweep picked up new threads after reaching `complete` (moose 84→98, tuaone 486→522 at the 17:01 UTC run; 535 threads stored, 395 summarized, 140 pending, draining hourly). All five launchd services loaded. `tsc` + `next build` clean; eslint unchanged from baseline. macOS Finder-duplicate artifacts (`routes.d 3.ts`) had returned and were breaking `tsc` again — removed.
- **Still untested: the digest's CONTENT path.** `buildMemberDigests()` finds **0 candidates** because no open task carries a due date, so the Monday 7:00am run would go out empty. Put due dates on a few tasks, then trigger `/api/cron/task-digest` and read the result — note it is **idempotent per member per day** via `notification_log`, so a manual run consumes that day's slot.


**Done 2026-08-23 (Outlook → Google Workspace + the mailbox sweep):**
- **Auth replaced, not ported.** Microsoft Graph is gone: `microsoft-graph.ts`, `graph-search.ts`, the `/api/email/oauth(+callback)` flow, and the `integrations/types.ts` stub are deleted. New `src/lib/integrations/google-workspace.ts` authenticates with a **service account using domain-wide delegation** (no SDK — §11 intact) and impersonates each mailbox directly.
- **Auth is per-mailbox OAuth, NOT a service account.** Three modes exist in `google-workspace.ts` and `getAccessToken()` selects by config; only the third is usable here:
  1. downloaded service account key → **blocked**: the Cloud org enforces `iam.managed.disableServiceAccountKeyCreation` (Google's auto-applied security baseline, inherited, and IT would not carve out an exception in time);
  2. service account + IAM `signJwt` (Google signs with its own managed key, no key ever downloaded) → **works, unused**: needs `gcloud` on the Studio for a caller credential. Kept in the code; set `GOOGLE_SERVICE_ACCOUNT_EMAIL` to switch to it;
  3. **per-mailbox OAuth refresh tokens** → **IN USE**. Desktop-app OAuth client (loopback redirect, the only client type that allows it), one browser consent per mailbox via `scripts/setup-google-oauth.mjs`, tokens at `~/berwilson-data/google-oauth-tokens.json` (mode 600, on both machines, outside the app dir). Selected by the ABSENCE of the two service-account env vars. `/settings/health` reports the live mode.
  - **The regression to accept:** a refresh token can be revoked (password change, admin revoking app access), so "reconnect the mailbox" exists again in a smaller form. Recovery: `node scripts/setup-google-oauth.mjs --only <addr>` then scp to the Studio. `scripts/verify-google-auth.mjs` walks the whole chain and names the broken link.
  - Service account (unused) client ID `111229421328742032282`; OAuth client `247991610437-…` in project `berwilson-website` (now displayed as "Ber Intelligence"). **Nothing is stored and nothing expires**, so the entire AADSTS "reconnect the mailbox" apparatus in `system-health.ts` and `/settings/health` collapsed into one "can we mint a token" probe. Read-only scopes: `gmail.readonly`, `calendar.readonly`, `contacts.readonly`, `contacts.other.readonly`. Setup procedure: **`deploy/google-workspace-setup.md`**.
- **Three dead tables dropped**, not migrated: `email_tokens`, `graph_subscriptions`, `processed_emails` (the latter two had zero reads/writes anywhere in `src`), plus the unused `outlook_web_link` columns. `email_tokens` also carried a real hole — it granted `select` on raw access + refresh tokens to every authenticated user.
- **New mailbox sweep** (`src/lib/email-sweep/`, migration `20260823000001`) — reads ALL history in `moose@` + `tuaone@` and populates the CRM. Four resumable phases: **fetch** (page through Gmail, checkpoint the page token after every page → `mailbox_sync`), **map** (one AI call per thread → `ThreadSummary` with a deal/operational/noise triage, → `email_threads`), **cluster** (deterministic union-find grouping on canonical deal name + shared *external* participant → `thread_clusters`), **reduce** (one cluster → one `pending` review session via the existing `analyzeEmailReport`). Driven hourly by `com.berwilson.cron-email-sweep`; `POST /api/email-sweep/run` and `GET /api/email-sweep/status` for manual control.
- **Why map/reduce.** Measured on the Studio (2026-08-23): `qwen3.6-35b-a3b` at 64k loaded context reads one thread in ~25-50s. Feeding a whole deal's raw correspondence to the intake prompt would blow context and degrade badly, so the model only ever sees one thread (map) or a page of summaries (cluster reduce). Budget **~8-14h per 1,000 threads**; a full two-mailbox backfill runs for days. The queue drains **newest-first** so the live pipeline lands first. `analyze.ts`'s input cap is now provider-aware (`maxInputChars()`: 200k Gemini / 40k local) and it gained `documentText`, so the model can read a summary while the record still stores the real correspondence.
- **Gmail vs Graph, encoded:** `q` filters dates server-side (the client-side date pass is gone); a thread arrives whole in one call; but attachments are NOT inline — every file costs a `messages.attachments.get`, so `email-research/run` now only downloads bytes when analysis or staging actually wants them. Critically, **`threadId` is per-mailbox** — the same conversation in both mailboxes has two ids — so all cross-mailbox dedupe goes through the RFC 2822 `Message-ID` fingerprint.
- **Also rewired:** `/api/calendar/events`, `/api/meeting-intake/calendar-events`, and the daily-brief cron onto a provider-neutral `CalendarEvent` shape; party enrichment onto the People API (`graph_done` → `directory_done` end-to-end); the dashboard's mailbox alert off token staleness (which no longer exists) onto `mailbox_sync.state === 'failed'`. `middleware.ts` swaps the OAuth callback allowlist entry for the sweep cron. `tsc`, eslint, and `next build` all clean.

- **APPLIED + LIVE (2026-08-23).** Migration applied, deployed, and the first sweep has run. Fetch phase complete: **533 unique threads** (84 in moose@, 487 in tuaone@, 38 deduped as the same conversations across both — the Message-ID fingerprinting works). Summarizing runs hourly from there; ~25-50s/thread means the backlog drains in roughly 4-7 hours, not the days a bigger mailbox would have taken.
- **⚠ The DB is self-hosted on the Studio — `supabase db push --linked` targets the WRONG database.** `supabase/.temp/linked-project.json` still pointed at the retired hosted project (`qauclkrdejgtpywqixho`, AWS us-west-2) long after the move to self-hosted Supabase under Colima on the Studio. Pushing would have migrated the dead cloud DB and silently left the live one untouched. Fixes applied: `supabase/.temp/` is now **untracked and gitignored**, and `npm run gen-types` **fails loudly** with an explanation instead of running against the wrong target.
  - **How to apply a migration now:** `cat supabase/migrations/X.sql | ssh richardwhite@100.86.79.4 'export PATH=$HOME/.local/bin:$PATH DOCKER_HOST="unix://$HOME/.colima/default/docker.sock"; docker exec -i supabase-db psql -U postgres -d postgres -v ON_ERROR_STOP=1 --single-transaction'`. Back up any table being dropped first (`pg_dump --data-only`); `email_tokens` was dumped to `~/berwilson-data/backups/` before this migration dropped it.
  - **Type regeneration is currently impossible** and deliberately not depended on: the Supabase CLI runs `postgres-meta` in a container and needs a DB host reachable from both host and container, which Colima doesn't provide. `src/lib/email-sweep/db.ts` carries hand-maintained row types plus an untyped client precisely so the sweep never needs `database.ts`. The dead `email_tokens` block was removed from `database.ts` by hand.
- **Known benign build warning:** Turbopack reports "unexpected file in NFT list" naming `google-workspace.ts`, because credential paths come from env vars and can't be traced statically. Harmless — output tracing only shapes serverless bundles and this runs as a long-lived `next start`. Documented at the top of that file; do not "fix" it by hard-coding credential paths.

**Highest-leverage next work:**
1. **Refine the pursuit profile** on `/company` — a first draft was written 2026-07-03 (see below); Richard still needs to confirm delivery methods/contract vehicles, add the project size range (min/sweet spot/max), disqualifiers, and past performance. Those four gaps are what's left between "usable" and "sharp" fit assessments.
2. Optionally persist `fit_assessment` on `proposal_intake_sessions` (currently returned in the intake response but not stored).
3. Tend the known debt in §9 as it gets in the way.

**Done 2026-07-03 (migrations applied + pursuit profile seeded):**
- **All migrations through `20260704000004` are applied to prod** and `npm run gen-types` verified `database.ts` matches (byte-identical to the hand-maintained file). Note: `supabase db push` / `supabase db query --linked` DO work from Claude Code when Richard explicitly asks — the old "Richard runs migrations" notes below are historical.
- **Pursuit profile first draft written to `company_profile`** (via `db query --linked`, from Richard's direction: full-spectrum GC, prefab steel manufacturing, military-standard compliance, Native American-owned by a 501(c)(3)): capabilities, all 5 target sectors, geographies (UT flagship + Mountain West + nationwide federal), delivery methods (D-B/D-B-B/CMAR — **assumed, confirm**), contract vehicles (FFP/GMP/Lump Sum — **assumed, confirm**), differentiators, pursuit notes. `about` (the platform brief) untouched. Two certification rows seeded from the brief's credentials line: USACE CQM + DoD EM 385-1-1 (numbers/dates blank). `hasPursuitProfile` is now true — fit assessments no longer self-flag `profile_incomplete`. Left deliberately empty (don't invent): project size range, disqualifiers, past performance, annual revenue, diversity-cert booleans (Native-owned ≠ certified — verify Buy Indian Act / IIP / tribal 8(a) eligibility before claiming set-asides).
- **UX fix:** the Pursuit Profile section on `/company` now has its own Edit button (the only entry point was the "Edit Profile" button at the top of Company Identity — Richard couldn't find it).

> UPDATE THIS SECTION (and §9 if you resolve debt) at the end of every Claude Code session.
